FROM quay.io/keycloak/keycloak:26.0.0

# Copy entrypoint script that handles rebuild on environment change
COPY infra/docker/keycloak-entrypoint.sh /
RUN chmod +x /keycloak-entrypoint.sh

# Use entrypoint to handle rebuild and start
ENTRYPOINT ["/keycloak-entrypoint.sh"]
