FROM quay.io/keycloak/keycloak:26.0.0

# Build Keycloak optimizations (required before using --optimized)
RUN /opt/keycloak/bin/kc.sh build || true

# Start in optimized mode
CMD ["start", "--optimized"]
