FROM quay.io/keycloak/keycloak:26.0.0

# Use the default image - let Keycloak handle build/rebuild as needed
# On first start: will do build automatically
# On subsequent starts with same DB: uses optimized mode
CMD ["start"]
