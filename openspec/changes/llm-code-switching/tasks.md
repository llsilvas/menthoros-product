## 1. System Prompt

- [ ] 1.1 Rewrite `src/main/resources/prompts/system-prompt.txt` in English: agent role, safety rules, mandatory validations, output format constraints, and training principles
- [ ] 1.2 Add explicit instruction in the system prompt to fill free-text JSON fields (`justificativaIa`, `descricaoEtapa`, `objetivoSemanal`) in Brazilian Portuguese and to preserve English sports terms (`TSS`, `tempo run`, `Z1–Z5`, etc.) as-is

## 2. Optimized Template (`plano-treino-otimizado-claude.txt`)

- [ ] 2.1 Translate all structural headings and section instructions to English (keep `%s`/`%d` placeholders for athlete data untouched)
- [ ] 2.2 Translate inline analysis directives (mandatory analysis steps, validation labels) to English
- [ ] 2.3 Translate the output format section (JSON structure instructions, enum value lists) to English

## 3. PlanoTreinoPromptBuilder — Section Headers and Decision Block

- [ ] 3.1 Translate section heading strings in `buildOptimizedPrompt` to English (e.g., `## 📊 DADOS FISIOLÓGICOS E ZONAS DE TREINO` → `## 📊 Physiological Data and Training Zones`)
- [ ] 3.2 Translate `formatarDecisaoIntervalado`: heading, markers `[PROIBIDO]`→`[FORBIDDEN]`, `[DEGRADADO]`→`[DEGRADED]`, `[AUTORIZADO]`→`[AUTHORIZED]`, and surrounding instruction text

## 4. Formatters — Labels and Headings

- [ ] 4.1 `AlertasPromptFormatter`: translate `gerarAlertasObrigatorios` and `gerarHierarquiaDecisao` headings and labels to English
- [ ] 4.2 `MetricasPromptFormatter`: translate section headings and metric labels generated by `formatarMetricas` and helper methods to English
- [ ] 4.3 `RecuperacaoPromptFormatter`: translate `detalharRecuperacao` section headings and instruction text to English
- [ ] 4.4 `PeriodizacaoPromptFormatter`: translate `formatarPeriodizacaoProva`, `formatarEventoCompetitivoSemana`, and related heading strings to English
- [ ] 4.5 `VariabilidadePromptFormatter`: translate `analisarEstimulosRecentes`, `identificarMatrizVariabilidade`, and `gerarAlertasVariabilidade` headings and labels to English
- [ ] 4.6 `DisponibilidadePromptFormatter`: translate `formatarDisponibilidade` section headings and descriptive labels to English
- [ ] 4.7 `PaceHistoricoFormatter`: translate `formatarHistoricoPace`, `formatarTetoPace`, and `verificarPaceLimiarAtualizado` heading/label strings to English

## 5. Legacy Template (`plano-treino-prompt.txt`)

- [ ] 5.1 Translate structural instruction sections in `plano-treino-prompt.txt` to English (keep athlete data placeholders and enum values as-is)

## 6. Test Updates

- [ ] 6.1 Update `AlertasPromptFormatterTest` assertions to match new English labels
- [ ] 6.2 Update `MetricasPromptFormatter`-related test assertions (if any) to English headers
- [ ] 6.3 Update `PeriodizacaoPromptFormatterTest` assertions to English section headings
- [ ] 6.4 Update `VariabilidadePromptFormatterTest` assertions to English labels
- [ ] 6.5 Update `PaceHistoricoFormatterTest` assertions to English label strings
- [ ] 6.6 Run `./mvnw test` and confirm all formatter tests pass with no regressions
