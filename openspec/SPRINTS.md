# Menthoros — Planejamento por Sprint

Ordem de execução das changes ativas, organizada por sprint. **Prioridade: base de IA primeiro**, com features visíveis do treinador intercaladas para preservar time-to-value.

**Última atualização:** 2026-08-22, tarde (**`intervals-icu-activity-sync-scheduler` entregue e
arquivada — a trilha intervals.icu fecha a metade "polling" do non-goal.** Atletas conectados passam a
ter os treinos aparecendo sozinhos, a cada 2h, sem o coach colar id de atividade. Feita inteira num
dia. O desenho que importava — teto por contagem contra a cota de 100 req/usuário/dia — foi decidido
no `init`, não descoberto em produção; e o smoke real pagou por si de novo: achou 72 req/dia sendo
queimadas em atividades que a listagem já dizia que não serviam. Detalhes na trilha intervals.icu.)

Antes, na manhã do mesmo dia: (**`intervals-icu-oauth2-integration` entregue e arquivada.**
A conexão com o intervals.icu deixou de exigir API key colada à mão: o atleta autoriza por botão,
com escopo limitado e revogável no provedor. Backend#74 e front#83 mergeados com 51 segundos de
diferença — a janela curta importava, porque o backend deixa de aceitar API key e, entre um merge e
outro, quem já estava conectado ficaria sem sincronizar e sem botão para reconectar.

**O que essa change ensinou sobre o gate de DoR.** A revisão da Claude aprovou a spec; a passada
adversarial do Codex reprovou, com 2 blockers e 4 majors — **todos confirmados no código**. A
diferença não foi competência, foi a pergunta: a primeira conferiu se o que a spec **cita** existe,
a segunda perguntou o que a spec **deixa de dizer**. Todas as lacunas estavam na segunda categoria.
A mais séria: o `clientSecret` tinha default vazio e era a chave do HMAC que assina o `state` do
callback público — com chave `""` o state assinado vira forjável para qualquer atleta, **e o fluxo
continua funcionando, sem sintoma nenhum**. Um segundo achado, na revisão seguinte, era mais sutil:
o guard de conta duplicada rodava depois do find-or-create, e em JPA a entidade managed é gravada no
flush do commit mesmo sem `save()` — o "nada é persistido" do critério de aceite dependeria de um
rollback que nunca aconteceria.

**E o que o smoke ensinou de novo.** Três defeitos só apareceram fora dos testes: o callback
redirecionava para fora do hash (o feedback nunca chegaria à tela), `setSearchParams` não surte
efeito neste app, e o provedor concede `ACTIVITY:WRITE` mesmo pedindo `READ`. O último ainda está
aberto, como task adiada. Detalhes na seção de concluídas.

Antes: 2026-08-20 (**`polish-workout-profile-legibilidade` entregue e arquivada.**
Segunda change da linha do perfil de treino, nascida de uma navegação de verificação em treinos
reais logo após o merge da primeira. Corrigiu duas afirmações falsas do gráfico — a razão
trabalho:recuperação que contradizia o próprio desenho, e a zona escrita na prosa sendo descartada —
mais quatro acabamentos. Detalhes na seção de concluídas.

**O número que essa linha de trabalho produziu:** somando as duas changes, **oito dos defeitos mais
sérios não apareceram em teste** — vieram de renderizar e olhar a tela. Três foram causados por
correções anteriores, feitas sem ver o resultado. Vale como regra prática: depois de mexer em como
alguma coisa é desenhada, olhar o desenho não é opcional.)

Antes: 2026-08-16 (**`intervals-icu-oauth2-integration` reescrita e escalonada —
a trilha intervals.icu ganha seção própria.** O founder confirmou que o app OAuth (client ID `663`)
foi liberado pelo David, e a spec de 02/08 foi verificada contra a API real antes de qualquer código.

**Três premissas da spec estavam erradas, e todas as três teriam compilado.** Ela foi escrita por
analogia com o Strava, sem confirmar o contrato: (a) o token endpoint é `intervals.icu/**api**/oauth/token`,
não `/oauth/token`; (b) a resposta **não traz `refresh_token` nem `expires_in`** — o token não expira
e não há refresh flow, mas a spec mandava popular `tokenExpiraEm` a partir de um campo inexistente;
(c) os escopos são **maiúsculos e separados por vírgula** (`ACTIVITY:READ,CALENDAR:WRITE`), não
`activity:read`. Nenhuma das três quebra o build — as três quebram em runtime, contra um servidor
de terceiro, que é o pior lugar para descobrir.

**A lacuna mais cara não era um erro, era uma ausência: nada na spec trocava Basic por Bearer.**
O client autentica com `setBasicAuth("API_KEY", apiKey)`; OAuth exige Bearer. Implementar a spec
como estava entregaria um fluxo de autorização perfeito e um token que nenhuma chamada aceita.
Investigar isso trouxe a boa notícia da change: os **seis** call sites já leem
`conexao.getAccessToken()` e repassam, então a troca é de **uma linha** e nenhuma assinatura muda —
a entidade sempre chamou o campo de `accessToken`, era o conteúdo que era uma API key.

**Um escopo a mais, por evidência de produção.** A spec pedia só `ACTIVITY:READ`, pensando em
ingestão. Mas o **push de treinos ao relógio já roda desde 2026-07-14** e é escrita no calendário:
subir sem `CALENDAR:WRITE` quebraria o canal validado, e o sintoma só apareceria na primeira
aprovação de plano depois do deploy.

**E uma correção de segurança que o molde do Strava traria junto.** A spec copiava `state=atletaId`
cru. Lá é tolerável (quem dispara é o técnico, superfície interna); aqui o fluxo é self-service e o
callback é público — sem assinatura, quem descobrir um `atletaId` vincula a **própria** conta
intervals.icu ao registro de outro atleta. Virou state assinado com HMAC, stateless, sem tabela nova.
Corrigir o Strava ficou **fora de escopo de propósito**: é dívida pré-existente e misturaria changes.

**Três decisões do founder mudaram o tamanho de S para M.** Quem conecta é o **atleta**
(self-service `/me`; a spec exigia `ROLE_TECNICO`, herança do molde Strava que não se aplica — quem
loga no intervals.icu é o dono da conta); a **API key é removida**, sem convivência; e o **pull
automático diário entra no roadmap imediato**, na change própria que já existe. O front deixou de ser
"change separada": remover a API key remove o campo de colar a key, então o botão novo é parte da
entrega, não um follow-up.

**A consequência foi aceita explicitamente, não descoberta depois:** no deploy, toda API key existente
vira `Bearer <api key>` e o provedor devolve 401 — os atletas conectados param de sincronizar até
reconectar. Não há caminho técnico para converter uma key em token OAuth; a reconexão seria manual em
qualquer desenho. **Daí a regra operacional que virou parte da change:** backend e front sobem juntos,
senão o atleta cuja key morreu não tem por onde reconectar.

**Gate externo aberto:** a redirect URI de produção precisa estar registrada no app 663 — o provedor
não aceita wildcard, e `http://localhost/` é sempre aceito, então o fluxo **passa em local e falha em
produção** se ninguém confirmar. É a única dependência externa da change, e está como task 0.1.)

Antes: 2026-08-15 (**`assessoria-settings-ui` entregue e arquivada — 31/31.**
Configuração da assessoria (nome + logo) fecha o segundo passo do Bloco 3. Três decisões saíram
diferentes do planejado, e todas por evidência, não por preferência.

**A propriedade da assessoria não cabia numa role.** O DoR reprovou a primeira versão: `Usuario.role`
é single-valued e `mapToUserRole` colapsa as roles do JWT numa cadeia `if/else`, então resolver o dono
como `PROPRIETARIO` o tiraria de `countByTenantIdAndRoleAndAtivoTrue` — a contagem de técnicos do
plano, com `maxTecnicos = 1` no BASIC. A assessoria apareceria com **zero técnicos** e liberaria um
segundo que o plano não comporta. `role` permanece `TECNICO`; a propriedade virou a flag `owner`,
espelhada do JWT a cada sync, com o Keycloak como fonte única.

**Três defeitos foram encontrados por teste, não por revisão.** (a) `@Lob` no Postgres mapeia para
`oid`, e large objects **sobrevivem ao `DELETE` da linha** — o `ON DELETE CASCADE` escrito para não
deixar lixo teria deixado lixo invisível em `pg_largeobject`; `byte[]` puro vira `bytea`, que morre
junto. (b) `@JsonIgnoreProperties(ignoreUnknown = false)` **não rejeita** campo desconhecido: quem
decide é `FAIL_ON_UNKNOWN_PROPERTIES`, `false` por default — com a anotação, `corPrimaria` no PATCH
respondia `200`, e o critério de aceite era letra morta. (c) No front, o campo de nome usava `''`
para "não carregado" e para "apagado", então **apagar o campo repunha o nome antigo** e o coach não
conseguia renomear a assessoria — a função central da página.

**Dois counterfactuals reprovaram testes que passavam.** O do gate de coerência provou que a brecha
era real: sem o guard, um token com `sub` do dono de A e `tenant_id` de B responde `200` e escreve na
assessoria errada. O do destaque da sidebar reprovou o **teste**, não o código: a asserção original
passava com e sem a correção, e virou `[aria-current="page"]`.

**Validado contra ambiente real, não só suíte:** signup → token → endpoints no HomeLab, com
`uso.tecnicos = 1` confirmando que o dono continua contado como técnico. Realm sincronizado nos dois
ambientes. **Pendência honesta:** falta um coach real abrir a página no Railway, e o backfill de
produção nunca foi exercido — não há assessoria de cliente lá.)

Antes: 2026-08-09 (**CI do backend no ar; `develop` protegida nos dois repos.**
`enable-backend-ci` entregue (backend PR **#63**, `f0b9911`) — 15/16 tasks, 1 adiada. O último
repositório sem CI passou a rodar `clean verify` em cada PR, em cada push para `develop` e nos dias
úteis às 09:00 UTC: **2368 unitários + 62 de integração**, e é a primeira vez que os 11 `*IT` executam
automaticamente. **A tese da change se comprovou construindo a própria change, de novo** — como no
front, mas com defeitos de outra natureza: o primeiro run derrubou a hipótese de hermeticidade e achou
**três testes que só passavam na máquina do dev**. Dois exigiam Postgres em `localhost:5432` — e o
padrão hermético **já existia no mesmo pacote** (`HealthConfigTest` com H2), só não fora aplicado a
eles. O terceiro comparava precisão de timestamp que o `TIMESTAMPTZ` não guarda: **irreproduzível
fora de Linux**, portanto invisível a qualquer suíte local, com ou sem Docker. **O gate foi provado,
não presumido:** PR #64 quebrou um `*IT` de propósito e o merge ficou `BLOCKED` — conferido **fora de
draft**, porque draft bloqueia sozinho e provaria nada; e o push direto em `develop`, feito pelo dono
com `enforce_admins`, foi recusado com `GH006`. **A CA4 saiu mais fraca do que a spec supunha, e só
porque foi medida:** o deploy do Railway **concluiu 2min41s antes** de o CI em `develop` terminar —
correm em paralelo, o Railway não espera verificação. A ordem vem inteiramente do gate no PR, o que
promove a proibição de push direto de acessória a **estrutural**. **Descoberta com alcance além da
change:** no plano Free, repositório **privado não tem branch protection nem rulesets** — privatizar o
backend desmontaria o gate inteiro; ficou registrado antes de a chave ser virada. **Adiado:** a CA8
está **meio provada** — o agendamento existe e vai rodar, mas ninguém verificou se um run vermelho
chega a um humano, mesmo ponto cego herdado do `enable-frontend-ci`; e o tempo de CI (**4min18s** vs
1min55s local) foi medido **quatro vezes, sempre com cache frio**, porque cache criado numa branch não
é visível de outra — o número com cache quente sai no próximo PR e é ele que decide se vale dividir em
jobs. **Também registrado no `CLAUDE.md` da raiz:** duas sessões no mesmo repositório **exigem
worktree** — `git checkout` altera o diretório, não a sessão, e um commit desta change chegou a cair
dentro da branch de `keycloak-user-onboarding-auth` sem nenhum comando errado ter sido dado.)

Antes: 2026-08-05 (**Login migrado para PKCE; change arquivada INCOMPLETA, com o
corte do ROPC em aberto.** A change foi ao arquivo por decisão do founder com 4 tasks abertas — entre
elas o próprio objetivo de segurança. Para a pendência não sumir junto, ela virou **item 0 da fila**
("Corte do ROPC"), com a sequência e o pré-requisito de acesso admin registrados; quando for
retomada, merece change nova. PRs front **#54** (fluxo Authorization Code + PKCE, walking skeleton e E2E) e **#55**
(remoção do `AuthService` do repositório) mergeados; **#52** (convenção de E2E) também, depois de
destravado. **A migração não está concluída, e a distinção importa:** o `menthoros-web` ainda aceita
`grant_type=password` — quem tiver credenciais obtém token por ali hoje. O #55 tirou o caminho do
código, o que impede reintrodução por descuido, mas o provedor não mudou. As 4 pendências restantes
são **de infraestrutura**: sync do realm fora do HomeLab, deploy em produção, o corte propriamente
dito e a métrica. **O tema da semana foi validação achando o que teste verde não acha.** O walking
skeleton pegou os dois únicos defeitos da change — o reload voltava ao login (o design previa a
sessão vir do cookie do Keycloak; nada no código perguntava a ele) e **não existia ação de logout em
nenhum shell**, com `logout()` implementado e nunca chamado, defeito para o qual não havia teste
possível porque não havia código a exercitar. Depois, **levar o E2E ao CI achou mais dois**: a suíte
passava 15/15 aqui e caía para 5/15 no runner, porque o issuer do IdP falso estava escrito à mão e
casava com o `.env` da máquina do autor — arquivo que não é versionado; e havia uma corrida entre
`toHaveURL` e os redirects do fluxo, que fazia specs diferentes pipocarem entre rodadas idênticas.
**Ou seja: quatro defeitos, zero encontrados por suíte local verde.** Também destravado o PR #52, que
estava bloqueado por um motivo que não se parece com o sintoma — a branch nasceu **antes** do
`ci.yml` existir, então os checks obrigatórios não tinham como rodar e ficariam pendentes para
sempre; qualquer branch aberta antes do CI trava igual, e `gh pr update-branch` resolve. ⚠️ **Registrado
como pré-condição de arquivamento:** sem linha de base (task 0.6), a métrica primária do proposal não
é falsificável — arquivar alegando sucesso registraria um resultado que ninguém mediu.
Antes: 2026-08-04 (**CI do frontend e migração do login — dois PRs abertos.**
`enable-frontend-ci` implementada (front PR #53, **CI verde no próprio PR**: `verify` 2m27s + `e2e`
1m21s, runner limpo sem secret). **Preparar o CI encontrou mais do que a spec previa.** A suíte E2E
nunca era executada e estava podre: **6 de 15 specs passavam**. Faltava `webServer` no
`playwright.config`; **a porta do Vite não era fixa** — default 5173 com fallback silencioso, e a
porta é parte da identidade da app para o Keycloak, porque o `redirect_uri` sai de
`window.location.origin` e o client só registra `localhost:5174` (isso teria quebrado o walking
skeleton do PKCE); e o fixture de auth emitia JWT sem claim de tenant. No fim, **um bug de produção
que os specs detectavam sem plateia**: o item "Dashboard" da sidebar apontava para `href="/"`,
obsoleto desde que a landing assumiu a raiz — clicar nele levava o `ADMIN` para a landing pública.
E2E **15/15**. É a tese da change comprovada enquanto se construía a própria change. Também
mergeada a convenção que torna **E2E obrigatório em fluxo crítico** (front PR #52), com a
justificativa escrita a partir do histórico do repo — `add-coach-lgpd-consent` foi entregue com 762
testes verdes e dois bugs que só apareceram no navegador. **`migrate-login-to-authorization-code-pkce`
com blocos 0–2 entregues**: a senha deixou de passar pela aplicação. Dois achados de discovery
mudaram o desenho — **cross-site em todos os ambientes** (renovação por redirect, não iframe, porque
o silent renew falharia em silêncio) e o **`stateStore` do PKCE precisa persistir**, o que estava
implícito no default e teria quebrado 100% dos logins. **Sequenciamento decidido:** o CI vem antes do
bloco 2 do PKCE, porque o E2E de autenticação — que a convenção agora exige — depende do `webServer`.
Antes: (**Gates de ativação LGPD: enforcement em `report-only` e Termos de
Uso publicados.** PRs front #51 + backend #62 mergeados. A intenção era ligar o enforcement em `on`;
a verificação mostrou que isso teria efeito oposto ao pretendido. **O `CoachConsentDialog` pedia
aceite dos Termos de Uso, e o documento não existia** — sem rota, sem texto e sem link, só a Política
tinha. Em `on`, todo coach ativo ficaria impedido de escrever até aceitar algo que não tinha como
ler, com o aceite gravado em tabela append-only: consentimento não informado, que é pior do que não
ter consentimento. Além disso, as versões estavam divergentes — backend exigindo `2026-06-30` e
página exibindo `2026-08-02`. Entregue então o passo que a própria change previa antes da virada:
`report-only` (não bloqueia, registra quem seria bloqueado), Termos publicados em `/termos`, dialog
linkando os dois documentos, versões alinhadas em `2026-08-03`. **Dois gates seguem abertos e
impedem o `on`:** o texto dos Termos é rascunho pendente de revisão jurídica (5.1) e a comunicação
prévia aos coaches não foi feita (5.3). **Lição de registro:** o bloqueio que importava — Termos
inexistentes — não estava anotado em lugar nenhum; o `tasks.md` arquivado listava "publicar os
Termos" como gate, mas nada indicava que o dialog *já pedia aceite deles em produção*. Um gate
descrito como pendência administrativa era, na verdade, um defeito ativo. **Também hoje:
`migrate-login-to-authorization-code-pkce` criada** (M · Full) — o login usa Resource Owner Password
Credentials, grant **removido** do OAuth 2.1, o que além do risco impede MFA. É pré-requisito de
`keycloak-user-onboarding-auth`, cujo critério de aceite assume um fluxo PKCE que não existe. O
pré-mortem cross-model achou 5 Critical, quatro deles confirmados no código — com destaque para o
scope `organization` ser *optional* no client: esquecê-lo no fluxo novo derruba o app inteiro com 403
enquanto o login conclui normalmente. Antes: 2026-08-03 (**Bloco 3 — loop de auto-cadastro do coach — trazido ao doc.** As
cinco changes da jornada de entrada do treinador foram criadas em **2026-07-31** e viveram até hoje
**fora do `SPRINTS.md`**: nenhuma delas era referenciada aqui, então não apareciam em nenhuma fila de
prioridade. Duas já estão entregues (`add-coach-lgpd-consent`, `add-coach-settings-page`) e três
seguem com **0% de tasks** (`keycloak-user-onboarding-auth` → `assessoria-settings-ui` →
`coach-first-login-wizard`), mais `import-atletas-csv` em paralelo. Registrado como bloco próprio
porque a ordem entre elas é imposta por dependência, não por preferência — e porque o gate LGPD, que
abre a sequência, ainda está `off` esperando validação jurídica e a publicação dos Termos de Uso.
**Divergência de estimativa corrigida na mesma passada:** `keycloak-user-onboarding-auth` aparecia
como `S · ~8 tasks` no Bloco 1 e `0/28` no Bloco de Segurança; a spec foi reescrita em 31/07 e é
**L · Full com 20 tasks** — planejar pela estimativa antiga subdimensiona a change em ordem de
grandeza. **Regressão de conteúdo detectada e revertida:** o commit `7b9dbf6` ("docs(cpo): weekly
audit 2026-08-03", autor `Hermes Agent`) reverteu 15 linhas deste arquivo para a versão de 28/07 —
apagando o changelog de 02/08, a linha de `intervals-icu-activity-laps` ✅, os status de entrega de
`fix-tsb-recalculo-resiliente`/`fix-tss-planejado-divergente`/`add-coach-lgpd-consent`/`add-coach-settings-page`/`complete-authorization-controllers`,
as três changes abertas em 02/08 e três linhas do Radar. As 6 linhas que ele inseriu eram todas texto
antigo, sem conteúdo novo, então o conteúdo foi restaurado de `4e975fb` sem perda. O efeito prático
enquanto durou: o doc reapresentava como "prioridade desta sprint" uma change entregue em 30/07.
**Reconciliação do doc com o estado real das changes, na mesma passada** (varredura de `tasks.md` de
todas as 35 changes ativas contra os merges em `develop`): **`fix-volume-realizado-recalculo`
apareceu como órfã** — 11/11 tasks, PR backend #48 mergeado em **2026-07-23**, e **nenhuma menção a
ela neste arquivo**; ficou 11 dias entregue e invisível ao planejamento, mesma classe da auditoria de
20/07. `fix-reconciliation-it-auth` saiu do Radar como entregue (13/13, PR #59, 02/08) — as duas
seguem em `changes/` **sem arquivar**, anotadas antes da tabela de concluídas. O item de Radar
"Habilitar CI" foi fechado: virou `enable-backend-ci`, e sua pré-condição (o `verify` verde) foi
satisfeita pela própria `fix-reconciliation-it-auth`. Quatro contagens de task corrigidas contra o
`tasks.md` real: `refactor-llm-call-outside-transaction` (~18 → **0/20**),
`fix-tenant-validation-not-found` (~14 → **0/20**), `fix-fc-alvo-base-inconsistente` (20 → **2/21**,
já em andamento) e `add-retention-radar-lite` ("Pronta" → **1/28**, iniciada).
Antes: 2026-08-02 (**três changes abertas no mesmo dia.** **`fix-fc-alvo-base-inconsistente`** — bug reportado pelo founder: a FC enviada ao relógio sai **+10% a +21% acima** da prescrita, medido contra o banco de dev. O domínio raciocina em **%LTHR** e o canal relativo do padrão Garmin é **%FCmax por definição**, então não existe percentual que se possa enviar por ali e chegue certo; a correção resolve para **bpm absoluto**, que é a forma nativa do Garmin. **Raio de alcance medido:** 183 das 629 etapas com alvo de FC (28,9%). **A contradição do prompt está visível na base** — ele entrega zonas em bpm e exemplifica `% FCmax`, e 71% seguiram a instrução contra 29% que seguiram o exemplo. **`coach-meta-intensidade-editor`** (no radar, bloqueada pela anterior) traz o seletor de meta de intensidade do Garmin para o editor — mas a contagem **refutou sua premissa de urgência**: zero etapas caem em "não reconhecido", então ela se justifica por prevenção, não por dano medido. **`enable-backend-ci`** fecha a lacuna estrutural que deixou um `*IT` vermelho por 2,5 meses: o repo **não tem CI**, `develop` **não tem branch protection** e o único check nos PRs é um deploy do Railway — o PR mergeia sem nada ser compilado. **Duas lições de método.** **Primeira: medir antes de afirmar.** Eu previ inflação de "~18%" assumindo LTHR = 0,85 × FCmax; o dado real mostrou **+10% a +21%**, porque a razão varia por atleta. Direção certa, magnitude errada. **Segunda: dado refuta narrativa.** Argumentei que o campo de texto livre fazia o treinador perder prescrições em silêncio; a base mostrou zero ocorrências, e a justificativa da change teve de ser reescrita. Antes, no mesmo dia: **`fix-tenant-validation-not-found` ABERTA** — bug reportado pelo founder: 403 "Acesso negado" ao aprovar um plano da própria assessoria. O id enviado não existia em tabela nenhuma, e o `TenantValidationAspect` traduziu "não encontrei" para "tentativa de acesso cross-tenant". Duas consequências, e a segunda é a grave: a mensagem não ajuda o treinador, e o log de segurança passa a disparar `SECURITY_VIOLATION` para plano deletado — envenenando justamente o sinal que deveria indicar ataque. Vai para o Bloco de Segurança. Antes: **`intervals-icu-activity-laps` ENTREGUE e ARQUIVADA — treino do intervals.icu passa a chegar km a km.** Backend PR #61 mergeado; 2368 unitários + 62 de integração verdes, migration V74. Todo `TreinoRealizado` importado do intervals.icu nascia sem etapas: o coach não via o desdobramento do treino e duas skills de análise caíam em fallback degradado, entregando análise mais pobre para o atleta intervals.icu do que para o do Strava — sem avisar que a diferença era dado faltando, não desempenho. **A lição principal foi de processo:** o design inteiro presumia um endpoint separado de laps, por simetria com o Strava. Não existe — é `?intervals=true` no endpoint que já usávamos. Duas revisões e um pre-mortem gastaram esforço atacando consequências dessa premissa não verificada, e o achado HIGH do Codex (429 transitório virando perda permanente de etapas sob o scheduler) evaporou por construção quando a premissa caiu. **Confirmar o contrato externo antes de desenhar em cima dele**, não depois. **Três armadilhas de unidade**, nenhuma dedutível do nome do campo, todas pegas pelo smoke contra payload real: cadência de perna única, oscilação vertical em milímetros e inclinação em fração. **E uma quase-armadilha:** a coluna "Tempo Decorrido" do CSV do intervals.icu é `elapsed_time` — alinhar `duracao` com o que o treinador vê na tela injetaria tempo de atleta parado no TSS, no tempo em zona e no decoupling, inflando o TSB que sustenta a decisão da semana. Antes, em 2026-08-02: **`fix-tss-planejado-divergente` ENTREGUE e ARQUIVADA — BUG-CONF-001 fechado.** Backend PR #57 mergeado; 12/12 tasks, 2311 testes verdes. O TSS planejado e o realizado usavam fórmulas diferentes para a mesma grandeza, divergindo 2,4×–6×; agora compartilham um núcleo único. **A migração dos dados históricos foi removida do escopo por evidência:** executar em dev mostrou que só 3 das 129 linhas vinham da fórmula antiga — as outras 126 saíram do gerador de plano, que já operava na escala certa, e recalculá-las teria destruído valores melhores que o substituto. A decisão anterior, tomada duas vezes, assentava numa premissa que ninguém tinha testado. **Duas lições técnicas.** **Teste com colaborador mockado não prova escala:** o recálculo tinha três testes, todos com o calculador `@Mock`, e o valor esperado era o stub `49` — exatamente o resultado da fórmula errada. **Refatoração "neutra" precisa ser provada:** extrair o núcleo reordenou as multiplicações e, como ponto flutuante não é associativo, virou o arredondamento em 8 pares (duração, RPE), alterando em silêncio o caminho realizado. A suíte de 2307 testes não pegou; varredura exaustiva pegou, e o Codex chegou aos mesmos 8 casos de forma independente. Antes, em 2026-07-31: **`add-coach-settings-page` ENTREGUE e ARQUIVADA — arco de LGPD do coach fechado.** PRs backend #56 + front #48 mergeados; 5/10 tasks (bloco 3, click-through manual, não executado). A change anterior coleta o aceite, esta dá ao coach onde consultá-lo e exercer os direitos de titular. **Duas lições de processo saíram daqui.** **Primeira: spec envelhece rápido.** Escrita antes de `add-coach-lgpd-consent` existir, tinha 4 premissas mortas que o `init` pegou — incluindo uma que não compilaria (`CoachRoute` é union tipada) e uma que leria consentimento de outro tenant. **Segunda: Fast track não justifica pular o passe cross-model.** O Codex reprovou o DoR com um BLOCKER que dois revisores Claude e o autor deixaram passar — o CA4 descrevia um fluxo tornado *inalcançável* pela change anterior. Tamanho é proxy ruim para "a spec está certa": change pequena sobre uma recém-mergeada é onde premissa velha se esconde. O comando `implement` foi alterado para tornar o Codex obrigatório em toda trilha. **No QA, dois achados de segunda ordem:** a correção do gate de loading da change anterior tinha criado um spinner infinito no erro do `/users/me`, e preencher `avatarUrl` ativou carregamento de imagem externa em toda página do coach sem proteção — um campo que estava declarado e nunca populado. **E uma sugestão convergente de 3 revisores foi refutada:** colapsar as duas queries de consentimento quebra em rollback de versão. **Nem convergência dispensa verificação.** Antes, no mesmo dia: **`add-coach-lgpd-consent` ENTREGUE e ARQUIVADA no mesmo dia — criada, decomposta, implementada e mergeada.** PRs backend #55 + front #47 em `develop`; arquivada em `changes/archive/2026-07/2026-07-31-add-coach-lgpd-consent/`. 16/44 tasks — os blocos 4 (E2E manual), 5 (gates de ativação) e 6 (procedimento de bump) ficam abertos por natureza, não por esquecimento: o 5 depende de artefatos que não existem (Termos de Uso publicados, validação jurídica) e o 6 só se executa quando a Política mudar. **A change entra em produção inerte** — a flag `app.lgpd.consent-enforcement` nasce `off`, então o `403` não atinge ninguém; o que já opera é o modal e o registro do aceite. Fechar o bloco 5 é o que liga o enforcement. **Desbloqueia `keycloak-user-onboarding-auth`, `coach-first-login-wizard` e `add-coach-settings-page`.** **A pendência que mais importa é o bloco 4:** o click-through manual não foi feito, e foram exatamente os dois bugs pegos no navegador — link dentro do `<label>` marcando o checkbox em vez de abrir a Política, e `href` absoluto sem resolver sob `createHashRouter` — que passaram por 762 testes verdes. Pior: o teste que cobria o segundo **passava para a forma correta e para a quebrada** (`MemoryRouter` não reproduz hash routing), ou seja, documentava uma regressão que não pegaria. Lição concreta: asserção rígida na string errada dá falso verde, e suíte verde não substitui abrir o app. **QA gate com 5 revisores (4 Claude + Codex), zero Critical, 9 achados corrigidos**, 2 por convergência cross-model; um achado foi **refutado por verificação** (typo em `policy-version` não bloqueia permanentemente — o fluxo se auto-corrige), reforçando que nem o passe cross-model se aceita sem conferir. **Custo de processo a não repetir:** a V73 foi editada depois de aplicada no Postgres de dev, exigindo `DROP TABLE` + limpeza do `flyway_schema_history` — a migration ainda era da branch, mas o banco compartilhado já não era; reparar antes de rodar a suíte inteira teria evitado. **Decisão de processo registrada:** as specs foram pushadas direto em `master` do `menthoros-product`, divergindo do precedente do PR #10 — escolha consciente do founder. Antes, no mesmo dia: **`add-coach-lgpd-consent` criada, decomposta e aprovada no DoR; `add-coach-settings-page` extraída dela — ambas registradas no Bloco de Segurança.** O coach como titular de dados estava descoberto: Política e RIPD cobrem atleta e assessoria, não o treinador. A change original juntava consentimento com página de perfil; o `/change` cortou no seam — **consentimento bloqueia `keycloak-user-onboarding-auth` e `coach-first-login-wizard`, a tela de perfil não bloqueia ninguém**, então separá-las adianta o destravamento. **Trilha escalada de Fast para Full** pelos critérios do `config.yaml` (dois repos, schema, contrato de API, risco legal) — a proposta original declarava só "S" sem trilha. **A revisão contra o código real derrubou premissas da spec:** o endpoint declarado (`/api/v1/me/consentimento`) **não existe** — `UsuarioController` é `@RequestMapping("/api/v1/users")`; campos nasciam em PT violando o ADR-0007; migration estava como `V??` (real: V73). **Achado que reenquadrou a change (pre-mortem cross-model):** o `JwtTenantFilter` chama `syncUsuarioFromJwt` em **toda** request autenticada, gravando os dados do coach em `tb_usuario` *antes* de qualquer gate — logo o `403` planejado **não é** base legal do tratamento, é controle de produto, e a base do sync (execução de contrato) precisa estar declarada no RIPD. A spec anterior vendia o contrário. **Product-review deu Refine** apontando algo que nenhuma das duas changes via sozinha: `coach-first-login-wizard` empilha um **segundo** overlay bloqueante logo após este, no momento que aquela change define como determinante para retenção — daí o stepper compartilhado ("Passo 1 de 4"). **DoR com divergência entre revisores, e o placar importa:** `spec-reviewer` deu READY na primeira passada; o **Codex deu NOT READY** e achou 5 lacunas de contrato que o revisor Claude não viu (JPQL vs SQL inconsistente entre `design.md` e `tasks.md`, cache de 1º nível stale, `WHERE` sem tenant, flag de rollout como `String` sem fail-fast, atributo de request sem contrato). **Uma das 5 foi refutada por verificação no código** — o Codex atribuiu o stale à entidade carregada pelo `JwtTenantFilter`, mas `open-in-view: false` nos três profiles somado ao `@Transactional` próprio do sync faz aquela instância chegar **detached**; o risco real é intra-método, e o `flushAutomatically` sugerido junto saiu por não haver mutação pendente. **Lição de método: nem o passe cross-model se aceita sem verificar** — 4 de 5 procederam, 1 tinha a mecânica errada, e a justificativa errada teria virado comentário permanente no repositório. Registrado também que o binário do `codex` está instável: uma das três invocações abortou sem veredito (`failed to renew cache TTL`). **Rollout desenhado como flag de 3 estágios** (`off`→`report-only`→`on`, default `off`, enum validado no boot): sem isso, o deploy bloquearia a escrita de **todos** os coaches ativos ao mesmo tempo, e qualquer bug no modal viraria incidente. **Gates de ativação separados do merge (bloco 5):** validação jurídica do checkbox, publicação dos Termos (o link é placeholder `#` hoje), aviso prévio e período em `report-only`. **Q4 reaberta e revertida no mesmo dia, por decisão do founder:** sai booleano + timestamp, entra registro **versionado e append-only**. A pergunta que virou o jogo foi o que acontece no *segundo* aceite: guardar a versão numa coluna de `tb_usuario` resolveria o re-consentimento e **destruiria a prova do aceite anterior** — exatamente a lacuna que a Q4 existia para fechar, só que deslocada para o passado. Com `tb_usuario_lgpd_consent` e `UNIQUE (usuario_id, policy_version, terms_version)`, a idempotência e a corrida passam a ser resolvidas pelo banco num insert, e **o update condicional atômico sumiu da spec** junto com toda a discussão de `clearAutomatically` que tinha custado uma rodada de revisão. Menos código e mais prova. Branches `feature/add-coach-lgpd-consent` criadas em backend (`e7ad39e`) e front (`d221e21`); nenhuma linha de código escrita ainda. Antes, no mesmo dia: **`add-external-call-resilience` ARQUIVADA — o loop estava aberto desde 27/07.** A change aparecia como "✅ ENTREGUE" aqui e simultaneamente como **ativa** em `changes/`, com 15/19 tasks marcadas: o PR #53 mergeou em 27/07 mas o `/done` nunca rodou. Descoberto ao cruzar o `SPRINTS.md` com o estado real de `changes/`. Fechada em 17/19. **A task 5.2 — teste manual de degradação — NÃO foi feita e viaja com a entrega:** derrubar/atrasar uma dependência real e confirmar que login e telas do atleta seguem respondendo. É o próprio sintoma que motivou a change, ou seja, a validação que mais importa é justamente a que faltou. O `tasks.md` original condicionava o arquivamento a ela; **essa condição foi quebrada de propósito**, porque segurar o arquivamento por uma validação operacional humana deixou a change em limbo por 4 dias — mesmo tratamento dado ao gate A1 de `add-weekly-review-llm-focus`. A 5.1 pôde ser encerrada: o defeito que travava o `verify` foi diagnosticado e virou `fix-reconciliation-it-auth`; a suspeita registrada em 27/07 de que "é defeito da base, sem relação com esta change" estava **correta**. **Duas inconsistências de documentação corrigidas na mesma auditoria:** `complete-authorization-controllers` aparecia sem tachado no Bloco de Segurança apesar de arquivada desde 14/07, e com 29 tasks quando o `tasks.md` tem 8 — a limpeza de 2026-07-20 pegou a duplicata dela no Radar mas não esta; e `keycloak-user-onboarding-auth` tinha duas contagens divergentes no mesmo arquivo (`~8` no Bloco 1, `28` no Bloco de Segurança — a real é 28). **Lição de processo:** o `SPRINTS.md` e `changes/` divergiram sem que nada avisasse. Vale rodar esse cruzamento na revisão de sprint prevista no `CLAUDE.md` da raiz, não por acaso. Antes, em 2026-07-30: **`fix-reconciliation-it-auth` criada — S · Fast, no Radar, sem sprint atribuído.** Saiu do `verify` rodado para validar a `fix-tsb-recalculo-resiliente`: `Task5p1ControllerIT` está vermelho desde **2026-05-14**, 14 dos 20 testes em 403, e **os dois defeitos são do teste, não da produção** — role `ROLE_USER` inexistente no domínio, e ausência de JWT impedindo o `JwtTenantFilter` de popular o `TenantContext`. **O que importa não é o vermelho:** os 3 endpoints de reconciliação manual estão **sem cobertura de contrato há 2,5 meses**, autorização e isolamento multi-tenant incluídos, no fluxo em que o treinador vincula atividade externa ao plano do atleta. Change é S · Fast com **zero diff em `src/main`**, e ficou Fast justamente porque o padrão de correção **já existia no repo** (`jwtDe` em `OnboardingSensitiveDataAccessIT`) — sem precedente teria subido para Full por incerteza de design. **Registro do método:** a sonda usada no diagnóstico passou com subject não-UUID e deu 200, mas por acidente — caía no branch fail-safe `syncFalhou` do filtro; foi o padrão canônico do repo que expôs a exigência de `UUID.fromString(keycloakId)`. Lição: sonda que fica verde não prova que o caminho exercitado é o certo. **Registrado junto, como gap estrutural no Radar: o `menthoros-backend` não tem CI** — `.github/workflows` inexistente, então o "CI verde + branch protection" do `CLAUDE.md` é fachada, e as branch protections não têm status check para exigir. É a causa raiz deste bug, não um vizinho: Surefire só roda `*Test`, `*IT` só em `verify`, e `verify` não roda em lugar nenhum automaticamente. Sequência correta é corrigir o IT **antes** de ligar o CI — CI que nasce vermelho é treinado a ser ignorado. Antes, no mesmo dia: **`fix-tsb-recalculo-resiliente` ENTREGUE e arquivada.** Backend PR #54 mergeado em `develop` hoje; specs no product PR #10. 26/26 tasks, 9 CAs cobertos por teste automatizado, 2215/2215 unitários + 15/15 ITs de TSB. O recálculo histórico passou de transação única sobre 400+ dias para blocos de 30 dias com transação própria (`TsbRecalculoExecutor`, `REQUIRES_NEW`). **A change entregou mais do que chunking:** corrigiu um contrato de falha que mentia — javadoc, log e mensagem de exceção prometiam rollback e restauração de um `backup` que nunca restaurou nada, e a lista `backup` na verdade servia para **delimitar o intervalo**, não para restaurar (achado do pre-mortem cross-model, que evitou uma remoção que quebraria os dias de descanso posteriores ao último treino). **Rebaselinada em `develop` durante a execução (task 0.0)** — a baseline original era `feature/testes-carga-referencia` @ `f9e754b`, sob a tese de "terminar o que aquele commit começou"; o teste de equivalência do CA5 provou o contrário: sem `@Transactional` o método lança `LazyInitializationException` para todo atleta, 5 erros de 5 naquele commit contra 5 verdes em `develop`. Aquele commit não deixou meio caminho andado, deixou um método que apaga e aborta. **Registro do método:** escrever a rede de segurança do CA5 *antes* de tocar no código foi o que derrubou a premissa da spec — sem ela, a change teria sido construída sobre uma baseline quebrada. Três mudanças de comportamento observável assumidas explicitamente: métricas de data futura passam a sobreviver ao recálculo (antes o `deleteByAtletaId` apagava tudo e o `dataFim` clampado em `now()` nunca as reconstruía), falha parcial **não** reverte os blocos comitados (Decisão 4, coberta por teste para que mudá-la seja decisão consciente) e `PlanoMetaDados` pode ficar stale se a consolidação falhar após os blocos (Decisão 7 — sem campo de status, sem migration). A janela de histórico misto (Decisão 6) ficou como contrato documentado com **zero diff nos cinco leitores**, provado por teste de leitura concorrente. **Duas pendências viajam com a entrega:** (1) **flake não diagnosticada** em `TsbRecalculoJanelaMistaIT` — falhou 1 vez em ~9 execuções durante o desenvolvimento, não reproduziu nas seguintes nem na validação final, e a **causa raiz não foi identificada**; a asserção passou a reportar classe e mensagem da exceção para que a próxima ocorrência seja acionável, mas a pausa de 50ms no laço do leitor é **hipótese, não diagnóstico**; (2) `./mvnw verify` completo segue vermelho por defeito **pré-existente** em `Task5p1ControllerIT` — 14 falhas com 403, agora **diagnosticado**: o teste usa `@WithMockUser`, que não produz principal `Jwt`, então `JwtTenantFilter` não popula o `TenantContext` e `getRequiredTenantId()` estoura; o header `X-Tenant-ID` que o teste envia é ignorado pela produção. Correção validada por sonda (post-processor `jwt()` com claim `tenant_id`), mas é rewrite do mecanismo de auth em 20 testes e **virou change própria** — 2 daqueles testes afirmam um contrato que não existe mais. **Achado transversal a registrar:** o repo `menthoros-backend` **não tem CI** (`.github/workflows` inexistente), então o "CI verde + branch protection" descrito no `CLAUDE.md` não existe na prática — foi por isso que o defeito do `Task5p1ControllerIT` ficou 2,5 meses invisível, já que o Surefire só roda `*Test` e os `*IT` só rodam em `verify`. Arquivada em `changes/archive/2026-07/2026-07-30-fix-tsb-recalculo-resiliente/`. Antes: 2026-07-28 (**`fix-tsb-recalculo-resiliente` criada e priorizada para esta sprint** — decisão do founder no mesmo dia. Origem: **BUG-TEC-002**, que já tinha um fix parcial commitado e que **piorou o quadro**. O commit `f9e754b` ("fix(BUG-TEC-002): remover @Transactional do recálculo histórico") alterou duas linhas: tirou a anotação e escreveu um comentário afirmando que existem "chunks de 30 dias com flush/clear". **O chunking nunca foi implementado** — `recalcularPeriodoComProgresso` segue um laço dia a dia. Com isso: o caminho do **onboarding** continua rodando 400+ dias numa transação única (herda a do chamador, então a remoção não o alcançou) e o caminho do **controller** passou a comitar o `deleteByAtletaId` de verdade, de modo que uma falha no meio deixa o atleta com histórico **parcial, sem volta e sem sinal** — enquanto o javadoc segue dizendo "dados são restaurados do backup" e o log, "a transação será revertida". O código mente sobre o que faz, e um CTL/ATL truncado vira recomendação de carga errada até alguém notar. **M · Full**, baseline `feature/testes-carga-referencia` (**não** `develop`, onde a anotação ainda existe e o comentário não). **Pre-mortem cross-model (Codex) rodado antes da implementação** e valeu o passe: achou (1) quebra de atomicidade do onboarding — com `REQUIRES_NEW`, falha após o recálculo deixa TSB novo sem baseline, estado que hoje não existe; (2) `PlanoMetaDados` fora do escopo transacional, lido por planner e prontidão; (3) cache `metadados-atleta` servindo valor pré-recálculo para a geração de plano; (4) leitores concorrentes reais na janela de histórico misto (PMC, home, dashboard do coach, fila de atenção, agregados); (5) `rampRate` dependendo de **D-7**, não só de D-1, no critério de equivalência numérica. Também corrigiu um erro da 1ª versão da spec — a lista `backup` **não** é "nunca lida": ela delimita o intervalo, e removê-la quebraria os dias de descanso posteriores ao último treino. Spec foi de 6 para 9 CAs. **Decisão pendente antes de começar (task 0.0):** há alteração **não commitada** na branch re-adicionando o `@Transactional`, ou seja, alguém está revertendo o `f9e754b` — o alvo da change não muda, o ponto de partida sim. **`add-external-call-resilience` ENTREGUE** — backend PR #53 mergeado em `develop`, suíte 2215/2215; o `verify` segue vermelho por defeito **pré-existente** em `Task5p1ControllerIT`, confirmado rodando o mesmo IT em `origin/develop`. Antes: 2026-07-27 (**`add-weekly-review-llm-focus` — Fatia 2 de 3 — ENTREGUE e arquivada.** Backend PR #52 mergeado em `develop` hoje, fechando a task 4.2; o front PR #46 já tinha ido em 26/07. A ordem saiu invertida em relação ao plano — a janela de campo `undefined` no card, que a D13 aceitava como "curta", durou um dia inteiro em vez de minutos; sem impacto, porque a flag nasce `false` e o card degrada para os estados de empty já cobertos por teste. Arquivada em `changes/archive/2026-07/2026-07-27-add-weekly-review-llm-focus/`. **Com isso o épico `weekly-athlete-review` está fechado nas três fatias** — consolidação determinística, narrativa de foco e card do coach. **Uma pendência viaja com a entrega:** o gate A1 de custo LLM — task 0.4 — foi adiada no arquivamento por não ser task de código; exige tráfego real com a flag ligada, que não existe pré-pilot. Enquanto o gate não fecha, produção roda só o template determinístico e o custo LLM da feature é zero. A instrumentação para medir já foi entregue nas tasks 1.4 e 3.4. Antes: 2026-07-26 (**`refactor-llm-call-outside-transaction` criada e promovida a PRIORIDADE ALTA no Bloco de Segurança** — decisão do founder no mesmo dia. A chamada ao LLM roda dentro da `@Transactional` da geração de plano: ~80s segurando uma conexão do pool (default 10), com o lote de 4 em paralelo fixando 4 delas — sobra pouco para login, telas do atleta e dashboard. **O ponto de ruptura é calculável:** o lote leva `N × 20s`, então em **~90 atletas numa mesma assessoria** ele ultrapassa os 30 min do `recovery-limite-min`, cujo próprio comentário no `application.yml` afirma que "nenhum lote real dura tanto" — uma assessoria de 100 alunos já invalida a premissa e faz um lote saudável ser tratado como órfão no recovery. Agrava: a reação natural a lote lento é subir `BATCH_PLAN_LLM_CONCORRENCIA` (env var, sem deploy), e de 4 para 8 fixa 8 das 10 conexões. Não é risco hipotético — é aritmética de crescimento, e dispara na primeira assessoria grande. M · **Full com `design.md` bloqueante**: o código faz `Hibernate.initialize` no grafo do atleta e usa as entidades depois da chamada; fora da transação elas ficam detached e `LazyInitializationException` é o risco real de regressão. Precedente a seguir: `intervals-icu-activity-ingestion` já mantém a chamada HTTP fora da transação; o `assessoria-billing-asaas` a mantém dentro **de propósito**, mas por atomicidade com estado de terceiro — o que não se aplica ao LLM, que não muda nada lá fora. Antes: 2026-07-26 (**`add-weekly-review-llm-focus` (Fatia 2) IMPLEMENTADA — PRs abertos, aguardando merge.** Backend PR #52 + front PR #46 (renomeação coordenada do contrato, D13/ADR-0007); **mergear backend primeiro**, a janela curta de campo `undefined` no card é a que a spec aceita (task 4.2). Entregue: migration V72, narrativa `nextWeekFocus` com template determinístico + upgrade opcional por LLM em executor dedicado, `WeeklyFocusConsistencyChecker` (narrativa que contraria RECOVERY/MAINTAIN cai no template), injeção no prompt com janela de 7d (D11) e loop de aprendizado via `ConsumedReviewOutcomeResolver`. Backend **2191/2191**, front **748/748**. **Nasce desligada** — `menthoros.weekly-review.llm.enabled=false`, gate A1 de custo ainda aberto (0.4 não bloqueia o merge). **Dois ciclos de QA gate:** o 1º produziu 6.1-6.4 (tenant no caminho `@Async`, `@Retryable` que o `try/catch` matava, evento publicado antes do save com `planoId` nulo, truncamento da saída do LLM); o 2º produziu 6.5 (revisão consumida resolvida 2× por geração em leituras não-atômicas — o LLM podia ver uma revisão e o plano gravar outra) e, via **`/codex:review`, um P2 que nenhum dos três reviewers Claude viu**: o prompt era montado para uma semana e o plano salvo em outra quando o atleta já tinha plano futuro. Registro do placar: o passe cross-model achou defeito de comportamento real — vale manter no gate. **`add-external-call-resilience` priorizada para a próxima sprint** (decisão do founder), saindo de "sem sprint atribuído" que durava desde 2026-07-20: o gap deixou de ser teórico — a F2 adicionou um 3º call site de LLM sem timeout (`WeeklyFocusModelClient`) e o Codex mostrou a degradação (pool cheio ⇒ retry e fallback não disparam). Recomendação registrada na linha do Radar: **quebrar em dois PRs** — bloco 1 (timeouts) não tem dependência nova e não espera decisão nenhuma; bloco 2 (Resilience4j) segue sequenciado com `keycloak-user-onboarding-auth`. Antes: 2026-07-25 (**`add-weekly-review-coach-card` (Fatia 3) ENTREGUE e arquivada** — front PR #45 mergeado: card read-only "Revisão semanal" no drilldown do atleta, hook espelhando `useAthleteProfile` com 404 mapeado para empty (não erro), adapter + VM, estados loading/empty/error e read-only assegurado por teste; lint + build + suíte 748/748 verdes. Arquivada em `changes/archive/2026-07/2026-07-25-add-weekly-review-coach-card/`. **Fatia 2 (`add-weekly-review-llm-focus`) refinada e pronta para implementação** — gate DoR reprovou 2× antes de passar (anchor do hook errado: quem gera é `RevisaoSemanalListener`, não `EncerramentoSemanaService`; chamada LLM síncrona na thread do encerramento, que o scheduler usa em lote; captura do desfecho sem mecanismo definido). Grilling de domínio na sequência produziu 5 decisões: desfecho renomeado `focusOutcome`→`consumedReviewOutcome` e **movido para o `PlanoSemanal`** (a revisão é 1:N com os planos que a consomem — no campo único da revisão, rejeitar-e-regerar apagaria a rejeição que interessa ao moat; de quebra a revisão deixa de sofrer escrita pós-congelamento, mais fiel ao ADR-0006), 6 valores separando ausência de dado (`NOT_CONSUMED`/`NO_COACH_IN_LOOP`) de julgamento real, janela de validade da revisão consumida (semana anterior + 7d — revisão de 3 semanas atrás viraria desinformação no prompt), `focusSource` (`LLM`|`TEMPLATE`, sem o qual o gate A1 tem custo de um lado e nenhuma medida de benefício do outro) e **padronização de idioma de identificadores (ADR-0007)** — código novo em inglês, campo legado em PT normalizado só quando a change já mexe naquela entidade; a F2 deixa de ser backend-only por causa do PR coordenado de renomeação no front. Antes: 2026-07-24 (**`add-weekly-review-consolidation` (Fatia 1 do weekly-athlete-review) ENTREGUE e arquivada.** Backend PR #50 mergeado em `develop`: `tb_revisao_semanal` 1:1 com `PlanoSemanal`, consolidação determinística (aderência por contagem na janela + árvore de `recommendationType` sobre `tsb_fim`), geração no encerramento (listener `SemanaEncerradaEvent` AFTER_COMMIT, insert-if-absent idempotente), endpoint read-only coach-only com `weekOverWeekDelta`, e congelamento do sinal proposto (**ADR-0006** — regras ajustáveis não reescrevem a história do moat). Implementada em TDD (4 blocos), suíte completa **2134/2134 verde**, QA gate (code+security+clean-code) **sem Critical**. Arquivada em `changes/archive/2026-07/2026-07-24-add-weekly-review-consolidation/`. Fatias 2 (`add-weekly-review-llm-focus`, IA) e 3 (`add-weekly-review-coach-card`, front) seguem **ativas**, dependentes desta. Débitos anotados no tasks.md: teste automatizado de 403-por-role (limitação do slice `@WebMvcTest`, idem `CoachKudosControllerTest`) e reconciliação da falha silenciosa do listener (aceitável no pilot).) Antes: 2026-07-24 (**`add-retention-radar-lite` criada e trazida ao Radar canônico —
fecha o gap do Retention Loop 90d.** O CPO weekly (`artifacts/cpo-weekly/2026-07-24.md`) apontou que o
bloco Retention Loop 90d estava maduro em `prd/` (discovery + PRD + roadmap) mas **ausente do radar
oficial**, e sugeriu na seção "Fora da caixa #1" um radar *lite* rodando sobre sinais já disponíveis.
Verificação de código confirmou: **5 dos 7 sinais do RF2 já têm dado pronto hoje** (lacuna de treino,
aderência, sem prova/meta, readiness, plano vigente — reaproveitando `add-athlete-engagement-signals`,
`add-daily-readiness-checkin`, `athlete-onboarding-baseline`). Change criada como **fatia** do bloco
completo: 7º motivo `RISCO_RETENCAO` na fila de atenção existente (fase D1-D120 + risco explicável +
Next Best Action estático RF4), reaproveitando `SinalAtencao`/`CoachAttentionSignalEvaluator` — o coach
é informado na fila que já usa, sem tela nova. **M · Full, backend + front mínimo** (o tipo
`AttentionReason` é curado à mão, não gerado — exige 3 pontos de wiring no front). Trilha Full
concluída: **product-review (Go)** — reforçou o RF4 estático para o card não virar "radar sem ação";
**pre-mortem cross-model (Codex)** corrigiu 2 críticos (sinal de plano sem filtro de `status`/`reviewStatus`
→ falso negativo; fase D1-D120 quebrada por `createdAt` de atleta legado da migração V25) + achou o
`SugestaoCoachGeneratorJob` que a premissa "read-only" ignorava; **6 Open Questions decididas no DoR**
(Q1 corte por-motivo, Q2 sem efeito, Q3 peso 45, Q4 "sem prova" condicionada a histórico de prova, Q5
sub-sinal de baseline cortado → v1 com 5 sinais, Q6 relógio = data de onboarding + fallback). Pronta
para `/implement init`; RF5 editável/jornada/coorte ficam para o bloco completo. Adicionada ao Radar
(specs no horizonte). Antes: 2026-07-24 (**`weekly-athlete-review` refinada ao DoR (READY) e fatiada em 3 changes.** Após 2 passadas do `spec-reviewer` (NOT READY → READY) e anchors de código (backend + frontend), a change cresceu — 2 repos, endpoint novo, camada LLM com gate de custo — e foi dividida no **seam de risco de LLM** (decisão do founder): **`add-weekly-review-consolidation`** (backend determinístico — consolidação, `recommendationType`, `weekOverWeekDelta`, endpoint read-only; **sem LLM, sem gate A1**) → **`add-weekly-review-llm-focus`** (backend — narrativa por IA atrás de flag `menthoros.weekly-review.llm.enabled`, insumo na geração de plano, `focusOutcome`; **gate A1 de custo confinado aqui**) + **`add-weekly-review-coach-card`** (frontend, S · Fast — card read-only no drilldown do atleta `CoachAthleteProfilePage.tsx`). Q1/Q2 detalhadas e decididas: **Q1-B** (`weekOverWeekDelta` na v1) e **Q2-B** (card read-only no shell do coach na v1); tendência multi-semana (Q1-C) e tela dedicada (Q2-C) ficam pós-v1. A pasta `add-weekly-athlete-review` foi renomeada via `git mv` para `add-weekly-review-consolidation` (Fatia 1, pronta para `/implement init`). Antes: 2026-07-24 (**CPO product review do épico `introduce-coach-assistant-core-features` — 4 threads de IA não entregues avaliadas** (agent `product-reviewer`). Das 6 capabilities do épico, 4 já saíram (`coach-dashboard-shell`, `coach-attention-queue`, `recommendation-explainability` fatia determinística, `coach-suggestion-inbox`); restavam 4 threads de IA, avaliadas pela lente de produto. Decisões do founder: **(1) `add-post-workout-debrief` FECHADA como redundante** — o `WorkoutAnalysisListener` (painel "Coach Insight" em `TreinoCard.tsx`, Claude Sonnet por treino realizado) já entrega planejado-vs-realizado + score + recomendação em produção; o que falta de fato (camada determinística de zona/decoupling + captura do loop de aprendizado `PENDING/ACCEPTED`) já está nomeado em `add-workout-metrics-analyzer`, não nesta change. Spec **não deletada** (permanece em `changes/` como referência, fora de sprint), só marcada superseded. **(2) `add-weekly-athlete-review` ANTECIPADA como v1 simplificada** (puxada de Sprint 27) — maior alavancagem no North Star (tempo do coach), custo baixo (cadência semanal, não por treino), sem bloqueio de infra pesada; nasce sobre dado do log manual, sem métricas `.fit` de zona. **Gate:** o `proposal.md` é stub — falta `Tamanho/Trilha`, critérios de aceite testáveis e métrica de sucesso (exigências do `config.yaml`); refinar antes de `/implement init`, ancorando a métrica em minutos/revisão de atleta e amarrando ao sinal de aprendizado do `WeekSuggestion`. **(3)** resto de `recommendation-explainability` (explicabilidade de geração de plano e de pós-treino) formalizado como **parcial, bloqueado por `add-llm-tool-use`/`add-workout-metrics-analyzer`** — não órfão, não esquecido. **(4) `add-zone-confidence-management`** confirmada em **backlog frio** (Reconsider) — sem dor real pré-pilot. Nenhuma das 4 é gate do primeiro cliente pagante — são reforço de retenção pós-onboarding para o próximo ciclo de vendas. Perguntas abertas ao founder (não resolvidas): custo do `WorkoutAnalysisListener` vs. envelope R$1,10/atleta/mês (Sonnet por treino, não instrumentado); reavaliar se `first-party-ingestion` ainda é pré-req real de `add-workout-metrics-analyzer` (`fit-lap-derived-metrics` já dá decoupling/EF/GAP); formalizar o Coach Insight como change OpenSpec no `menthoros-product` — hoje só existe como change local do repo backend, fora da governança central.) Antes: 2026-07-24 (**Poda de 8 changes zumbis confirmada — CPO weekly.** O
relatório `artifacts/cpo-weekly/2026-07-24.md` (§4, recomendação 5) reclassificou 23 das 26 changes
ativas como zumbis (0% tasks, sem commit há meses) e recomendou podar 8 delas para fora do foco
coach-facing. Na apuração: as 8 — `add-continuous-daily-load-management`,
`add-macrociclo-structure`, `add-running-field-tests`, `add-taper-guidance`,
`add-zone-confidence-management`, `refine-tss-tsb-precision`, `add-race-evaluation-skill`,
`add-health-connect-ingestion` — já estavam textualmente deprioritizadas na seção "Pós-MVP" deste
doc (linhas ~301-330); o gap real era de **governança, não de priorização**: os specs seguem soltos
em `openspec/changes/` sem essa correspondência registrada em lugar nenhum. Fechado agora — as 8
ficam **confirmadas como backlog frio**, sem trabalho previsto até os gatilhos já documentados em
cada bullet do Pós-MVP (fim do MVP, RAG, planner, decisão de app mobile). **Não movidas** para
`archive/` (reservado a trabalho entregue) nem deletadas — permanecem em `openspec/changes/` como
spec de referência, só que fora de qualquer sprint ativo. As 15 zumbis restantes seguem como
decisões distintas do founder (pacote pré-beta `keycloak-user-onboarding-auth` +
`add-external-call-resilience`, debrief/review coach-facing, família RAG condicionada, bloco de
engenharia) — não entraram nesta poda.) Antes: 2026-07-20 (**Priorização por ROI a partir do CPO weekly.** O relatório
`artifacts/cpo-weekly/2026-07-20.md` listava `deterministic-planner-engine` como "0% concluído,
Sprint 17-18 recém iniciada" — desatualizado: a auditoria desta mesma sessão já havia confirmado e
arquivado a change como 100% entregue (ver abaixo). Com a capacidade liberada,
`athlete-onboarding-baseline` foi **puxada de Sprint 19-20 para Sprint 17-18** (decisão do founder) —
spec já pronta para `/implement init`. **Radar limpo** (`SPRINTS.md` linha ~370): removidas 10
linhas já concluídas/duplicadas da tabela de sprints principal (achado do CPO weekly), corrigido
`complete-authorization-controllers` (status incoerente) e a linha corrompida de
`intervals-icu-workout-push`; adicionados 2 itens genuínos ao radar —
`add-external-call-resilience` (gap de visibilidade, gate de segurança sem sprint) e o quick-win
"expor métricas .fit no drilldown" (XS, frontend-only, achado "fora da caixa" do próprio CPO
weekly). Decisões pendentes do founder, não executadas nesta sessão: sequenciamento de
`keycloak-user-onboarding-auth`/`add-external-call-resilience` (P0) e reavaliação de
`add-post-workout-debrief` (P2).) Antes: 2026-07-20 (**Auditoria de sprint — 10 changes órfãs
resolvidas.** Varredura
completa das 37 changes ativas em `openspec/changes/` (grilling + auditoria pedida pelo founder)
achou trabalho já entregue em produção há meses sem nunca ter sido arquivado, além de specs mortas
sem nenhuma evidência de implementação. Ações: **6 arquivadas retroativamente** —
`deterministic-planner-engine` (PR #46, 2026-07-19, tasks já 39/39 — só faltava mover a pasta);
`align-coach-dialogs-atleta` (entregue de fato via `standardize-coach-dialogs` em 2026-06-27, sem PR
próprio); `strava-oauth` e `strava-webhooks` (`StravaOAuthServiceImpl`/`StravaWebhookServiceImpl` em
produção desde 2026-04-26, tasks.md nunca marcado); `strava-async-import` (endpoint
`GET /sync-status/{atletaId}` em produção desde 2026-04-29); `strava-activity-sync`
(`DailyActivitySyncScheduler`+`MatchingDecisionEngine` em produção desde 2026-05-01, o próprio
tasks.md já registrava "MVP 100% complete"). **2 deletadas** (decisão do founder, sem registro em
archive) — `strava-conditional-insights` e `strava-risk-semaphore`: zero evidência de implementação
no código ou no git log; a segunda tem uma versão conceitual diferente já coberta por
`coach-attention-queue` ✅, mas o spec original (score 0-100, tabela `tb_risco_atleta`) nunca saiu do
papel. **2 pastas vazias removidas** — `refactor-threshold-orchestration/` e
`refactor-color-system-premium-v2/specs/` (conteúdo real já estava em `archive/2026-07/`, sobra de
arquivamento incompleto). Changes ativas: 37 → 27. **`intervals-icu-activity-sync-scheduler`
inserida — Sprint 23.** Proposal + design + tasks criados (M · Full, backend-only, sem migration): scheduler
cross-tenant que espelha o `StravaActivitySyncScheduler`, fechando a metade "polling" do non-goal
deixado por `intervals-icu-activity-ingestion` ("sync automático, scheduler ou webhook"). Product
review (GO, 6 achados de refinamento incorporados) e pre-mortem cross-model (Codex — 5 críticos + 5
moderados + 1 menor, todos corrigidos no design: cursor condicional, reload-antes-do-save, gate de
paginação, classificação retryable-vs-permanente de erro) concluídos.
**`intervals-icu-webhook-ingestion` inserida como placeholder — Sprint 24.** Segunda metade do
non-goal (caminho OAuth + webhook em tempo real); decisão do founder: **já nasce assumindo a
migração do fluxo de conexão do atleta de API key para OAuth** (mesmo modelo do
`StravaOAuthServiceImpl`) — não um detalhe a resolver depois, é o veículo da substituição
estratégica gradual do Strava pelo intervals.icu (que integra direto com Garmin, com menos
limitações de API). Proposal ainda não escrita; abre com `/opsx:propose
intervals-icu-webhook-ingestion` depois que o scheduler estiver validado em produção.) Antes:
2026-07-18 (**`refactor-threshold-orchestration` ✅ entregue** — refactor
interno sem mudança de comportamento observável (S · Fast, backend): extrai a orquestração de
inferência de limiar (staleness → busca de treinos/provas → precedência prova-vs-quintil →
mutação de `PlanoMetaDados` → log de outlier), antes em métodos privados de `TsbServiceImpl` só
alcançáveis via `Method.setAccessible`, para um novo colaborador público testável
`AthleteThresholdUpdater`. Unifica um predicado de staleness duplicado 3x (com divergência de
semântica de `null` não intencional entre as cópias) em
`ThresholdInferenceService.isFcLimiarDesatualizado`/`isPaceLimiarDesatualizado`;
`CoachAthleteProfileServiceImpl` e `ThresholdConstraintFormatter` migrados para os métodos
novos. `TsbServiceImpl`: 748 → 643 linhas (volta a respeitar o teto do CLAUDE.md). Os 10 cenários
de teste originais portados sem reflection; golden-master do prompt confirma saída byte-idêntica.
QA (code + clean-code) zero Critical; corrigiu null-check explícito no método agora público, teste
do caminho `assessoria == null` (dívida pré-existente) e 4 mensagens de log desatualizadas. PR
backend #45 mergeado; arquivada em
`archive/2026-07/2026-07-18-refactor-threshold-orchestration`. Suíte 1786.) Antes: 2026-07-18
(**`refactor-color-system-premium-v2` ✅ entregue** — paleta
"Premium v2.0": elimina colisão semântica entre categorias (trainingType/trainingStage) e tokens
semantic (danger/warning/success/info), tira o lime de marca de contextos indevidos (Lime
Discipline), garante contraste WCAG AA nos categóricos dedicados (`violet`/`magenta` clareados),
consolida `theme.premium.ts` como single source of truth (glass, sidebar). 3 testes automatizados
novos (não-colisão CA3, Lime Discipline CA2, matriz de contraste — 106 casos). Visual diff ao vivo
nas 3 telas-âncora (cockpit/plano/detalhe de treino) achou e corrigiu 2 bugs reais de truncamento
de texto. QA (frontend + clean-code) corrigiu valor morto (`WORKOUT_STATUS_COLORS.PARCIAL`) e
mecanismo de enforcement morto/desatualizado (`forbidden-uses.ts`); duplicação
`colors.ts`/`theme.premium.ts` registrada como follow-up, não corrigida. PR front #40 mergeado;
arquivada em `archive/2026-07/2026-07-18-refactor-color-system-premium-v2`. Suíte 676.) Antes:
2026-07-17 (**`add-athlete-billing-plan` ✅ entregue** — dados de cobrança
do atleta: `tipoPlanoAtleta` + `dataVencimentoPlano` (migration V57, aditiva) com status derivado
`StatusVencimentoPlano` (EM_DIA/PROXIMO_VENCIMENTO/VENCIDO, janela de 7 dias), exposto no CRUD de
atleta, no perfil e no roster do coach; edição em `AtletaDialog`, badge no perfil, coluna
"Vencimento" no roster. QA gate (4 revisores paralelos) corrigiu 2 Important: `GET
/api/v1/atletas`/`{id}` sem `@PreAuthorize` (IDOR horizontal, agravado por esta change — restrito
a TECNICO/ADMIN) e coluna de vencimento sem ano no roster (dd/MM → dd/MM/yyyy). PRs backend #44 +
front #39 mergeados; arquivada em `archive/2026-07/2026-07-17-add-athlete-billing-plan`. Suíte back
1772 / front 570.) Antes: 2026-07-17 (**`infer-threshold-from-race-result` ✅ entregue** — 2ª fonte
de `paceLimiarEstimado`: uma prova real, válida e recente (5000-21097m, 90 dias) passa a ter
precedência sobre o quintil de `infer-thresholds-from-recent-workouts`, via fórmula de Riegel
isolada (`+8s/km`, sem reusar `RiegelCalculator`). Migration V56 (`fonte_limiar_pace`, aditiva),
persistida no cálculo e exposta ao coach. 4 rodadas de pre-mortem adversarial no DoR corrigiram 5
achados antes de implementar. PR backend #43 mergeado; arquivada em
`archive/2026-07/2026-07-17-infer-threshold-from-race-result`. Suíte 1747.) Antes: 2026-07-16
(**Sprint 17 `intervals-icu-activity-ingestion` ✅ entregue** —
sentido pull da integração intervals.icu: coach importa uma activity específica realizada pelo
atleta no intervals.icu como `TreinoRealizado` via `POST
/api/v1/intervals-icu/atletas/{id}/activities/import?activityId={id}`. Inclui flag
`autoSyncPausado` em `IntegracaoExterna` que pausa automaticamente o Strava do atleta ao conectar
qualquer uma das duas integrações (evita duplicação cross-fonte), com override manual
(`pausar-sync`/`retomar-sync`). **Smoke real com atleta founder** (Postgres do HomeLab dev)
encontrou e corrigiu um bug real de produção: `LazyInitializationException` (500 em vez de 200) no
re-import de uma activity já importada, mascarado pelo próprio teste de integração da change (um
`@Transactional` de classe escondia o mesmo bug) — corrigido com `@EntityGraph`. QA gate (3
revisores paralelos) corrigiu JSON sem escape em `metadadosSincronizacao` (achado convergente nos
3) + índice composto faltante; achado fora do QA gate original (unidade `velocidadeMedia` m/s→km/h
sem conversão) corrigido no fluxo do `/pr`. PR backend #42 mergeado; arquivada em
`archive/2026-07/2026-07-16-intervals-icu-activity-ingestion`. Suíte 1714.) Antes: 2026-07-14
(**Sprint 15 `intervals-icu-workout-push` inserida** — coach-in-the-loop físico: push de treinos aprovados ao relógio do atleta via intervals.icu → Garmin Connect. M · Full, ~31 tasks. Gate CA0 fechado com verificação no relógio do founder. Sucede `export-planned-workout-fit` (arquivada por canal inexistente). Modelo canônico `StructuredWorkout` + `WorkoutChannel` isola o formato de destino para canais futuros.) (**Sprint 14–15 `fit-running-dynamics-ingestion` ✅ entregue** — GCT, equilíbrio E/D, passada, oscilação/proporção vertical, temperatura, tempo em movimento e calorias do import .fit (migration V53, 15 colunas nullable); fecha o achado registrado por `fit-lap-derived-metrics`: `velocidadeMedia`/`paceMedia` por lap passam a usar `tempoMovimento` (timer time) em vez da duração elapsed quando presente e menor, corrigindo o pace em laps com pausa (design D6, CA7) — validado com dado real (pace de 10:11/km para 6:03/km na mesma volta com pausa da fixture original). Validação campo a campo contra CSV real do Garmin Connect (zero divergência); resolvida a assumption do lado do equilíbrio de GCT (é o pé esquerdo). QA gate (2 rodadas, code/security/clean-code + `/codex:review` + `/codex:adversarial-review`, GO) corrigiu uma `NumberFormatException` não tratada em upload adversarial, um risco de overflow de `NUMERIC(4,1)` em oscilação/proporção, e extraiu `RunningDynamicsOutputDto` (débito de DTO com campos posicionais demais, sinalizado pela 2ª vez). PR backend #38 mergeado; arquivada em `archive/2026-07/2026-07-13-fit-running-dynamics-ingestion`. Suíte 1379. Sequência .fit completa: `fit-lap-metrics-parser` ✅ → `fit-lap-derived-metrics` ✅ → `fit-running-dynamics-ingestion` ✅. Antes: **Sprint 16 `fit-lap-derived-metrics` ✅ entregue** — curva de EF por volta, Pw:HR e GAP interno, cálculo puro sobre dado já persistido pelo `fit-lap-metrics-parser`, zero migration; GAP nasce desligado por hard gate (calibração reprovou contra a fixture real — pausas dentro de laps distorcem o pace derivado de elapsed time). QA gate rodou 2 rodadas e corrigiu 3 Important: bug de partição temporal do Pw:HR, vazamento do envelope de decoupling para fora do detalhe, e uma regressão no Pa:HR introduzida pelo próprio fix do Pw:HR. PR backend #37 mergeado; arquivada em `archive/2026-07/2026-07-13-fit-lap-derived-metrics`. Suíte 1360. Antes: **Sprint 14 `fit-lap-metrics-parser` ✅ entregue** — elevação/potência/cadência por lap e sessão no import .fit (PR backend #36, validado contra CSV Garmin). `deterministic-planner-engine` deslocado para Sprint 17–18; `athlete-onboarding-baseline` para Sprint 19–22; `llm-code-switching` Sprint 23+; bloco engenharia Sprint 24+. Antes: Sprint 13 `add-aerobic-decoupling` ✅; Sprint 14 `progressao-treinos` ✅ + `fix-llm-routing-finops` ✅.)
change "encerrar a semana" via `/change`: treinador finaliza treinos não realizados (`PERDIDO`) e fecha o
`PlanoSemanal`, destravando a geração da próxima. Dividida em backend (M · Full, backend-only) + frontend
(S · Fast, fast-follow bloqueante) por decisão do founder. Posicionada na Sprint 11, paralela/antes de
`add-llm-tool-use` (não depende do LLM) e **antes da 12b `coach-batch-plan-generation`**, que consome o estado
`CONCLUIDO`. Product-review Go (2º passe) + pré-mortem adversarial (8 riscos aterrados no código). Notificação
ao atleta adiada para `add-weekly-athlete-review` (aceitável no beta). Antes: **Sprint 10 `fit-file-upload-ingestion` mergeada** —
upload de .fit (Garmin/Suunto/Coros/Polar/Wahoo) como treino realizado, dedup idempotente
compartilhado com o Strava via `TreinoDedupHelper`. QA gate rodou **4 rodadas** completas
(backend: code/security/clean-code-reviewer; frontend: frontend/clean-code-reviewer) sem
nenhum Critical, mas com Important corrigidos em cada rodada — dedup quebrava sem
`Session.startTime`, corrida de concorrência duplicava evento/TSB, flag `novo` hardcoded
quebrando o contrato 200/201, flood de laps sem limite, arquivo multiessão corrompendo dados
silenciosamente, `FonteDados` mascarando proveniência, parse dentro da transação (extraído
`FitTreinoPersister`), frontend descartando mensagens de erro curadas; PRs backend #24 + front
#32; arquivada em `archive/2026-07/2026-07-05-fit-file-upload-ingestion`).
Antes: 2026-07-04 (**Sprint 9.9 `add-athlete-retention-quick-wins` mergeada** —
3 quick-wins de retenção: feedback pós-treino determinístico, kudos coach→atleta (idempotente),
resumo semanal na Home (zero endpoint novo). QA gate (6 análises, backend+frontend, incl. Codex)
sem Critical — corrigidos `Kudos.motivo` sem length, `KudosDialog` não resetava seleção ao
reabrir (mesma classe do bug do 9.8), estado vazio fabricado no resumo semanal durante loading,
e acoplamento de `FAIXA_APRESENTACAO` entre shells coach/atleta; PRs backend #23 + front #31;
arquivada em `archive/2026-07/2026-07-04-add-athlete-retention-quick-wins`).
Antes: 2026-07-04 (**Sprint 9.8 `wire-readiness-checkin-to-athlete` mergeada** —
fecha o loop do check-in de prontidão: `QuickCheckInModal` completo (5 campos) ligado ao
`POST /api/v1/checkins` real + `GET /me/readiness` passa a preferir o check-in do dia; QA gate
encontrou e corrigiu bug de re-sincronização do modal + fuso horário + erro engolido; PRs backend
#22 + front #30; arquivada em `archive/2026-07/2026-07-04-wire-readiness-checkin-to-athlete`).
Antes: 2026-07-04 (**Sprint 9.6 `wire-athlete-progress-to-endpoints` mergeada** —
tela de Progresso do atleta com dado real (PMC, zonas, recordes, aderência) + 4 endpoints `/me/*`;
PRs backend #20 + front #28; arquivada em `archive/2026-07/2026-07-04-wire-athlete-progress-to-endpoints`).
Antes: 2026-07-03 (**Sprint 10 `fit-file-upload-ingestion` inserida** — upload de .fit como alternativa a Garmin/Strava API, priorizada antes de `add-llm-tool-use`; ver proposal em `changes/fit-file-upload-ingestion/`. Antes: **Sprint 9.9 `add-athlete-retention-quick-wins` adicionada** — 3 quick-wins de retenção XS, sequenciadas por ROI: feedback pós-treino > kudos coach > resumo semanal; ver proposal em `changes/add-athlete-retention-quick-wins/`. Antes: **Sprint 9.8 `wire-readiness-checkin-to-athlete` adicionada** — achado de auditoria de lacunas: `add-daily-readiness-checkin` (9k) ficou backend-only sem front de captura nem consumo real em `GET /me/readiness`; ver proposal em `changes/wire-readiness-checkin-to-athlete/`. Antes: **Sprint 9.7 `add-athlete-engagement-signals` adicionada** — streak de consistência + próxima prova/meta, recuperados após terem ficado de fora do split de `wire-athlete-shell-to-endpoints`; ver proposal em `changes/add-athlete-engagement-signals/`. Antes: **Shell do atleta — `/change` fechado e dividido em 2 changes** após product-review (Refine) + decisão do founder: **9.5 `wire-athlete-shell-to-endpoints`** (S · Fast, frontend-only — Home+Plano+Chat, fecha o loop coach→atleta, zero endpoint novo) e **9.6 `wire-athlete-progress-to-endpoints`** (M · Full — tela de Progresso + 4 endpoints `/me/*`, engajamento do atleta). Corte no seam natural: Home+Plano não precisam de backend, todos os endpoints novos são da tela de Progresso. Demo abre o app do atleta (confirmado). Ambas prontas para `/implement init`, 9.5 tem prioridade. Antes — inserção original como change única. Antes:) 2026-06-30 (**Go-live waitlist [3/4]:** `harden-waitlist-rate-limit` mergeada (Fast track, backend) — `server.forward-headers-strategy=framework` + `WaitlistRateLimitFilter` usa `getRemoteAddr()` (não o XFF cru), fechando a limitação de spoofing aceita no MVP; PR backend #15, 1053 testes. **CORS prod verificado ao vivo** (`Access-Control-Allow-Origin: https://menthoros.com` em `menthoros.up.railway.app`; origem não-listada → 403). Antes — [1/4]: `add-privacy-policy` mergeada (Fast track, sem change formal) — Política de Privacidade LGPD em `/privacidade` (10 seções: controlador, dados, base legal Art. 7º I, direitos Art. 18, etc.), fechando a task 4.3 deferida da `add-waitlist`; PR front #25. **Go-live pendente:** e-mail de contato real na política, logo/foto-bio reais, e DNS `api.menthoros.com` (hoje a API responde em `menthoros.up.railway.app`). Antes: `redesign-landing-premium` mergeada — landing premium token-driven + captura inline na waitlist + vídeo de showcase no hero + fix da rota `/` (home legada → `/inicio`) + link "Entrar"; PR front #22; arquivada em `archive/2026-06/2026-06-30-redesign-landing-premium`. **Supera `marketing-landing-page`** (arquivada junto). Antes: `add-waitlist` mergeada — waitlist pública pré-lançamento: `POST /api/v1/waitlist` + tela `/waitlist` + CTAs da landing repontados; PRs backend #14 + front #21; arquivada em `archive/2026-06/2026-06-29-add-waitlist`. Antes: 2026-06-27 — `standardize-coach-dialogs` mergeada — fecha o arco dos dialogs do coach: shell único `CoachDialog` + helpers de botão + fonte única de cor de treino, todos movidos para `src/shared/`, PR #19; `align-coach-dialogs-base-plano` arquivada — fundação `CoachDialog`; migração `coach/atletas` concluída — `coach-roster-operational-actions` + `coach-roster-athlete-crud` arquivadas; tríade do dashboard do coach concluída: `fix-coach-inbox-metrics`, `consolidate-coach-inbox-tabs`, `coach-training-strain` e `coach-race-form-prediction` arquivadas; `introduce-coach-assistant-core-features` / `coach-assistant-triage-ui` concluídos e mergeados — endpoint agregado do dashboard, triagem UI do coach (inbox decomposto, aderência e nível no roster), aderenciaPercentual no roster; próxima: `add-llm-tool-use` sprint 11–12))))))
**Fonte canônica de especificação:** `changes/<change-id>/` (estrutura flat — este doc NÃO move pastas)
**Roadmap por ondas/dependências:** `ROADMAP.md`
**Capacidade assumida:** 1 dev (solo/CTO), sprints de 2 semanas (~1 change média por sprint; changes grandes ocupam 2+).

---

## Como ler este documento

- `changes/` permanece **flat**. O sprint é um *atributo* da change, representado aqui — não na hierarquia de diretórios. Isso evita `git mv` em cascata a cada repriorização e preserva a identidade `change-id = nome da pasta` que o OpenSpec e o fluxo de archive (`changes/archive/YYYY-MM/`) assumem.
- Cada change é referenciada pelo seu `change-id`. Para o conteúdo, ver `changes/<change-id>/`.
- Ao concluir uma change: marcar `tasks.md`, mergear em `develop` e arquivar em `changes/archive/YYYY-MM/`. Atualizar a linha correspondente aqui.

---

## Princípio de ordenação desta versão

1. **Feature visível primeiro.** Coach e atleta devem perceber valor a cada sprint. Infraestrutura de IA pura (tool-use, RAG, skills migration) é agrupada e adiada — nunca bloqueia feature visível.
2. **Qualidade de IA intercalada.** Changes que melhoram a qualidade percebida do plano (progressão, code-switching, custo) entram intercaladas com features visíveis.
3. **Segurança antes do beta.** Gates de autorização e onboarding fecham antes de expor a usuários reais.
4. **Engenharia agrupada no fim.** Refactors (IaService, skills migration) e infraestrutura (tool-use, RAG) rodam em bloco coeso, sem fragmentar sprints de produto.

---

## Bloco 0 — Identidade mínima (desbloqueio) — ✅ CONCLUÍDO (Sprint 1, 2026-06-16)

Sprint 1 encerrada: ambas as changes mergeadas em `develop` e arquivadas (ver "Changes concluídas").

| Sprint | Change | Tasks | Objetivo | Dependência |
|:---:|---|:---:|---|---|
| 1 | ~~`add-current-user-endpoint`~~ ✅ | 13/13 | "Quem sou eu" → roteia shell coach/atleta, resolve contexto. | — |
| 1 | ~~`add-assessoria-onboarding`~~ ✅ | 24/24 | Base de tenant + role ATLETA. **Infra Keycloak de produção pendente** (operacional — runbook). | — |

---

## Bloco 1 — Features visíveis + qualidade de IA (repriorizado por ROI — 2026-07-06)

### Princípio de ordenação (v2)

**ROI direto primeiro.** A repriorização de 2026-07-06 inverte a ordem anterior (infra IA → features) para **features visíveis → qualidade IA → segurança beta → engenharia agrupada**. O coach e o atleta devem perceber valor a cada sprint; a infraestrutura de IA (`add-llm-tool-use`, RAG, skills migration, decomposição do IaService) é agrupada num bloco de engenharia posterior, executada em sequência coesa sem interromper entregas de produto.

Justificativa da mudança: análise CPO (2026-07-06) demonstrou que `add-llm-tool-use` (35→27 tasks, Sprint 10-11 original) é infraestrutura sem valor direto para o coach, com risco empírico não validado (tools + structured output `strict`), e dependência artificial bloqueando `coach-batch-plan-generation` (a feature mais pedida em escala). A separação system/user prompt (~4h) é organizacional/estrutural — custo neutro hoje (GPT-4o já cacheia prefixo automaticamente).

### 🤖 Guarda-chuva: Modernização do motor de IA

Thread que moderniza a geração de plano — skills determinísticas, rede de testes, estratégia EN/PT. Marcada com 🤖. Fases entregues e pendentes:

```
ENTREGUE:
  skills-core ✅ ─▶ eval-harness ✅ ─▶ introduce-plan-constraints ✅ ─▶ harden-plan-generation-resilience ✅
     (fundação)       (rede)            (seam Constraint + checker)       (reparo + retry)
                         │
                         ├▶ debito-tecnico ✅
                         │  (confiabilidade)
                         └▶ progressao-treinos ✅ ─▶ fix-llm-routing-finops ✅
                            (envelope confiável)      (FinOps: routing externalizado + métricas de custo)

PRÓXIMO (intercalado com features visíveis):
  llm-code-switching
  (EN/PT, ~20% ganho reasoning)

BLOCO DE ENGENHARIA (agrupado, Sprint 24+):
  refactor-iaservice-decomposition ─▶ add-llm-tool-use (spike+infra) ─▶ migrate-plan-prompt-to-skills
                                                   │
                                                   └▶ RAG family (engine → injury → coach-method)
```

- **progressao-treinos** ✅ (entregue 2026-07-08) e **llm-code-switching** são qualidade de IA perceptível pelo coach (planos melhores) — intercalados com features visíveis. A **fix-llm-routing-finops** ✅ (mesma data) instrumentou custo/cache por rota — a decisão de TTL Anthropic espera ≥2 semanas de `cache_hit_rate` em produção.
- **add-llm-tool-use** rebaixada para bloco de engenharia: fundação para RAG, sem valor direto para o coach. Inicia com spike de validação empírica (~3 dias) antes de investir na infraestrutura completa.
- **coach-batch-plan-generation** não depende mais de `add-llm-tool-use` (dependência artificial removida — batch funciona com o prompt atual).
- `refactor-iaservice-decomposition`, `migrate-plan-prompt-to-skills` e a família RAG são executados em sequência coesa no bloco de engenharia.

| Sprint | Change | Tasks | Objetivo | Dependência |
|:---:|---|:---:|---|---|
| — | 🤖 ~~`build-skills-core-foundation`~~ ✅ superada | 30 | **Fundação de skills já em `develop`** (contratos, `SkillRegistry`, `SkillOrchestratorService`, persistência `V32`, 7+ skills) — entregue por `introduce-domain-skills-architecture` + follow-ups. Arquivada como superada (ver "Changes concluídas"). | Bloco 0 |
| 2 | 🤖 ~~`add-plan-generation-eval-harness`~~ ✅ | S | **A rede (mínima).** Golden-master de `buildOptimizedPrompt` (533 linhas → 707 testes). Trilho de regressão ANTES da migração de formatters. Reescopada (product-lens): o `PlanQualityChecker` vai na migração; eval ao vivo no Pós-MVP. | skills-core (em develop) |
| 3–4 | 🤖 ~~`debito-tecnico-camada-ia`~~ ✅ | 41 | Confiabilidade: `.entity()` no lugar de parsing frágil, prompts externalizados, roteamento de modelo explícito (`TaskComplexity.PLANO`), limpeza. Gate antes de empilhar mais IA. | eval-harness |
| 5 | ~~`add-athlete-progress-endpoints`~~ ✅ *(visível, dado)* | 22 | Curva PMC, distribuição de zonas, PRs, readiness, resumo de hoje. **Camada de dados da shell** (PMC/zonas) e base da revisão profunda do atleta. Movida do Bloco 2 para destravar a shell. | Bloco 0 |
| 6 | ~~`add-coach-shell-dashboards`~~ ✅ *(visível)* | 16 | Roster + calendário semanal + KPIs por tenant. Primeira "casa" do treinador. Flags ainda não entregues (`hasPendingSuggestion`) iniciam `false`. Integração frontend fica em change separada. | progress-endpoints; Bloco 0 |
| 6b | ~~`wire-coach-shell-to-dashboards`~~ ✅ *(visível, front)* | M | **Liga as 3 telas do coach aos endpoints reais** (antes em mock): cliente curado + hooks + reconciliação DTO↔view-model (mapear/derivar/adiar). Fecha o valor visível da sprint 6 no front. | `add-coach-shell-dashboards` ✅ |
| 6-DX | 🔧 ~~`fix-openapi-client-generation`~~ ✅ *(tech-debt/DX)* | L | **`generate:api` determinístico restaurado.** Fase A (`@Tag` ASCII) + A2 (`array` em listarAtletas/listarProvas) + `--useUnionTypes` → saída limpa/idempotente/compilável. Fase B **reescopada (opção B)**: cliente `src/api` mantido como **fachada curada** (corrige tipos do OpenAPI + nomes ergonômicos); adoção do cru **adiada** (degrada tipagem + endpoints inexistentes). CA3/CA7 abandonados (doc). | — |
| 7 | 🤖 ~~`introduce-plan-constraints`~~ ✅ | M | **Anti-alucinação (o "coração").** Seam `Constraint` (key+descrição+params); formatters emitem Constraints; bloco mandatório [1] no topo + `PlanQualityChecker` (4 keys, offline) com contador Micrometer. Sem migrar skill. Golden regenerado. | eval-harness |
| 8 | 🤖 ~~`harden-plan-generation-resilience`~~ ✅ | M | Resiliência estrutural: reparo determinístico (`PlanoEstruturaReparador`) + 1 retry com feedback (`PlanoResilienceService`) → fim do 503 do `REGENERATIVO` 2 etapas (vira 422 com mensagem ao treinador). Dedup dos 4 validadores em `validarEstrutura3Etapas` + telemetria Micrometer (taxa de sucesso). Independente do seam e do strangler. | debito-tecnico ✅ |
| 9 | ~~`add-coach-attention-queue`~~ ✅ + ~~`add-recommendation-explainability`~~ ✅ *(visível)* | 13 + 9 | Fila de atenção on-demand (2026-06-18) + explicabilidade estruturada (2026-06-19). Hook diário do treinador + rationale determinístico por sinal. | shell-dashboards |
| 9b | ~~`wire-coach-identity-and-attention-queue`~~ ✅ *(visível, front)* | 14/14 | **Liga identidade e fila de atenção reais ao shell.** Substitui `mockCoach`/`mockTenant` por `GET /api/v1/users/me`; cria `CoachAttentionQueuePage` wired a `GET /api/v1/coach/attention-queue` com `explanation.rationale`; badge do Inbox com count real. Frontend-only, zero backend. | `add-coach-attention-queue` ✅, `add-current-user-endpoint` ✅ |
| 9c | ~~`add-coach-suggestion-inbox`~~ ✅ *(visível, backend+front)* | 21 | **Coach-in-the-loop completo (v1 sem RAG).** Workflow aprovar/rejeitar sugestões IA geradas dos sinais da fila de atenção: migration `tb_sugestao_coach`, listener idempotente, service + controller CRUD, UI 3-colunas real (substituindo o placeholder do 9b). Sem RAG no v1 — a fundamentação com citações entra pós-RAG via upgrade. | `wire-coach-identity-and-attention-queue`, `add-recommendation-explainability` ✅ |
| 9d | ~~`manual-training-entry-lightweight`~~ ✅ *(XS, visível, backend+front)* | 8/8 | **Desbloqueador de dado real.** Log manual do atleta: tipo + duração + distância + RPE + data → persiste em `TreinoRealizado` com `fonte=MANUAL`; TSS estimado calculado. Desbloqueia fila de atenção com dado real, antecipa debrief e métricas sem esperar `first-party-ingestion-architecture` (Sprint 23). | `add-current-user-endpoint` ✅, `add-assessoria-onboarding` ✅ |
| 9e | ~~`coach-plan-review-workflow`~~ ✅ *(M, visível, backend+front)* | 18/18 | **Desbloqueador de confiança e adoção.** Coach revisa e aprova planos gerados pela IA antes de chegarem ao atleta. `review_status` (AGUARDANDO_REVISAO → APROVADO/REJEITADO) em `tb_plano_semanal`; `GET /coach/planos/pendentes` + `GET /coach/planos/revisao?status=` + `POST aprovar/rejeitar`; `CoachPlanReviewPage` 3-colunas (filtros + fila + painel); badge de pendentes no nav. | `add-coach-shell-dashboards` ✅, `add-coach-suggestion-inbox` ✅ |
| 9f | ~~`athlete-profile-drilldown`~~ ✅ *(M, visível, backend+front)* | 16/16 | **Prontuário do atleta.** Tela `/coach/athletes/:id` com PMC 90d, aderência 8 semanas, plano vigente (7 cards), últimos 3 sinais e 3 sugestões, recordes. Endpoint agregador `GET /coach/atletas/{id}/perfil`. Navegação do roster → perfil. Coach tem contexto completo antes de qualquer decisão. | `athlete-progress-endpoints` ✅, `add-coach-attention-queue` ✅, `add-coach-suggestion-inbox` ✅ |
| 9g.1 | ~~`fix-intervalado-distance-calculation`~~ ✅ *(XS, backend)* | 10/10 | **Hotfix: distância errada em treinos intervalados.** Pós-processamento determinístico em `IaServiceImpl`: `corrigirDistanciasEtapasTemporais` deriva `distanciaKm` via `duracaoMin ÷ paceZona` (Z2 = limiar×1.20, Z1 = limiar×1.35) para AQUECIMENTO/DESAQUECIMENTO/RECUPERACAO antes de `expandirEtapasAgregadas`. PR #8 mergeado 2026-06-21. | — |
| 9g | ~~`coach-edit-planned-workout`~~ ✅ *(S, visível, backend+front)* | ~22 | **Fecha o ciclo de revisão.** Coach edita treinos individuais durante revisão do plano (`AGUARDANDO_REVISAO`): tipo, distância, duração, zona, RPE, TSS (recalculado automaticamente ou override manual). `PATCH /coach/planos/{id}/treinos/{id}`; migration V39 (`editado_pelo_coach`); chip "Editado manualmente" na UI; `editadoPeloCoach = true` alimentará `rag-coach-methodology-personalization` (Sprint 18). | `coach-plan-review-workflow` ✅ |
| 9h | ~~`infer-thresholds-from-recent-workouts`~~ ✅ *(S, backend+front)* | ~18 | **Calibração automática de zonas.** Quando `fcLimiar`/`paceLimiar` estão desatualizados (> 90 dias), infere o valor atual a partir do quintil superior dos treinos dos últimos 30 dias. Injeta como Constraint no prompt; retorna `limiareisInferidos` no response da geração; banner de transparência na `CoachAthleteProfilePage`. Inclui bug fix de alucinação de BPM para atletas sem teste formal. PR #10 mergeado 2026-06-23. | `coach-edit-planned-workout` ✅ |
| 9i | ~~`coach-add-workout-to-plan`~~ ✅ *(S, visível, backend+front)* | ~25 | **Fecha o ciclo de adição.** Coach inclui treinos não previstos pela IA durante revisão (`POST /coach/planos/{id}/treinos`); dialog com etapas dinâmicas e blocos repetidos; migration V41 (`adicionado_pelo_coach`); aviso double-day; guardrail 14 treinos/semana; chip "Adicionado pelo coach". PR #11 (backend) + PR #7 (front) mergeados 2026-06-23. | `coach-edit-planned-workout` ✅ |
| 9j | ~~`expose-form-status`~~ ✅ *(S · Full, backend+front)* | ~14 | **Dívida de confiança: forma consistente backend↔UI.** Backend expõe `statusForma` (`FaixaTsb`, 9 faixas) nos DTOs; front consome a forma atual (CoachInbox) sem recomputar `formFromTSB`. PRs: backend #13 + front #20, mergeados 2026-06-29. Arquivada em `archive/2026-06/2026-06-29-expose-form-status`. Follow-ups: taper boundaries (remove o último `formFromTSB` na projeção); dead code `AthleteRow`. | `add-coach-suggestion-inbox` ✅, `fix-coach-inbox-metrics` ✅ |
| 9k | ~~`add-daily-readiness-checkin`~~ ✅ *(M, backend-only)* | 36/36 | **Sinal preditivo de prontidão.** Check-in diário subjetivo (sono/humor/dores/energia/estresse) → `readinessScore` + `NivelProntidao` (PRONTO/CAUTELOSO/DESCANSAR); antecede queda de prontidão em 24–48h, antes do TSB reagir. Integrado como sexto portão em `IntervaladoElegibilidadeService` (bloqueio por DESCANSAR, atenuação por CAUTELOSO — overload aditivo, zero regressão) e no contexto do `PlanoTreinoPromptBuilder` (seção READINESS + instruction hardening). Endpoints `/api/v1/checkins`. **QA gate:** IDOR crítico encontrado e corrigido (atleta lia checkin de outro atleta do tenant) antes do merge. PR backend #19 mergeado 2026-07-03; arquivada em `archive/2026-07/2026-07-03-add-daily-readiness-checkin`. Front (UI de captura/dashboard) fica para change separada — fora do escopo aprovado no DoR. | `add-coach-attention-queue` ✅ (consumidor natural do sinal) |
| 9.5 | ~~`wire-athlete-shell-to-endpoints`~~ ✅ *(S · Fast, **frontend-only**)* | 17/17 | **Fecha o loop coach→atleta antes da demo (Home + Plano + Chat).** Reescopada 2026-07-03 após product-review (Refine) + decisão do founder: split. Home e Plano do atleta consumindo dado real (`GET /me/home`, `/me/readiness`, `/planos/{id}` — zero endpoint novo) + chat vira placeholder honesto. `readiness.factors` granular removido (sem fonte, D0.3); "TSS meta" → volume planejado/realizado (D0.5). Smoke manual corrigiu 403 (guard de role `/athlete`), 404 (vínculo automático de `Atleta`) e cor do card "Próximo" fora do padrão (`workoutTypeColor()`). Adendo pós-merge: redireciona ATLETA direto para `/athlete/home` no login (fix de corrida no `useUserInfo` memoizado). PR #27 mergeada (squash `ad64e766`); arquivada em `archive/2026-07/2026-07-03-wire-athlete-shell-to-endpoints`. | `add-athlete-progress-endpoints` ✅, `coach-plan-review-workflow` ✅ |
| 9.6 | ~~`wire-athlete-progress-to-endpoints`~~ ✅ *(M · Full, backend+front)* | 15/15 | **Progresso do atleta (engajamento).** Separada da 9.5 (justificativa distinta — atleta olhando o próprio histórico, não fechamento do loop; e onde vivem os endpoints novos). 4 endpoints `/me/*` (`metricas/historico`, `metricas/zonas`, `recordes`, `aderencia` — todos espelham métodos de serviço já existentes, D0.1) + wiring de `AthleteProgressPage`; KPI de volume derivado de `/me/treinos` já existente (D0.2, reaproveitando `useManualTraining` já existente — QA gate pegou uma duplicata de cliente/hook antes do merge). Smoke manual confirmou os 5 endpoints com dado real batendo com a Home. PRs backend #20 + front #28 mergeados 2026-07-04; arquivada em `archive/2026-07/2026-07-04-wire-athlete-progress-to-endpoints`. | `add-athlete-progress-endpoints` ✅, `manual-training-entry-lightweight` ✅ |
| 9.7 | ~~`add-athlete-engagement-signals`~~ ✅ *(XS, backend+front)* | 15/15 | **Streak de consistência + próxima prova/meta (engajamento do atleta).** Recuperada 2026-07-03: ideia surgida numa sessão CPO anterior, encaixada inicialmente em `wire-athlete-shell-to-endpoints`, mas ficou de fora sem rejeição explícita no split/refino daquela change — reencaixada aqui como change própria e pequena. Streak calculado client-side sobre `/me/treinos` (já existe); próxima prova via endpoint novo `/me/provas` (espelha `ProvaService.listarProvas`, já usado pelo coach). Adendo pós-PR: card de próxima prova também na Home (não só na tab Provas). QA gate corrigiu 3 Important no frontend (falhas silenciosas de rede viravam "sem dado"/"sem streak"; `diasFaltando` fabricado como 0). Security-reviewer confirmou débito pré-existente registrado (IDOR intra-tenant em `GET /atletas/{atletaId}/provas`, fora do escopo). PRs backend #21 + front #29 mergeados 2026-07-04; arquivada em `archive/2026-07/2026-07-04-add-athlete-engagement-signals`. | `manual-training-entry-lightweight` ✅ |
| 9.8 | ~~`wire-readiness-checkin-to-athlete`~~ ✅ *(S, backend+front)* | 15/15 | **Fecha o loop do check-in de prontidão (achado de auditoria).** `add-daily-readiness-checkin` (9k) foi entregue backend-only — o motor de elegibilidade (6º portão) e a seção READINESS do prompt estão prontos, mas nenhuma UI grava o check-in. Expandido `QuickCheckInModal` para os 5 campos do contrato + ligado ao `POST /api/v1/checkins` real + `GET /me/readiness` passa a preferir o check-in do dia sobre o score objetivo. **QA gate** encontrou (independentemente, por 2 revisores) um bug funcional real: o modal não re-sincronizava os campos quando o check-in de hoje chegava por fetch assíncrono, travando os sliders em valores default e arriscando sobrescrever o check-in real ao editar — corrigido com `useEffect` + teste de regressão. Também corrigidos: fuso horário (UTC vs. data local do atleta em `hojeIso()`), erro de fetch descartado sem aviso, janela de duplo-submit, precisão de arredondamento `BigDecimal`→`int`, e JavaDoc/Swagger desatualizados. Task 4.4 (verificar prompt do coach via log) deferida com justificativa — caminho não tocado por esta change, já coberto por testes de `add-daily-readiness-checkin`. PRs backend #22 + front #30 mergeados 2026-07-04; arquivada em `archive/2026-07/2026-07-04-wire-readiness-checkin-to-athlete`. | `add-daily-readiness-checkin` ✅ |
| 9.9 | ~~`add-athlete-retention-quick-wins`~~ ✅ *(S, backend+front)* | 18/18 | **3 quick-wins de retenção, sequenciadas por ROI.** Atacam causas 3 (baixa percepção de progresso) e 5 (pouca conexão coach-atleta) do discovery (`prd/product-discovery-retencao-atletas-90d.md`). **A — Feedback pós-treino:** card determinístico pós-log (sem IA), substitui o toast antigo. **B — Kudos coach→atleta:** `POST /coach/atletas/{id}/kudos` (idempotente via `UNIQUE(atleta_id,coach_id,motivo,data)`) + `KudosDialog` no perfil do coach + card na Home do atleta. **C — Resumo semanal na Home:** `buildWeeklySummary` (zero endpoint novo) agrega treinos/volume/streak/forma/próximo treino. **QA gate** (6 análises: 2× code/security/clean-code + Codex, backend e frontend) sem Critical — corrigidos: `Kudos.motivo` sem `length=20`, `KudosDialog` não resetava seleção ao reabrir (mesma classe do bug do `QuickCheckInModal`), `WeeklySummaryCard` com estado vazio fabricado durante loading, `FAIXA_APRESENTACAO` acoplada entre shells (convergência de 2 revisores), `parseDuracaoMin` sem suporte a "MM:SS", concordância de gênero errada no texto do kudo. PRs backend #23 + front #31 mergeados 2026-07-04; arquivada em `archive/2026-07/2026-07-04-add-athlete-retention-quick-wins`. | `manual-training-entry-lightweight` ✅, `athlete-profile-drilldown` ✅, `wire-athlete-shell-to-endpoints` ✅ (9.5) |
| 10 | ~~`fit-file-upload-ingestion`~~ ✅ *(S, backend+front)* | ~15/~15 | **Upload de .fit (dado rico de wearable).** Pesquisa de mercado (2026-07-03) confirmou que Garmin Health API exige aprovação individual inacessível, Health Connect/HealthKit não recebem dados ricos do Garmin, e Strava API tem restrição legal para ML. Upload de .fit funciona com Garmin/Suunto/Coros/Polar/Wahoo, sem burocracia e sem restrição de uso dos dados. Backend: SDK oficial Garmin (`com.garmin:fit`) + `FitParseService` + `FitTreinoPersister` (dedup idempotente via `TreinoDedupHelper`, compartilhado com o Strava). Frontend: drag-and-drop (`FileUploadZone`) na `ManualTrainingFormPage`, acima do formulário manual. **QA gate (4 rodadas)** sem Critical — corrigidos ao longo das rodadas: dedup quebrava sem `Session.startTime` (fabricava `now()`); corrida de concorrência duplicava evento/TSB; flag `novo` hardcoded quebrando o contrato 200/201; flood de laps sem limite (cap de 1000); arquivo multiessão (triathlon) corrompia dados silenciosamente; `FonteDados.MANUAL` mascarava proveniência (corrigido para `GARMIN`); parse rodava dentro da transação (extraído `FitTreinoPersister` transacional); frontend descartava mensagens de erro curadas do backend. PRs backend #24 + front #32 mergeados 2026-07-05; arquivada em `archive/2026-07/2026-07-05-fit-file-upload-ingestion`. Sprint 23 (`first-party-ingestion-architecture`) permanece como upgrade com compute-on-import automático. | `manual-training-entry-lightweight` ✅ |
| 11 | ~~`coach-encerrar-semana`~~ ✅ *(M · Full, backend-only)* | ~18/~18 | **Encerrar a semana.** Treinador finaliza treinos `PENDENTE` passados como `PERDIDO` e fecha o `PlanoSemanal` (`CONCLUIDO`), destravando a geração da próxima. 3 gatilhos: on-demand por atleta, lote da assessoria (+ preview/dry-run), fallback automático (carência parametrizável, default 3d). Dia corrente só vira perdido no fim da semana (`hoje == semanaFim`). Não gera o próximo plano (coach-in-the-loop); emite `SemanaEncerradaEvent` (`origem` ON_DEMAND/AUTOMATICO). Produz o `CONCLUIDO` consumido pela 12b. **QA gate** (Claude + cross-model Codex) sem Critical — corrigidos N+1 na marcação e no lote, filtro de tenant nas queries, sanitização de erro no lote, evento só em mudança real, guard de status/tenant em `marcarTreinosPerdidos`. PR backend #26 mergeado 2026-07-06 (merge `95f3c99`); arquivada em `archive/2026-07/2026-07-06-coach-encerrar-semana`. Suíte: 1206 testes. Frontend segue em `coach-encerrar-semana-ui`. | — |
| 11 | ~~`coach-encerrar-semana-ui`~~ ✅ *(S · Fast, frontend-only)* | ~8/~8 | **Botões + confirmação do encerramento.** Ação "Encerrar semana" no card de plano (resumo verde/amarelo + CTA gerar próxima) e "Encerrar semana (N)" em lote com **preview obrigatório** e resumo com falhas. Respeita a **seleção do grid** do roster (`atletaIds`) — exigiu o follow-up de backend `coach-encerrar-lote-selecao` (PR backend #27, merge `1ac468f`), que fez `encerrar-lote`/`preview` aceitarem `{ atletaIds }`. Habilita a métrica-farol de adoção on-demand vs automático. **QA gate** (frontend + clean-code) sem Critical. PR front #33 mergeado 2026-07-06 (merge `c1dfb73`); arquivada em `archive/2026-07/2026-07-06-coach-encerrar-semana-ui`. Suíte front 476 / back 1207. Follow-up ⚠: `console.log`/PII pré-existente no `planosDialog` (higiene separada). | `coach-encerrar-semana` ✅ |
| 12 | ~~`coach-batch-plan-generation`~~ ✅ *(M · Full, backend+front)* | ~32/~32 | **Geração em escala.** Coach seleciona N atletas no roster e gera planos para todos de uma vez. `POST /coach/planos/gerar-lote` → `202 Accepted + jobId`; processamento `@Async` em **virtual threads** (uma por atleta) com `Semaphore` limitando a concorrência ao LLM; `GET /coach/planos/lote/{jobId}` para polling; nova tabela `tb_batch_plan_job` + índice único parcial `uk_plano_semanal_atleta_semana_ativo`; recovery de jobs órfãos no restart; barra de progresso e erros individuais sem abortar o lote no frontend. **Dependência de `add-llm-tool-use` removida** (2026-07-06). **QA gate** (5 revisores Claude + cross-model Codex) — NO-GO inicial com 1 Critical (async disparado antes do commit → `afterCommit`) + Importants, todos corrigidos: REJEITADO regenerável (`existePlanoParaSemana` + `PlanoJaExistenteException` tipada), fast-path tenant-scoped, `finalizarJob` com fechamento de emergência, loop do `onConcluido` (tripla convergência), polling fora de ordem (guard de geração + `setTimeout` encadeado). PRs backend #28 + front #34 mergeados 2026-07-06; arquivada em `archive/2026-07/2026-07-06-coach-batch-plan-generation`. Suítes: back 1234 / front 487. Deferidos p/ staging: E2E manual + baseline da métrica. | `coach-encerrar-semana` ✅, `coach-edit-planned-workout` ✅ |
| 12 | ~~`notify-athlete-week-closed`~~ ✅ *(XS · Fast, frontend-only)* | ~13/~13 | **Retenção: atleta sabe que a semana fechou.** Banner na Home quando o plano corrente está `CONCLUIDO` com treinos `PERDIDO`. **Premissa do SPRINTS corrigida na spec:** a fonte é `useAthletePlan` (`treinosPlanejados.statusTreino === 'PERDIDO'`), **não** `GET /me/treinos` (que retorna realizados). Zero backend. **QA** (frontend + clean-code) sem Critical; Important convergente corrigido (`planoError` silencioso → Alert + retry). Inclui fix de flakiness de timezone num teste pré-existente. PR front #35 mergeado 2026-07-07 (merge `bc76f4c`); arquivada em `archive/2026-07/2026-07-07-notify-athlete-week-closed`. Suíte front 502. | `coach-encerrar-semana` ✅ |
| 12 | ~~`measure-openai-prompt-cache`~~ ✅ *(XS · Fast, backend)* | ~4/~4 | **Medir antes de otimizar (product-lens 2026-07-07).** A geração de plano usava `.entity(...)` e descartava o `ChatResponse`. Agora `LlmUsageLogger` registra prompt/cached/completion tokens + `cache_hit_ratio` (via `responseEntity`), best-effort, sem mudar a geração. **QA** (code + clean-code) sem Critical; golden-master intacto. PR backend #29 mergeado 2026-07-07 (merge `9ce67ba`); arquivada em `archive/2026-07/2026-07-07-measure-openai-prompt-cache`. Suíte 1241. **Pós-deploy pendente:** observar o `cache_hit_ratio` real → decide o destino da `system-user-prompt-split`. | — |
| 12 | ~~`system-user-prompt-split`~~ ⏸ **Deferida (product-lens)** | ~2 | **Premissa de custo debunkada:** `TaskComplexity.PLANO` usa **GPT-4o** (auto-cache por prefixo, ~50% sem código), **não** Claude — o `SYSTEM_ONLY` está nos beans Claude, que a geração de plano não usa. Split system/user é **neutro** de custo no GPT-4o (cacheia por prefixo, não por role) e carrega risco de regressão (reordena regras→system). Bloqueada por `measure-openai-prompt-cache`: se o auto-cache já economiza → arquivar. Alavanca real de custo = trocar `PLANO` p/ Claude Sonnet (change própria), quando o split faria sentido junto. Spec em `changes/system-user-prompt-split` (Deferida). | — |
| 13 | ~~`add-athlete-tsb-chart-on-selection`~~ ✅ *(M · Full, frontend-only)* | ~15/~15 | **PMC chart no drill-down do coach.** Coach lê a forma do atleta sem sair do contexto de triagem. **2 pivots revertidos** (drawer → expansão inline no roster → **aba Diagnóstico**): a versão final adiciona o gráfico PMC na aba Diagnóstico (`CoachInboxPage`→`DiagnosisTabPanel`, reusa `selectedProfile.pmc`); roster inalterado; `useAthletePmc` coach-scoped/`AthleteQuickViewPanel`/`AtletaProgressService` removidos. QA (frontend + clean-code, múltiplas rodadas 28/jun) sem Critical. Integrada ao `develop` 2026-06-28 via commits de dashboard (sem PR formal sob a branch — estado do develop verificado consistente com a spec no fechamento 2026-07-07). Arquivada em `archive/2026-06/2026-06-28-add-athlete-tsb-chart-on-selection`. | — |
| 13 | ~~`add-aerobic-decoupling`~~ ✅ *(M · Full, backend+front)* | ~12/~12 | **Métrica diferenciadora.** Calcula e exibe Pa:HR (aerobic decoupling) automaticamente para treinos steady-state. `DecouplingCalculatorService` com gate robusto (CV de FC/velocidade + tipo intervalado + duração mínima 20min); `DecouplingBadge` com 3 faixas de cor + tooltip de estimativa; integrado no `DetalheTreinoDialog` via `GET /realizados/{id}`. Extras: `WorkoutTimelineChart` dinâmico no `TreinoEditDialog` (reflate edições em tempo real) + volume do plano reativo (`PlanoDetalhePanel`/`PlanoPendenteItem`). QA (code/security/frontend/clean-code) sem Critical. PRs backend #30+#31 + front #36 mergeados 2026-07-07; arquivada em `archive/2026-07/2026-07-07-add-aerobic-decoupling`. | — |
| 14 | ~~`progressao-treinos`~~ ✅ *(M · Full, backend)* | ~14 | **Envelope de progressão confiável.** Substitui o contador de semanas ingênuo por engine multi-janela (7/21/42d) com aderência, RPE e carga — `ProgressaoTreinoService` produz `DecisaoProgressao` (`PROGREDIR`/`PROGREDIR_LEVE`/`MANTER`/`REDUZIR`) injetada no prompt de periodização antes da chamada à IA; `calcularProgressaoSegura` (TSB/rampRate) reutilizado como teto; janela de histórico ampliada de 7 treinos para 42 dias; fallback resiliente (falha no serviço não bloqueia a geração). Correções de isolamento de tenant e tratamento de exceções no fluxo novo. 30/33 itens; deferidos: 7.4 (smoke manual de prompt pré-deploy) e 8.1/8.2 (surfacing do `motivo` ao coach + log estruturado da decisão — Sprint 15). PR backend #33 mergeado 2026-07-08; arquivada em `archive/2026-07/2026-07-08-progressao-treinos`. Suíte 1267. | — |
| 14 | ~~`fix-llm-routing-finops`~~ ✅ *(S · Fast, backend)* | ~5 | **FinOps da camada multi-modelo (fix fora do planejamento, criado e entregue 2026-07-08).** Externaliza model IDs/parâmetros de `MultiModelConfig` em `app.llm.routing` (`LlmRoutingProperties`, fail-fast de rota ausente); preços saem do Javadoc para `llm-pricing.yml` (fonte única, `LlmPricingRegistry` com fail-fast de modelo sem preço); temperatura da rota `expert` 0.8→0.2 (determinismo p/ deep-reasoning clínico); `CostTrackingAdvisor` nos 5 beans publica `llm.tokens.*`/`llm.cache.*`/`llm.cost.estimated.usd` (tags `model`/`route`) — insumo da futura decisão de TTL Anthropic (mantido `ONE_HOUR`, non-goal); `/actuator/prometheus` exposto autenticado. **QA gate** (code + security + clean-code) zero Critical; Importants corrigidos (guards no parse do yml, `todasAsRotas()`, rounding). 21/21 tasks. PR backend #34 mergeado 2026-07-08; arquivada em `archive/2026-07/2026-07-08-fix-llm-routing-finops`. Suíte 1285. Follow-ups na change: aposentar `LlmUsageLogger`, decisão de TTL com ≥2 semanas de `cache_hit_rate`, `add-llm-batch-api`. | — |
| 17–18 | ~~`deterministic-planner-engine`~~ ✅ *(M, backend)* | 39/39 | **Motor determinístico em shadow + fundação do núcleo `domain/` (parte 1/2; estende `progressao-treinos`).** Split 2026-07-14 (shadow-first): `PlannerEngine` (PeriodizationPlanner + LoadTargetResolver + TaperStrategy + InjuryRiskEvaluator TSB-based + ConstraintValidator) + `SkeletonComplianceChecker` como lógica pura, rodando **só em shadow** (`planner-engine.shadow`): calcula skeleton, compliance hipotético, métricas e auditoria V54 sem alterar prompt/plano. Inaugura `domain/planner`+`domain/compliance` com fronteira ArchUnit. Sem dependência de change ativa — calibra thresholds antes do enforcement. `planner-rules.yml` criado como inventário comentado de thresholds (débito de extração futura, não carregado pela app). QA gate (code/security/clean-code) zero Critical. PR backend #46 mergeado 2026-07-19 (commit `9e39616`); arquivada em `archive/2026-07/2026-07-19-deterministic-planner-engine`. | calculadora TSS/CTL/ATL/TSB ✅, `SugestaoCoach` state machine ✅ |
| 14 | ~~`fit-lap-metrics-parser`~~ ✅ *(S · Fast, backend)* | ~8/~8 | **Parser .fit completo (parte 1/3).** Extrai e persiste elevação, potência e cadência por lap e sessão no import .fit — campos que o SDK já expõe e as colunas já existiam. Zero migration. Validado campo a campo contra CSV real do Garmin; PR backend #36 mergeado e arquivado em `archive/2026-07/2026-07-13-fit-lap-metrics-parser`. Desbloqueia `fit-lap-derived-metrics`. | — |
| 14–15 | ~~`fit-running-dynamics-ingestion`~~ ✅ *(M, backend)* | ~13/~13 | **Running dynamics + contexto (parte 2/3, implementada fora de ordem — após a parte 3/3).** GCT, equilíbrio E/D, passada, oscilação/proporção vertical, temperatura, tempo em movimento e calorias — migration V53, 15 colunas nullable. **Fecha o achado registrado por `fit-lap-derived-metrics`:** `velocidadeMedia`/`paceMedia` por lap passam a usar `tempoMovimento` (timer time) em vez da duração elapsed quando presente e menor (design D6, CA7) — validado com dado real (pace de 10:11/km para 6:03/km na mesma volta com pausa da fixture original). Validação campo a campo contra CSV real do Garmin Connect, zero divergência; resolvida a assumption do lado (E/D) do equilíbrio de GCT (pé esquerdo). **QA gate (2 rodadas** — code/security/clean-code + `/codex:review` + `/codex:adversarial-review`, GO) corrigiu: `NumberFormatException` não tratada em upload adversarial (NaN/Infinity em running dynamics, `montarResultado` rodava fora do `try/catch`); overflow de `NUMERIC(4,1)` em oscilação/proporção vertical sem cap; débito de DTO com 41 campos posicionais (2ª vez sinalizado) — extraído `RunningDynamicsOutputDto`. Um achado do security-reviewer (overflow de `INTERVAL`) foi investigado e refutado por verificação empírica. PR backend #38 mergeado 2026-07-13; arquivada em `archive/2026-07/2026-07-13-fit-running-dynamics-ingestion`. Suíte 1379. | `fit-lap-metrics-parser` ✅ |
| 16 | ~~`fit-lap-derived-metrics`~~ ✅ *(M, backend)* | ~15/~15 | **Métricas derivadas por volta (parte 3/3).** Curva de EF, Pw:HR e GAP interno (W/kg incluso na série). Cálculo puro sobre dados já persistidos, zero migration. GAP nasce implementado mas **desligado** por hard gate — calibração contra a fixture real reprovou (erro médio 29,2 s/km; causa dominante: pausas dentro de laps distorcem o pace derivado de `totalElapsedTime`); fica para `fit-running-dynamics-ingestion` corrigir com `tempo_movimento`. **QA gate (2 rodadas** — code/security/clean-code + `/codex:review` + `/codex:adversarial-review`) corrigiu 3 Important: bug de partição temporal do Pw:HR (usava a duração dos segmentos elegíveis em vez da linha do tempo completa, divergindo do gate de cobertura), vazamento do envelope de decoupling (Pw:HR/potência) para fora do detalhe, e uma regressão no Pa:HR legado introduzida pelo próprio fix do Pw:HR (corrigida com teste de regressão dedicado provando o cenário). Achado de EF corrompida por pausa nas mesmas voltas do GAP registrado e aceito (ship como está — mesmo risco do Pa:HR já em produção). PR backend #37 mergeado 2026-07-13; arquivada em `archive/2026-07/2026-07-13-fit-lap-derived-metrics`. Suíte 1360. | `fit-lap-metrics-parser` ✅ |
| 15 | ~~`intervals-icu-workout-push`~~ ✅ *(M · Full, backend+front)* | 31/33 (0.2/0.3 adiadas) | **Coach-in-the-loop físico: push de treinos ao relógio.** Na aprovação do plano, cada treino exportável é enviado automaticamente ao intervals.icu → Garmin Connect → relógio do atleta. `workout_doc` JSON estruturado (pace secs/km, FC bpm absoluta, blocos reps), idempotência client-side (API não deduplica), reconciliação de órfãos. Gate CA0 fechado (doc-only validado no relógio do founder 2026-07-14). Zero migration; reusa `IntegracaoExterna` + campos de sync do `TreinoPlanejado`. Modelo canônico `StructuredWorkout` + `WorkoutChannel` isola o formato de destino — Garmin Training API pluga como adapter futuro sem tocar no conversor. **Concluída 2026-07-15:** PRs backend#40 + front#38 mergeados; walking skeleton real no relógio do founder; QA gate (4 Claude + Codex cross-model) zero Critical; suítes 1571/559. Extras da execução: sync de DELEÇÃO de plano (PlanoDeletadoEvent), reset de treino que vira não-exportável, nome com distância. Follow-ups na spec arquivada: TX única do batch (convergência Claude+Codex — priorizar), debounce de rajada do uploader Garmin (decisão aberta), retry sem ultimaSincronizacao. Arquivada em `archive/2026-07/2026-07-15-intervals-icu-workout-push`. | `coach-plan-review-workflow` ✅ |
| 16 | ~~`intervals-icu-push-hardening`~~ ✅ *(S · Fast, backend)* | 5/5 (smoke deferido) | **Hardening do push (follow-ups priorizados da change-mãe).** TX por treino via REQUIRES_NEW no Processor (convergência Claude+Codex — rollback de lote deixa de descartar marcações); nudge anti-debounce do uploader Garmin (re-PUT no último evento quando 2+ criados em rajada — achado do walking skeleton); `ultimaSincronizacao` também no retry. Zero migration/contrato. **Concluída 2026-07-15:** PR backend #41 mergeado; CA1 provado com OptimisticLock REAL (Testcontainers); refinamento de execução: TransactionTemplate (TX-A claim / TX-B marcação) com HTTP fora de TX em vez de @Transactional no método; suíte 1589. Smoke de rajada deferido para verificação operacional pós-deploy. Arquivada em `archive/2026-07/2026-07-15-intervals-icu-push-hardening`. | `intervals-icu-workout-push` ✅ |
| 17 | ~~`intervals-icu-activity-ingestion`~~ ✅ *(M · Full, backend)* | 8 blocos | **Sentido pull da integração: import de activity realizada.** Coach importa uma activity específica do intervals.icu como `TreinoRealizado` via `POST /api/v1/intervals-icu/atletas/{id}/activities/import?activityId={id}`; flag `autoSyncPausado` em `IntegracaoExterna` pausa automaticamente o Strava do atleta ao conectar qualquer uma das duas integrações (evita duplicação cross-fonte de dado), com override manual (`pausar-sync`/`retomar-sync`). **Smoke real com atleta founder** (Postgres do HomeLab dev, gate 3.0 confirmou formato real do payload — `icu_athlete_id` não `athlete_id`, cadência perna única, sem campo de pareamento nativo) encontrou e corrigiu um `LazyInitializationException` real (500 no re-import de activity já importada) — o próprio teste de integração da task 6.8 mascarava o bug com um `@Transactional` de classe, removido para continuar detectando essa classe de regressão. **QA gate** (code/security/clean-code em paralelo) corrigiu JSON sem escape em `metadadosSincronizacao` (achado convergente nos 3 revisores) + índice composto faltante em `tb_integracao_externa`; achado fora do QA gate original (unidade de `velocidadeMedia`, m/s→km/h sem conversão) corrigido no fluxo do `/pr`. PR backend #42 mergeado 2026-07-16; arquivada em `archive/2026-07/2026-07-16-intervals-icu-activity-ingestion`. Suíte 1714. | `intervals-icu-workout-push` ✅ |
| 18 | ~~`intervals-icu-activity-laps`~~ ✅ *(M · Full, backend)* | 10 blocos | **Fecha o non-goal da change-mãe: as etapas (laps) do treino.** Os intervalos vêm no mesmo payload da activity (`?intervals=true`) — zero chamadas HTTP a mais. Mapeia 20 campos por volta, incluindo running dynamics e, via migration V74, zona/intensidade/inclinação. Descarta o intervalo degenerado que a fonte emite (1 s / 2,4 m) e que entraria nas skills como se fosse volta. Endpoint de backfill completa os treinos importados antes, que o dedup impede de corrigir por re-import. PR #61.
| 14 | ~~`complete-authorization-controllers`~~ ✅ *(XS, backend)* | 8/8 | **Gate de segurança para beta — concluída 2026-07-14.** Papel (`hasAnyRole TECNICO/ADMIN`) + `@RequireTenant` nos 5 controllers que só exigiam autenticação (Metricas, ProvasProximas, StravaActivity, StravaAuth; webhook/callback seguem públicos por design, provado por teste) + `isAuthenticated()` no `/users/me`. Spec de maio reconciliada no init (papéis corrigidos pela evidência de consumo — endpoints são superfície do coach, `hasRole('ROLE_ATLETA')` original quebraria o produto). **QA gate corrigiu 1 Critical:** `getProvasProximas` era global e vazava provas/atletas de todas as assessorias — agora tenant-scoped. **Descoberta estrutural:** `@WebMvcTest` não carrega `CoreSecurityConfig` — testes de controller nunca exercitaram `@PreAuthorize` (falso verde); criados `AuthWebMvcTestConfig`/`JwtTestSupport` como padrão. 6 débitos registrados na spec arquivada (destaque: state OAuth sem assinatura e POST do webhook sem validação — candidatos a change de hardening). PR backend #39 mergeado 2026-07-14; arquivada em `archive/2026-07/2026-07-14-complete-authorization-controllers`. Suíte 1414. Pendência: smoke manual em dev com token TECNICO. | — |

| 17–18 | ~~`athlete-onboarding-baseline`~~ ✅ *(L, backend+front)* | 100% (7.4 adiada) | **Onboarding + baseline + confidence score + calibração — entregue de uma vez (não dividida em parte 1/2 como planejado).** Activity Normalizer + Baseline Calculator + Confidence Scorer + Calibration Phase + PlanningPolicy + auto-approve Cenário A + badge de baixa confiança na fila do coach + `dataProva` cria/atualiza `Prova` alvo + migração de atletas legados + 4 endpoints (`/onboarding`, `/onboarding/concluir`, `/calibracao`) + formulário multi-etapa + `CalibrationBanner`. **Retrofit de domínio** (sessão de grilling 2026-07-21, 8 decisões, `apps/menthoros-backend/CONTEXT.md` + ADRs 0001-0003): rename `AthleteBaselineSnapshot`→`AthleteBaselineState` + `AthleteBaselineHistory` append-only, `origemAprovacao` em `PlanoSemanal`, draft de onboarding em staging (não escreve em `Atleta` até concluir), `CanalIntegracao`/`dispositivoMarca` como prior do Confidence Scorer. **DoD 9.1 verificado via click-through manual real** (atleta real, backend+front locais contra a base/Keycloak compartilhados) — achou e corrigiu 3 bugs que nenhum teste com mock pegava: enum `@JsonFormat(OBJECT)` reenviado cru no resume do rascunho (400), coleção Hibernate compartilhada entre `Atleta`/`PerfilOnboardingAtleta` (`JpaSystemException`), `dadosDescartados` sem `@JdbcTypeCode(JSON)` (Postgres rejeitava o insert de auditoria do dedup). QA gate (code/security/clean-code) sem Critical; débito aceito documentado (conclusão de onboarding é check-then-act sem lock otimista, baixo risco pré-launch). PRs backend #47 + front #41 mergeados 2026-07-22; arquivada em `archive/2026-07/2026-07-22-athlete-onboarding-baseline`. Suítes 2047 (backend) / lint+build+test verdes (front). Pendência registrada (não bloqueante): task 7.4, bonus coach-como-proxy na UI — falta decidir por onde o coach entraria no fluxo de onboarding de outro atleta. | `deterministic-planner-engine` ✅ |
| 21 | ~~`assessoria-billing-asaas`~~ ✅ *(L · Full, backend-only)* | 24/24 (+QA) | **Cobrança B2B das assessorias via Asaas — entregue com provider MOCKADO.** Entidade `Assinatura` (1:1 com `Assessoria`, sem histórico local — ADR-0004) + enum `StatusAssinatura` (`PENDENTE`/`ATIVA`/`INADIMPLENTE`/`SUSPENSA`/`CANCELADA`); gateway `AsaasGateway` (impl real `RestClient` com timeouts + idempotência por `externalReference`) **e `AsaasGatewayMock` selecionado por `@ConditionalOnProperty asaas.mock=true` (default)** — ainda sem ligação real com o provider (decisão do founder); a impl real entra em `ASAAS_MOCK=false` + credenciais, sem mudar código. Endpoints admin `POST`/`PATCH`/`DELETE /api/admin/assessorias/{id}/assinatura` (`ADMIN` = staff de plataforma), webhook público `/api/v1/asaas/webhook` (auth por header `asaas-access-token` constant-time + idempotência por `event.id`), job diário de carência (`INADIMPLENTE` > 5 dias → `SUSPENSA` + `Assessoria.ativo=false`). Migrations V68 (tb_assinatura), V69 (idempotência), V70 (remoção de 4 campos legados de `tb_assessoria`, sem backfill). Estratégia de **falha parcial** (Decisão 9 / ADR-0005): âncora local `PENDENTE` antes de chamar o Asaas (sobrevive a falha externa), `criar` deliberadamente não-`@Transactional`, PATCH/DELETE com chamada externa por último dentro da transação. **QA gate** (code/security/clean-code em paralelo): 2 Critical resolvidos — C1 authz cross-tenant (decisão: `ADMIN` é role de staff de plataforma, consistente com `AssessoriaController`, documentado) e C2 criação de subscription não idempotente (corrigida: `GET /subscriptions?externalReference` antes do POST, anti-cobrança-duplicada) — + Important (guardas de `CANCELADA`, retry sincroniza `valor`, passo 3 atômico via `TransactionTemplate`, corrida de idempotência do webhook → 200). **Ciclo validado ao vivo por HTTP** (app real + JWT ADMIN do realm + mock): criar (201/ATIVA) → `PAYMENT_OVERDUE` (INADIMPLENTE) → idempotência (evento repetido não duplica) → `PAYMENT_CONFIRMED` (ATIVA) → cancelar (204/CANCELADA + assessoria inativa). 2 ADRs (0004/0005) + glossário em `apps/menthoros-backend/CONTEXT.md`. PR backend #49 mergeado 2026-07-23 (commit `f6992a4`); arquivada em `archive/2026-07/2026-07-23-assessoria-billing-asaas`. Suíte 2083. Pré-go-live: `ASAAS_MOCK=false` + `ASAAS_API_KEY`/`ASAAS_WEBHOOK_ACCESS_TOKEN` + cadastro do webhook no painel do Asaas. | — |
| 14 | ~~`keycloak-user-onboarding-auth`~~ ✅ *(**L · Full**, backend+front)* | **24/24** | **Auto-cadastro de coach e assessoria.** Reescopada em 2026-07-31 — deixou de ser "provisioning backend-driven" (`S`, ~8) e virou signup público com compensação cross-sistema. **Escopo canônico no Bloco 3.** | `add-coach-lgpd-consent` ✅ |
| 23+ | 🤖 `llm-code-switching` *(S, backend)* | ~21 | **EN/PT: ~20% ganho de reasoning + custo menor.** Instruções em EN, conteúdo em PT. Adiado da Sprint 15 — reescorpado após estabilização do novo prompt (`WeekPlanSkeleton` no prompt = enforcement). | `planner-engine-enforcement` |
| — | ~~`refine-inbox-visual-hierarchy`~~ ✅ **ENTREGUE e ARQUIVADA** *(L · Full, frontend)* | **21/23** | **Hierarquia visual do Coach Inbox — Fases 1 e 2 completas.** 14 PRs mergeados em `develop` (#61–#74) em 2026-08-16; arquivada em `changes/archive/2026-08/2026-08-16-refine-inbox-visual-hierarchy/`. **CTA contextual** que troca de ação em vez de aparecer morto (era o elemento mais fraco da tela: 28px, na borda da dobra, cinza no estado comum); **gate de dados** — atleta em atenção fixado no topo em qualquer página ou filtro, porque o roster é paginado em 10 e a fila de atenção não; motivo e recência no card ("Alerta · Inatividade · 14d"); accent racionado; tipografia via `ThemeProvider` **aninhado no CoachLayout** (App.tsx intocado, para não vazar nas telas do atleta); nenhum texto abaixo de 11px (o menor era **4,8px**); estados vazios distinguindo zero medido de ausência de dado; insight da IA em 4 seções julgáveis; cinco estados de coluna onde antes carregando/erro/vazio davam a mesma tela. **Fase 3 (mobile) destacada** em `refine-inbox-mobile-breakpoint` — o founder decidiu que o coach não usa o inbox à beira de pista por enquanto, e essa era a única justificativa para priorizá-la; risco declarado: o inbox segue inutilizável em 390px e a faixa 900–1200px subprojetada. **Métrica de sucesso APROVADA (2026-08-16):** no teste dos 5 segundos, o founder identificou o atleta em atenção **e o motivo** — o que a auditoria apontava como impossível. Ressalva mantida: N=1 e avaliador enviesado, então vale como não-regressão, não como evidência de usabilidade; a prova real exige coaches externos. **Premissa da spec derrubada na 2ª rodada de DoR:** ela descrevia uma "Fila de revisão" que não existe no inbox — a coluna lista o roster, e o rótulo enganoso na UI foi corrigido para "Atletas". |
| — | ~~`fix-assessoria-logo-shell`~~ ✅ **ENTREGUE e ARQUIVADA** *(XS · Fast, back+front)* | **8/8** | **A logo da assessoria passa a aparecer no produto.** PRs `menthoros-backend#70`, `menthoros-front#75` e `#76`, mergeados em 2026-08-16; arquivada em `changes/archive/2026-08/2026-08-16-fix-assessoria-logo-shell/`. **Duas premissas erradas, ambas pegas antes de virar dívida.** (1) A spec mandava mapear `assessoria.getLogoUrl()` — campo legado, `NULL` desde que a logo virou BLOB; implementada assim, a correção não corrigiria nada e a change fecharia como concluída. Pego no `/implement init`, com IT que trava a armadilha (contrafactual falha). (2) **A causa raiz não estava na spec nem no meu diagnóstico:** a rota exige JWT e `<img src>` não envia `Authorization`, então a imagem recebia 403 — levar a URL até a shell não bastava, porque a URL sozinha não é carregável. Descoberto quando o founder testou na tela. **Alcance maior que o relatado:** a tela de configurações usava a mesma `<img>`, então a logo nunca apareceu em lugar nenhum do produto desde que o upload existe. **Lição registrada:** os testes de componente injetavam a URL e afirmavam que ela chegava ao `src` — todos verdes, imagem quebrada no navegador. O E2E agora responde 403 sem `Authorization`, como o backend. |
| 23+ | `polish-inbox-visual-semantics` *(XS · Fast, frontend)* | 0/5 | **Acabamento pós-verificação da `refine-inbox-visual-hierarchy`.** Navegação de verificação (2026-08-16, DOM autenticado) achou 4 pontas: accent lime ainda em secundários e no toggle do PMC (sólido, peso de CTA); badge "Alerta" em âmbar vs. card vermelho (semântica não fechou); "0 km" com check verde em atleta 209d inativa (regra zero-legítimo vs. ausência não pegou no strip de KPIs); rota inválida estoura o fallback cru do React Router (sem `errorElement`). Só CSS/tokens + uma página de erro. | refine-inbox ✅ |
| 23+ | `refine-inbox-mobile-breakpoint` *(M · Full, frontend)* | 0/6 | **Breakpoint mobile do Coach Inbox** — herdada da Fase 3 de `refine-inbox-visual-hierarchy`, destacada em 2026-08-16. Sidebar em `Drawer` abaixo de `md` (hoje 240px fixos ocupam 64% de um viewport de 390px), colunas em fluxo empilhado com volta explícita, CTA visível sem scroll em 390×844, e alinhamento do breakpoint do grid (colapsa em `lg`) com o do drawer (`md`), que hoje deixa a faixa 900–1200px subprojetada. **Despriorizada por decisão de uso, não por dado:** o coach não usa o inbox à beira de pista por enquanto. **Gatilho para repriorizar:** uso real de mobile no piloto. Auditoria deu **mobile 1/10** — o diagnóstico continua válido. |
| 24+ | 🔧 **BLOCO DE ENGENHARIA** | | Agrupamento coeso de refactors + infraestrutura IA. Coach só percebe valor na saída da RAG. | |
| 24 | 🔧 `refactor-iaservice-decomposition` *(M)* | ~26 | Decompõe `IaServiceImpl` (1500 linhas) em colaboradores testáveis. Limpa a casa antes de tool-use e skills migration. Validadores extraídos nascem no núcleo `domain/` (ADR-7 do planner). | `debito-tecnico` ✅ |
| 25 | 🤖 `planner-engine-enforcement` *(M, backend+front)* | ~20 | **Skeleton vinculante (parte 2/2 do planner).** `WeekPlanSkeleton` no prompt como contrato; compliance em 2 estágios (pré-redistribuição aciona o retry existente; pós-redistribuição terminal, fail-open); `SessionSlot` prescritivo (dia via absorção da `WeeklyDistributionSkill` órfã + TSS/sessão + zonas); `PeriodizacaoPromptFormatter` vira renderer + superfície mínima de review no coach (pre-mortem cross-model). Flags `planner-engine.enabled`/`fail-open`. Mede a north-star: MODIFIED/REJECTED com planner ≤ sem. | `deterministic-planner-engine` ✅ (hard), `refactor-iaservice-decomposition` (recomendado), shadow calibrado (divergência ≤ 2% em ≥ 2 semanas / ≥ 30 planos — CA11) |

| 25 | 🤖 `add-llm-tool-use` *(M, spike primeiro)* | ~27 | **Spike de 3 dias** para validar tools + structured output `strict`; se gate passar (~15% falha, ~2x latência), implementar infraestrutura completa. Fundação para RAG. | `debito-tecnico` ✅ |
| 26–27 | 🤖 `migrate-plan-prompt-to-skills` *(L/XL)* | L | Strangler: troca a fonte das `Constraint` de formatter → skill. `PromptBuilder` vira montador fino. | `introduce-plan-constraints` ✅, eval-harness ✅ |
| 28–30 | 🤖 `rag-tool-calling-prescription-engine` *(L)* | ~64 | Infra RAG (`PgVectorStore`) + motor de prescrição fundamentado. Destrava família `rag-*`. Infraestrutura de tool-use construída just-in-time se spike não tiver sido promovido antes. | `add-llm-tool-use` |
| 31 | 🤖 `rag-injury-aware-prescription` *(M)* | ~24 | Prescrição lesão-aware: protocolos, contraindicações, escalonamento. | RAG |
| 32 | 🤖 `rag-coach-methodology-personalization` *(M)* | ~29 | Aprende com planos aprovados/editados — personaliza para a "voz do coach". | RAG |
| 25+ | `add-coach-suggestion-inbox` *(upgrade RAG)* | — | Enriquecer sugestões do 9c com citações RAG. Workflow já existe; só muda a fonte dos dados. | RAG, 9c ✅ |

---

## Bloco 2 — Fechamento da jornada do treinador (coach-in-the-loop completo)

| Sprint | Change | Tasks | Objetivo | Dependência |
|:---:|---|:---:|---|---|
| 25 | `first-party-ingestion-architecture` | ~23 | **Dado first-party completo e ML-safe:** upload de `.fit`, dedup cross-source, tenant guard, compute-on-import com métricas reais de FC e pace. Sucede e substitui o log manual do 9d com dado rico. | `manual-training-entry-lightweight` 9d |
| 26 | `add-workout-metrics-analyzer` | ~22 | Métricas determinísticas (tempo em zona, decoupling, drift cardíaco) + skill `workout-analyzer` (proposta ao treinador). Reconcilia o `WorkoutAnalysisListener` existente. | first-party-ingestion; suggestion-inbox |
| ~~27~~ | ~~`add-post-workout-debrief`~~ 🔴 **fechada — redundante** (CPO 2026-07-24) · ~~`add-weekly-athlete-review`~~ → **fatiada em 3** (ver Radar) | — | **Repriorizado no CPO review 2026-07-24 (ver changelog no topo).** `post-workout-debrief` superseded pelo Coach Insight (`WorkoutAnalysisListener`) já em produção + `add-workout-metrics-analyzer`. `weekly-athlete-review` refinada ao DoR e **fatiada em 3 changes**: `add-weekly-review-consolidation` (backend determinístico) → `add-weekly-review-llm-focus` (IA + insumo no plano) + `add-weekly-review-coach-card` (front). | — |
| 28 | `add-athlete-coach-messaging` | 23 | Mensageria atleta↔coach + cards de `plan_adjustment`. Item mais independente. | Bloco 0 |

> **Fronteira do MVP (jornada completa coach-in-the-loop):** ao fim do Sprint 26, a jornada está entregue — identidade → casa do treinador → dado real (log manual 9d) → fila de atenção → sugestão IA explicável → **aprovação de plano pelo coach (9e)** → perfil completo do atleta (9f) → ingestion FIT completo → métricas + análise → debrief → revisão semanal → mensageria.
>
> **Mudança estratégica de ingestão:** o MVP passa a usar **ingestão first-party** (log manual 9d → FIT upload Sprint 23) em vez do Strava. O 9d desbloqueia dado real imediatamente; o Sprint 23 adiciona dado rico de GPS e FC. A família `strava-*` fica **deferida** (ver pós-MVP).

---

## Trilha intervals.icu — substituição gradual do Strava

Seção aberta em **2026-08-16**. As três changes desta trilha existiam desde 2026-08-02, mas só em
parágrafo de changelog — nenhuma aparecia em tabela de sprint, o que as tornava invisíveis a
qualquer planejamento que lesse só as tabelas.

**Por que é uma trilha e não três changes soltas:** o intervals.icu integra direto com Garmin e
outras fontes, com menos limitações de API que o Strava (cuja família está **deferida atrás de
clareza legal** — ver Pós-MVP). A intenção do founder é que ele se torne o caminho primário de
sincronização automática. A descomissão do Strava é **non-goal explícito** de todas as três: ele
continua funcionando, e o guard `autoSyncPausado` já pausa o Strava do atleta quando o intervals.icu
conecta, evitando duplicação cross-fonte.

**Já entregue e em produção:** push de treinos ao relógio (`intervals-icu-workout-push` +
`-push-hardening`, canal validado 2026-07-14), import manual de atividade
(`intervals-icu-activity-ingestion`) e backfill de laps (`intervals-icu-activity-laps`).
**O que falta é o automático** — hoje o coach cola o id de cada atividade à mão.

| Ordem | Change | Tam. | Tasks | Objetivo | Dependência |
|:---:|---|:---:|:---:|---|---|
| 1 | ~~`intervals-icu-oauth2-integration`~~ **✅ CONCLUÍDA 2026-08-22** | **M · Full** | 33/36 | **Trocou API key por OAuth2** (app `663`). Mergeada em backend#74 + front#83, com 51s entre os merges. O smoke real fechou o que teste nenhum prova: conectar → importar → aprovar plano com **push aceito** → desconectar com revogação confirmada — ou seja, **o canal de push ao relógio, em produção desde 2026-07-14, sobreviveu à troca para Bearer**. Gate de DoR (Codex) reprovou a spec com 2 blockers e 4 majors, todos reais: o pior era o `clientSecret` com default vazio servindo de chave do HMAC — com chave `""` o state assinado vira forjável **e o fluxo segue funcionando, sem sintoma**. Arquivada em `changes/archive/2026-08/2026-08-22-…`. **Adiado (3, nenhum de código):** trocar webhook secret exposto (0.4); reduzir `ACTIVITY:WRITE`→`READ` — o provedor concedeu WRITE sem termos pedido (0.5); `Integrations.md` no vault (9.3). | — |
| 2 | ~~`intervals-icu-activity-sync-scheduler`~~ **✅ CONCLUÍDA 2026-08-22** | **M · Full** | 30/30 | **Pull automático entregue** — PR backend **#77** (squash `7fd066e`), arquivada em `changes/archive/2026-08/2026-08-22-…`. Feita inteira num dia, do `init` ao merge. **A decisão que definiu a change foi a D4.1, tomada no `init`:** o provedor limita a 100 req/usuário/dia e não expõe a folga em header; com 1+N por ciclo, a carga inicial de 90 dias de um atleta diário estourava a cota e, como 429 é retryable, repetia a mesma janela todo dia sem nunca completar. Resolvido com **teto por contagem** (`sync-max-activities-per-cycle`, default 6): cada ciclo importa as N mais antigas ainda não importadas e o cursor avança **até a última processada, nunca além** — a carga vira progresso (~30h) em vez de laço. Fatiar por dias foi descartado porque o custo dependeria do hábito do atleta; eliminar o refetch foi **refutado no código** (os laps só vêm com `?intervals=true`). **DoR em 3 rodadas** (spec-reviewer + Codex ×2, 7 achados reais corrigidos, 1 refutado). **Gate 0.2 contra a API real** em 7 requisições: sem paginação na escala observada, filtro por data local inclusivo, ordem decrescente, ids globais, `start_date` UTC na listagem (virou a base do cursor). **QA**: 3 Claude + Codex ×2 — convergentes e corrigidos: `start_date` nulo derrubava o atleta; `lastSyncError` acima de `VARCHAR(500)` faria o registro do erro falhar *dentro do catch* e quebrar o isolamento por atleta. Overlap 1 → 7 dias (relógio offline). Quatro "críticos" refutados no código. **O smoke achou o que nenhum revisor viu:** 6 atividades de natação/bike/musculação sendo buscadas só para serem rejeitadas — com overlap de 7 dias, 72 das 100 req/dia de um triatleta. O filtro de modalidade passou a rodar **sobre a listagem**, antes de qualquer HTTP. O 2º ciclo reimportou sozinho 2 treinos apagados do banco de dev, em ordem cronológica, com reconciliação e zero duplicata — **CA1 provado ao vivo**. Suíte **2695 + 113 IT**. Lookback de 90 dias mantido por decisão do founder (τ do CTL é 42, mas 42 dias deixam o CTL 37% baixo e o TSB falsamente positivo); semear o PMC com o CTL do provedor foi para o Radar. | — |
| 3 | `intervals-icu-webhook-ingestion` | **M · Full** | 0/30 — **spec escrita em 2026-08-22** (`/change`, no mesmo dia do scheduler). Product review **Refine** (6 achados, incorporados) + pré-mortem Codex **NOT READY** (5 achados, todos verificados e incorporados — dois derrubaram premissas: não existe recuperação automática de laps e `backfillEtapas` faz até 50 fetches; por isso **só `ACTIVITY_ANALYZED` importa**). Header verificado em filtro, claim de idempotência liberado em falha, handler de rejeição com log. **Pré-condições do `/implement init`:** rotacionar o webhook secret exposto (é o mesmo da task 0.4 da OAuth2), gate 0.2 com payload real, e scheduler validado em produção. Base conectada hoje: 1 atleta — o product review recomenda sequenciar depois de `coach-meta-intensidade-editor` e `refactor-llm-call-outside-transaction`. | **Tempo real** — segunda metade do non-goal. O provedor passou a suportar webhook. Quando existir, o webhook vira o caminho primário e o scheduler (2) passa a ser fallback/reconciliação — mesmo par que `StravaWebhookServiceImpl` + `StravaActivitySyncScheduler` formam hoje. **A premissa registrada em 2026-08-02 de que esta change carregaria a migração para OAuth está superada:** a migração virou a change (1), que a antecede. Abrir com `/opsx:propose` depois que o scheduler estiver validado em produção. | (2) validada em produção |

> **A ordem aqui é técnica, não de prioridade.** (1) precisa vir antes de (2) porque o método
> `listarAtividades` do scheduler nasce autenticando de alguma forma — e nascer em Basic com uma API
> key que a change (1) remove seria escrever código para apagar em seguida.

---

## Bloco 3 — Loop de auto-cadastro do coach (self-service onboarding)

Bloco aberto em **2026-07-31**, quando as cinco changes foram criadas no mesmo dia. Fecha a jornada
de entrada do treinador **sem operação manual**: hoje cada assessoria nova exige criar tenant,
usuário e vínculos à mão no Keycloak e no banco.

**Estado em 2026-08-16:** **cinco das seis entregues** (`add-coach-lgpd-consent`,
`add-coach-settings-page`, `keycloak-user-onboarding-auth`, `assessoria-settings-ui` e
`coach-first-login-wizard`). **Resta `import-atletas-csv`**, ainda com 0% de tasks — ela é paralela e
nunca dependeu do signup.

**A jornada de entrada do coach está fechada ponta a ponta:** `/cadastro` → verificação de e-mail →
login PKCE → consentimento → wizard → dashboard, sem operação manual no Keycloak ou no banco.

**Jornada alvo:** `/cadastro` → verificação de e-mail (fluxo nativo do Keycloak) → login OIDC
(Authorization Code + PKCE) → consentimento LGPD → wizard de boas-vindas → dashboard.

```
add-coach-lgpd-consent ✅ ── gate: precisa estar no ar ANTES de abrir /cadastro ao público
        │                    (enforcement em `report-only` desde 2026-08-04; `on` ainda bloqueado)
        ├▶ add-coach-settings-page ✅ (onde o coach consulta o aceite)
        │
        ▼
migrate-login-to-authorization-code-pkce ── pré-requisito: o signup não pode nascer sobre ROPC
        │                                   (blocos 0–4 bastam para destravar; o 5 é hardening)
        ▼
keycloak-user-onboarding-auth ── cria o Usuario com onboardingConcluido=false
        │
        ▼
assessoria-settings-ui ✅ ── o wizard reutiliza estes clients/contratos
        │
        ▼
coach-first-login-wizard

import-atletas-csv ── paralelo, sem dependência do signup
```

| Ordem | Change | Tam. | Tasks | Objetivo |
|:---:|---|:---:|:---:|---|
| — | ~~`add-coach-lgpd-consent`~~ ✅ | S · Full | 16/44 | Entregue 2026-07-31 (detalhes no Bloco de Segurança). **Gate da jornada** — a flag `app.lgpd.consent-enforcement` ainda nasce `off` e os 4 gates de ativação seguem abertos. |
| — | ~~`add-coach-settings-page`~~ ✅ | XS · Fast | 5/10 | Entregue 2026-07-31 (detalhes no Bloco de Segurança). `/coach/settings` read-only com as duas versões aceitas, DPO e solicitação de exclusão. |
| — | ~~`migrate-login-to-authorization-code-pkce`~~ | M · Full | 35/39 | 🟠 **ARQUIVADA INCOMPLETA em 2026-08-05** (`archive/2026-08/2026-08-05-...`), por decisão do founder. **O corte do ROPC (5.3) não foi feito** — ver a linha "🔴 Corte do ROPC" abaixo, que é onde essa pendência passa a viver. **Código completo e em `develop`.** PRs front **#54** (fluxo PKCE) e **#55** (remoção do ROPC do código) mergeados. **A migração não está concluída:** o client `menthoros-web` ainda aceita `grant_type=password` — quem tiver credenciais obtém token por ali hoje. O #55 tirou o caminho do código, impedindo reintrodução por descuido, mas o provedor não mudou. **As 4 pendências são de infraestrutura**, não de código: sync do realm no Railway/produção (3.1) → deploy + repetir o walking skeleton lá (5.2) → cortar `directAccessGrantsEnabled` (5.3), mais a métrica (0.6). **Bloco 4 fechado:** roteiro executado ponta a ponta contra o HomeLab, com registro preenchido — e foi ele que achou **os dois únicos defeitos da change**, nenhum por teste automatizado: o reload voltava ao login (passava por 831 testes verdes, porque o design previa a sessão vir do cookie do Keycloak e nada no código perguntava a ele) e **não havia ação de logout em nenhum shell** — `logout()` existia e ninguém o chamava, defeito sem teste possível porque não havia código a exercitar. **Levar o E2E ao CI achou mais dois**, ambos invisíveis localmente: a suíte dependia do `.env` da máquina do autor (o issuer do IdP falso estava escrito à mão e casava por coincidência — no runner, 10 de 15 specs iam para o login) e uma corrida entre `toHaveURL` e os redirects do fluxo. ⚠️ **A métrica (0.6) é pré-condição para arquivar alegando sucesso:** sem linha de base ela não é falsificável, e "não piorou" vira julgamento, não medição. **Histórico:** o login usava **Resource Owner Password Credentials**, grant removido do OAuth 2.1 — além do risco, **impede MFA**, que é objeção de venda para assessoria que trata dado de saúde. É pré-requisito de `keycloak-user-onboarding-auth`, cujo critério de aceite assume um fluxo PKCE que não existia. **Bloco 0:** realm confirmado ao vivo; **cross-site em todos os ambientes ⇒ renovação por redirect, não iframe** (o cookie do Keycloak seria third-party e o silent renew falharia em silêncio); `post.logout.redirect.uris` aplicado no client, o que de quebra revelou drift entre o realm versionado e o servidor. **Bloco 1:** fonte única de token **e claims** — o risco não era o token, era `X-Tenant-ID` e roles saindo de leituras paralelas, o que numa renovação produz `Authorization` novo sem tenant. Guarda de arquitetura por padrão, verificada plantando uma violação. **Bloco 2:** fluxo trocado; a senha não passa mais pela aplicação. Estado terciário (`carregando`) separado de `isAuthenticated`, porque o booleano sozinho é ambíguo e faz o guard redirecionar durante o próprio callback. Escrever os testes achou um defeito de config: o `stateStore` do PKCE **precisa** persistir (o redirect recarrega a página) e estava implícito no default. Suíte **814/814** à época; hoje **831/831 + E2E 15/15** no CI. *(A dependência do `login.spec.ts` em relação a `enable-frontend-ci` foi resolvida: o spec foi reescrito para o fluxo PKCE e é o E2E que o bloco 4 exigia.)* |
| 0 | ~~🔴 **Corte do ROPC**~~ ✅ **ARQUIVADA** → `disable-ropc-direct-grant` em `changes/archive/2026-08/2026-08-06-disable-ropc-direct-grant/` | S · Full | 25/27 | **✅ APLICADO E VALIDADO NOS DOIS AMBIENTES (2026-08-06).** O `menthoros-web` **recusa** `grant_type=password` (`unauthorized_client`) e o PKCE `S256` é exigido pelo servidor, em HomeLab e Railway `develop`. CA1, CA2, CA3 e CA4 verificados em cada um; o fluxo Authorization Code + PKCE foi exercitado ponta a ponta, devolvendo token com `tenant_id` e roles idênticos aos de antes do corte. 4 PRs no `menthoros-infra` (#1 corte, #2 colisão do scope, #3 limite de 255, #4 serverless). **Estado anterior:** PR `menthoros-infra` **#1** mergeado (`dba238a`): `menthoros-web` com `directAccessGrantsEnabled: false` + `pkce.code.challenge.method: S256`; `menthoros-api` também com `S256`; **`menthoros-test` passa a nascer `enabled: false`**. O `sync-realm.sh` é manual e **não rodou** — nenhum Keycloak mudou, e o `menthoros-web` **segue aceitando `grant_type=password` em HomeLab e Railway**. ~~**Bloqueador:** colisão do client scope `organization`~~ **RESOLVIDO 2026-08-06.** Causa raiz reproduzida em laboratório: o gatilho é o client ter `organization` como scope **DEFAULT** *e* a requisição também pedir `scope=organization` — o Keycloak colide o scope atribuído com o `ClientScopeDecorator` que cria para o scope pedido. 40 chamadas por bateria: optional+pedindo 40/40 ok; **DEFAULT+pedindo 0/40**; DEFAULT sem pedir 40/40 ok com a claim presente. **Só o `menthoros-test` tem o scope como DEFAULT — a colisão nunca afetou o corte do ROPC.** Documentado na `description` do client (PR infra #2). Descartado por experimento: apagar o scope ou remover a atribuição fazem o `organization` sumir (`invalid_scope`). **Achado da auditoria que mudou a change:** o corte sozinho não eliminava o ROPC, só mudava o `client_id` — o `menthoros-test` é `publicClient`, com `organization` DEFAULT e `fullScopeAllowed`, num Keycloak exposto à internet; daí o `enabled: false`. **Também descoberto e roteado:** o backend **não tem credenciais de admin do Keycloak no Railway `develop`** (`KEYCLOAK_SERVER_URL`, `KC_ADMIN_USER` e `KC_ADMIN_PASSWORD` ausentes → defaults `localhost:8080` e senha vazia), pré-condição do `keycloak-user-onboarding-auth`. ⚠️ *Correção 2026-08-06: o registro original dizia "em nenhum ambiente" — errado. O local funciona; o `.env` que eu havia inspecionado era o do `menthoros-infra`, que serve ao compose, e o backend roda standalone lendo o `.env` do próprio repo.* e o realm não tem `bruteForceProtected` nem `passwordPolicy`. **Três armadilhas que só o sync real revelou:** (a) a `description` do client é `varchar(255)` e não trunca — o corte era **insincronizável desde o PR #1** (442 chars) e nenhuma revisão de diff pegaria; (b) os serviços do Railway estavam **serverless**, e o banco dormindo derrubou um sync pela metade com `HTTP 500` — os clients já estavam no estado alvo, o que tornava tentador dar por aplicado, mas configuração lida não prova sync completo (reexecutado, passou limpo); (c) o realm usa **login identity-first** (username numa página, senha na seguinte), detalhe não documentado que faz teste de login falhar sem erro visível. Tudo em `menthoros-infra/keycloak/README.md`. **Pendência roteada:** o backend não tem credenciais de admin do Keycloak em ambiente nenhum — virou a seção 0 do `keycloak-user-onboarding-auth`, que **bloqueia o signup** daquela change. *Histórico:* **O `menthoros-web` ainda aceita `grant_type=password`.** Quem tiver credenciais obtém token direto do Keycloak, sem passar pelo app — o grant que a migração existia para eliminar segue de pé em **todos** os ambientes, e ele é o que **impede MFA**. A migração para PKCE está completa no código; isto é o que falta para ela valer como controle de segurança. **Sequência:** sync do realm no Railway/produção (3.1) → deploy + repetir o walking skeleton lá (5.2) → `directAccessGrantsEnabled: false` + `pkce.code.challenge.method: S256` **só no `menthoros-web`** (5.3) → linha de base da métrica (0.6). ⚠️ **Não aplicar o corte de forma ampla:** o gateway admin usa password grant em **outro** client, e derrubá-lo quebra o signup do Bloco 3 — `KeycloakOrganizationGatewayImplTest` é a sentinela disso. **Pré-requisito real:** acesso admin aos Keycloaks de dev e produção; o teste manual de API não quebra, porque o client `menthoros-test` já existe (muda só o `client_id` no Apidog). **Quando for retomado, abrir change nova** — reabrir a arquivada é pior do que uma pequena e específica. Detalhes em `archive/2026-08/2026-08-05-migrate-login-to-authorization-code-pkce/tasks.md`. |
| 1 | ~~`keycloak-user-onboarding-auth`~~ ✅ **ENTREGUE e ARQUIVADA** | **L · Full** | **24/24** | **Auto-cadastro de coach e assessoria.** Página pública `/cadastro` (nome, e-mail, senha, nome da assessoria, slug) + `POST /api/public/coach-signups` que reserva o slug, cria `Assessoria` no plano BASIC (`maxAtletas=10`, `maxTecnicos=1`), provisiona organização/usuário/role no Keycloak e o `Usuario` local. **Sem garantia ACID entre Postgres e Keycloak** — exige compensação em ordem inversa e registro de `RECONCILIATION_REQUIRED`. Nunca retorna senha/token; verificação de e-mail pelo fluxo nativo. Fora do escopo: aceite LGPD no formulário (pertence a `add-coach-lgpd-consent`, não duplicar), conversão da waitlist, cobrança e escolha de plano. |
| 2 | ~~`assessoria-settings-ui`~~ ✅ **ENTREGUE e ARQUIVADA** | M · Full | **31/31** | **Configuração da assessoria — nome e logo.** `/coach/settings/assessoria` + `GET/PATCH /api/v1/assessorias/me` com concorrência otimista (`409`), e upload de logo **em BLOB no Postgres** (`tb_assessoria_logo`, 1:1) — não há object storage no projeto, e o contrato de leitura é rota do próprio produto, para trocar depois sem tocar no cliente. **Cores ficaram fora por decisão do founder** (D3): o tema segue estático. **A propriedade da assessoria virou `PROPRIETARIO` (role composite de `TECNICO`) + flag `owner` em `tb_usuario`** — o DoR reprovou a primeira versão porque resolver o dono em `Usuario.role`, que é single-valued, o tiraria da contagem de técnicos do plano (`maxTecnicos = 1` no BASIC) e liberaria um segundo técnico que o plano não comporta. Realm sincronizado no HomeLab e no Railway `develop`. PRs infra #10, backend #68, front #59. Arquivada em `archive/2026-08/2026-08-15-assessoria-settings-ui/`. |
| 3 | ~~`coach-first-login-wizard`~~ ✅ **ENTREGUE e ARQUIVADA** | M · Full | **15/19** | **Wizard de boas-vindas — entregue com DUAS etapas, não três.** PRs `menthoros-backend#69` e `menthoros-front#60` mergeados em 2026-08-16; arquivada em `changes/archive/2026-08/2026-08-16-coach-first-login-wizard/`. A proposal previa assessoria → primeiro atleta → convite; as duas últimas foram implementadas e **removidas no teste manual**: pedir dados de outra pessoa no primeiro minuto é atrito, e o cadastro errado feito ali vira registro difícil de remover (aconteceu). Ficou: confirmar assessoria → "Tudo pronto", que conclui no servidor e leva à tela de Atletas. **Efeito colateral valioso:** a etapa de convite escondia que **o convite não existia na interface** — endpoint no ar desde `assessoria-settings-ui`, nenhuma tela o expunha, e o formulário de atleta não tinha campo de e-mail; era impossível dar acesso a um atleta pela UI. Corrigido. **Três bugs de produção** vieram junto, todos porque o wizard tornou comum o caminho "atleta novo sem histórico": dono não conseguia excluir atleta, atleta inativado seguia no dashboard, e a geração do primeiro plano falhava para sempre por cache envenenado em rollback. **Pendência:** 3.3 (telemetria) adiada — não há canal de analytics no front, então a métrica de sucesso segue não falsificável. Escopo original: orquestrar os contratos que já existem (o wizard não cria endpoint novo; lacuna encontrada volta para a change dona). Estado `onboardingConcluido` + endpoint idempotente de conclusão; qualquer etapa é pulável e o atleta criado permanece criado. **Coaches existentes na data da migração não recebem o wizard.** |
| — | `import-atletas-csv` | M · Full | 0/20 | **Importação via CSV** (independente do signup — assessorias existentes também importam). Template UTF-8 com cabeçalho fixo, preview com `previewId` opaco e expiração, confirmação que só tenta as linhas válidas *daquele* preview, criação linha a linha sem reverter sucessos anteriores, CSV de erros. Capacidade do plano é checada **antes** de criar qualquer atleta. Fora do escopo: XLSX, merge de atletas existentes, desfazer importação, convites. |

> **Divergência de tamanho corrigida em 2026-08-03:** o Bloco 1 e o Bloco de Segurança listavam
> `keycloak-user-onboarding-auth` como `S · ~8 tasks` ("provisioning automatizado") e depois como
> `0/28`. A spec foi **reescrita em 2026-07-31** e hoje é **L · Full com 20 tasks**, incluindo signup
> público e compensação cross-sistema. É outra change com o mesmo nome — a estimativa antiga não
> serve para planejar.

> **Ordem não é sugestão:** abrir `/cadastro` ao público antes de o enforcement de consentimento
> estar ligado contraria a dependência declarada na própria proposal — o signup cria `Usuario`
> antes do aceite, então a whitelist do interceptor de consentimento precisa ser revisada dentro de
> `keycloak-user-onboarding-auth` (Q3 da change de consentimento). E o gate LGPD depende de
> validação jurídica do texto e da publicação dos Termos de Uso, cujo link ainda é placeholder `#`.

---

## Bloco de Segurança (pré-exposição a usuários reais)

Independente da ordem acima, estas changes **devem aterrissar antes de abrir para usuários reais** (beta). Encaixar conforme a data de exposição planejada — não depois.

Ordenadas por criticidade dentro do bloco: segurança (exposição de dados) antes de disponibilidade.

| Change | Tasks | Objetivo |
|---|:---:|---|
| ~~`complete-authorization-controllers`~~ | 8/8 | ✅ **ENTREGUE** — PR backend #39 mergeado 2026-07-14, arquivada em `archive/2026-07/2026-07-14-complete-authorization-controllers/`. *(Linha estava sem tachado e com contagem errada — 29, sendo que o `tasks.md` tem 8 — até a auditoria de 2026-07-31. A limpeza de 2026-07-20 corrigiu a duplicata desta change no Radar, mas esta segunda ocorrência passou batido.)* |
| **Gates de ativação LGPD** *(sem change própria — conclusão do bloco 5 de `add-coach-lgpd-consent`)* | 2 de 4 gates | ⚠️ **PARCIAL — entregue 2026-08-04.** PRs front #51 (`c04c69c`) + backend #62 (`bd6ac64`). **Enforcement saiu de `off` para `report-only`**: ninguém é bloqueado, e cada request que *seria* bloqueada passa a ser logada — o insumo da task 5.4. **O bloqueio real do `on` era outro, e não estava registrado em lugar nenhum:** o `CoachConsentDialog` pedia aceite dos **Termos de Uso** sem que o documento existisse — sem rota, sem texto, sem link, só a Política tinha. Ligado assim, o coach seria impedido de trabalhar até aceitar algo que não tinha como ler, com o aceite gravado em tabela append-only. Termos publicados em `/termos` (13 seções; a 5 separa a responsabilidade de prescrição do treinador da plataforma) e o dialog passou a linkar os dois documentos. Corrigida também uma divergência de versão que já estava de pé: o backend exigia `2026-06-30` enquanto a página exibia `2026-08-02` — ambos agora em `2026-08-03`. Front 785/785 + lint + build; backend `./mvnw clean verify` verde. **Ordem de merge saiu invertida:** o backend entrou 16s antes do front, apesar de os dois PRs documentarem o contrário. Sem dano observado — a janela é de segundos, o deploy não é instantâneo e o `report-only` não bloqueia; o pior caso seria um `409 CONSENT_VERSION_STALE` num aceite submetido exatamente nessa janela, resolvido reabrindo o modal. **Gates que seguem abertos e impedem o `on`:** 5.1 (o texto dos Termos é rascunho, pendente de revisão jurídica) e 5.3 (comunicação prévia aos coaches ativos); o bloco 4, com 14 verificações E2E manuais, também nunca foi executado. E `report-only` **só produz sinal com tráfego real em produção** — localmente não mede nada, então a 5.4 só fecha depois de alguém ler o log. |
| ~~`add-coach-lgpd-consent`~~ | 16/44 | ✅ **ENTREGUE e ARQUIVADA** — PRs backend #55 + front #47 mergeados em `develop` 2026-07-31; arquivada em `changes/archive/2026-07/2026-07-31-add-coach-lgpd-consent/`. **S · Full.** Consentimento do coach vira registro **append-only versionado** (`tb_usuario_lgpd_consent`, V73): uma linha por aceite, versionada pela data de vigência dos documentos, com `UNIQUE (usuario_id, tenant_id, policy_version, terms_version)` — nada é sobrescrito, então re-consentir após um bump preserva a prova do aceite anterior. `lgpdConsentGranted` é **derivado**, não persistido. **Entra em produção inerte:** a flag `app.lgpd.consent-enforcement` nasce `off`, então o `403` não dispara para ninguém; o que já opera é o modal e o registro do aceite. **O achado que reenquadrou a change:** o `JwtTenantFilter` já grava em `tb_usuario` a cada request autenticada, *antes* do gate — logo o `403` é controle de produto e **não** base legal do tratamento (a base do sync é execução de contrato, e precisa entrar no RIPD — task 5.5, aberta). **Três decisões contraintuitivas, documentadas no código para não serem 'corrigidas' depois:** `registerConsent` sem `@Transactional` (capturar a violação de constraint dentro de transação ativa a deixa rollback-only e vira 500 num caminho que o contrato promete idempotente — mesmo padrão de `WaitlistServiceImpl`); `503` e não `403` quando o usuário não resolve ("não consegui verificar" ≠ "não consentiu"); interceptor como `@Bean` e não `@Component` (`@WebMvcTest` arrasta todo `HandlerInterceptor` component-scanned para a fatia — derrubava 22 slices sem relação com LGPD, 136 erros). **QA gate com 5 revisores (4 Claude + Codex), zero Critical, 9 achados corrigidos**, 2 por convergência cross-model. Três valem registro: o `409` não refazia `GET /me`, deixando o modal em laço infinito; `GET /api/v1/strava/auth` é operação de coach (`hasAnyRole('TECNICO','ADMIN')`) e escapava do gate, contrariando a afirmação do próprio design de que "nenhum GET é operação de coach"; e a `UNIQUE` sem `tenant_id` causaria **bloqueio permanente** na troca de tenant (o security-reviewer classificou como Medium — a verificação mostrou pior). Um achado foi **refutado por verificação**: typo em `policy-version` não bloqueia permanentemente, o fluxo se auto-corrige. **Dois bugs escaparam de 762 testes verdes e só apareceram no navegador** — o link da Política dentro do `<label>` marcava o checkbox em vez de abrir a página (registrando aceite de um texto que o usuário tentou ler e não conseguiu), e `href` absoluto não resolvia sob `createHashRouter`. O teste que "cobria" o segundo passava para a forma correta e a quebrada; foi refeito com `createHashRouter`. **Pendência que viaja com a entrega: o bloco 4 (E2E manual) NÃO foi executado** — justamente a validação que pegou os dois bugs acima. **Bloco 5 (gates de ativação) aberto por design:** validação jurídica do texto, publicação dos Termos (link ainda é placeholder `#`), aviso prévio aos coaches e período em `report-only` antes de virar `on`. **Custo de processo registrado:** editar a V73 depois de aplicada no Postgres de dev exigiu `DROP TABLE` + limpeza do `flyway_schema_history` — a migration ainda era "minha", mas o banco compartilhado já não era. |
| ~~`add-coach-settings-page`~~ | 5/10 | ✅ **ENTREGUE e ARQUIVADA** — PRs backend #56 + front #48 mergeados em `develop` 2026-07-31 (backend primeiro); arquivada em `changes/archive/2026-07/2026-07-31-add-coach-settings-page/`. **XS · Fast.** Tela `/coach/settings` read-only: dados pessoais, data e **as duas versões aceitas** da Política e dos Termos, contato do DPO e solicitação de exclusão. `GET /users/me` passa a expor o último aceite (tenant-scoped) e `avatarUrl`. **Fecha o arco de LGPD do coach:** `add-coach-lgpd-consent` coleta o aceite, esta dá onde consultá-lo e agir sobre ele. **O `init` pegou 4 premissas mortas na spec** — escrita antes da change anterior existir: assinatura do repository sem tenant, `avatarUrl` ausente do DTO, `useCurrentUser` descartando o campo, e `CoachRoute` sendo union tipada (a task não compilaria). **O Codex reprovou o DoR mesmo em Fast track**, achando um BLOCKER que dois revisores Claude e o autor passaram: o CA4 descrevia um fluxo *inalcançável*, porque o `CoachLayout` da change anterior bloqueia o `Outlet` sem consentimento. Virou teste de componente; deliberadamente **não** se isentou `/coach/settings` do gate. Isso derrubou a premissa de que Fast track dispensa o passe cross-model — o comando `implement` foi alterado para torná-lo obrigatório em toda trilha. **QA com 5 revisores, zero Critical, 8 achados corrigidos.** Dois merecem registro: (a) a correção do gate de loading da change anterior tinha criado um **spinner infinito** quando `GET /users/me` falha — `loading` volta a false mas `granted` fica null; (b) preencher `avatarUrl` **não foi no-op** — o campo estava declarado e nunca populado, então a sidebar passou a carregar imagem externa em toda página do coach sem `referrerPolicy`. **Uma sugestão convergente de 3 revisores foi refutada por verificação:** colapsar as duas queries de consentimento quebra em rollback de versão, porque consentimento com uma versão não expira por ter aceito outra depois. **Pendência que viaja:** bloco 3 (click-through manual) não executado — o mesmo bloco onde os dois únicos bugs reais da change anterior apareceram. |
| ~~`keycloak-user-onboarding-auth`~~ | **24/24** | ✅ **ENTREGUE 2026-08-11** — PR backend **#65** mergeado em `develop` (merge `a62eaa9`); arquivada em `changes/archive/2026-08/2026-08-11-keycloak-user-onboarding-auth/`. **Cabeça do Bloco 3 destravada:** `assessoria-settings-ui` e `coach-first-login-wizard` deixam de estar bloqueadas. Estado anterior: PR aberto e verde (`Build e testes (verify)` SUCCESS, +3469/−107 em 14 commits, entra **desligado por feature flag**). A 4.1 (validação ponta a ponta em ambiente real) fechou em **2026-08-10**, com a perna de navegador confirmada pelo founder — confirmação verbal, sem evidência anexada, o que basta para destravar a 4.2 mas não para ser a única prova quando a flag subir. A 4.2 fechou em **2026-08-10** e **achou um bug**; a 4.3 fechou em **2026-08-11** e achou outros dois. **Todas as 24 tasks concluídas — pronta para merge.** 4.2 (falhas injetadas contra Keycloak real — o nível unitário já cobre compensação em ordem inversa, `FAILED` vs `RECONCILIATION_REQUIRED` e "não deixa conta utilizável") **O que a 4.3 achou:** (a) a sonda do kill switch no runbook era inútil — `POST` com corpo vazio devolve `400` com a flag ligada ou desligada, porque o `@Valid` roda antes do teste da flag, então num incidente ela diria "não desligou" quando desligou; trocada por corpo válido com honeypot, validada nos dois sentidos. (b) **"Observar métricas" não é executável**: `/actuator/prometheus` responde 401 e não há coletor nenhum em `menthoros-infra` — o contador existe, quem olhe não; mesmo ponto cego da `enable-backend-ci`. Runbook executado nos resíduos de QA do HomeLab, e a V76 se provou no ambiente real (o `assessoria_id` sobreviveu ao DELETE). **Decisão do founder em 2026-08-11:** manter o endpoint habilitado (validação jurídica da LGPD feita). Duas ressalvas registradas: "não foi divulgado" não é controle — `/v3/api-docs/**` e `/swagger-ui/**` são públicos e expõem a rota com schema; o que protege é o anti-abuso (3/h por IP, 3/dia por e-mail, teto de 20/dia). E validar o texto não liga o enforcement: `LGPD_CONSENT_ENFORCEMENT` continua `report-only` por default, virar para `on` é passo separado. **O bug que a 4.2 achou:** a V75 declarou `assessoria_id` com `ON DELETE SET NULL` para a compensação poder apagar a assessoria; o Postgres zera a coluna, mas a entidade em memória segue com o id antigo, e o UPDATE que grava `FAILED` reescreve a referência pendurada e viola a FK. Toda falha compensada congelava o rastro no passo anterior (invisível à varredura por `RECONCILIATION_REQUIRED`), devolvia 500 de constraint no lugar da causa real, e fazia métrica e rastro discordarem. Corrigido pela **V76**, que derruba a FK e preserva o id para perícia — mesma convenção de `tenant_id`. Nenhum teste unitário alcançaria: sem banco não há FK. Ambiente no ar: backend `:8099` (worktree `.worktrees/onboarding-backend`), front `:5174`, Postgres `menthoros-4a1-db`. *(Contagem corrigida em 2026-08-10: as três tabelas diziam `0/20`, de uma versão anterior da spec, e o planejamento indicava que o Bloco 3 não tinha começado. O total é **24**, não 23 — a 4.1 estava marcada `[~]`, que nenhum dos dois contadores enxerga.)* **Desbloqueada** desde `add-coach-lgpd-consent`, entregue em 2026-07-31. **Cabeça do Bloco 3 (loop de auto-cadastro) — ver a tabela daquele bloco para escopo completo.** Provisionamento consistente Keycloak↔domínio + signup público. **Depende de `add-coach-lgpd-consent`** (o modal de consentimento precisa existir antes; o fluxo de auto-cadastro cria `Usuario` antes do aceite, então a whitelist do interceptor precisa ser revisada aqui — Q3 da change de consentimento). *(Contagem corrigida em 2026-08-03: a spec foi reescrita em 31/07 e o `tasks.md` real tem **20** tasks, não 8 nem 28; o tamanho passou de `S` para **L · Full**.)* |
| ~~`add-external-call-resilience`~~ | 17/19 | ✅ **ENTREGUE e ARQUIVADA** — backend PR #53 mergeado em `develop` (2026-07-27, commit `9bd32ff`); arquivada em `changes/archive/2026-07/2026-07-31-add-external-call-resilience/`. **S · Fast** (reescrita 2026-07-26). Timeout **por rota** de LLM + timeout do Strava + `Timer` de latência + corte do lote por falhas consecutivas. Circuit breaker saiu (ADR-0008). Sem isso, uma dependência externa lenta segura conexão de banco e degrada o app inteiro. **Um achado mudou o escopo durante a execução:** o `RetryTemplate` auto-configurado do Spring AI retentava `ResourceAccessException` (o próprio timeout) até 10× por default, o que **anularia** o teto por rota — pior caso de ~20 min na rota `plano`, segurando uma conexão do pool. Corrigido na task 3.1b com aprovação explícita do autor, por sair do texto original da change. ⚠️ **Pendência que viaja com a entrega: a task 5.2 (teste manual de degradação) NÃO foi feita** — derrubar/atrasar uma dependência real e confirmar que login e telas do atleta seguem respondendo. É o próprio sintoma que motivou a change, então é a validação que mais importa, e é a única que faltou; o arquivamento seguiu mesmo assim (precedente: gate A1 de `add-weekly-review-llm-focus`). Derivadas: 6.1 virou `refactor-llm-call-outside-transaction`, 6.2 (reprocessar análises `FAILED`) segue sem change. |
| `refactor-llm-call-outside-transaction` | 0/20 | 🔴 **PRIORIDADE ALTA** (decisão do founder 2026-07-26). A chamada ao LLM roda dentro da `@Transactional` da geração de plano: ~80s segurando uma conexão do pool de 10, com o lote fixando 4 delas. **Quebra em ~90 atletas numa mesma assessoria** — o lote passa dos 30 min do `recovery-limite-min` e um lote saudável vira "órfão". Não é risco hipotético: é aritmética de crescimento, e a primeira assessoria grande dispara. |
| ~~`fix-tsb-recalculo-resiliente`~~ | 26 | ✅ **ENTREGUE** — backend PR #54 mergeado em `develop` (2026-07-30), specs no product PR #10. **M · Full**, 26/26 tasks, 9 CAs cobertos por teste. Recálculo histórico de TSB passou de uma transação única sobre 400+ dias para **blocos de 30 dias**, cada um na sua própria transação (`TsbRecalculoExecutor`, `REQUIRES_NEW`) com `flush`/`clear` por bloco. Corrigiu também uma **mentira no contrato de falha**: javadoc e log prometiam rollback e restauração de um "backup" que nunca restaurou nada; a exceção agora informa o intervalo efetivamente reconstruído. **Rebaselinada em `develop`** — a baseline anterior (`f9e754b`, `feature/testes-carga-referencia`) foi descartada com evidência: sem `@Transactional` o método lançava `LazyInitializationException` para todo atleta, 5 erros de 5 no teste de equivalência do CA5 contra 5 verdes em `develop`. Três mudanças de comportamento observável assumidas de propósito: métricas de data futura passam a sobreviver ao recálculo, falha parcial não reverte blocos comitados (Decisão 4) e `PlanoMetaDados` pode ficar stale se a consolidação falhar após os blocos (Decisão 7). Validação: 2215/2215 unitários + 15/15 ITs de TSB. **Duas pendências viajam com a entrega:** (1) flake não diagnosticada em `TsbRecalculoJanelaMistaIT` — 1 falha em ~9 execuções, não reproduzida depois, causa raiz **não** identificada; a mitigação aplicada (pausa de 50ms no laço do leitor) é hipótese, não diagnóstico; (2) o `verify` completo segue vermelho por defeito pré-existente em `Task5p1ControllerIT`. Arquivada em `changes/archive/2026-07/2026-07-30-fix-tsb-recalculo-resiliente/`. |
| ~~`fix-tss-planejado-divergente`~~ | 12/12 | ✅ **ENTREGUE e ARQUIVADA** — backend PR #57 mergeado em `develop` 2026-08-02; arquivada em `changes/archive/2026-08/2026-08-02-fix-tss-planejado-divergente/`. **S · Full**, CA1–CA3 cobertos, 2311 testes verdes. BUG-CONF-001: o TSS **planejado** usava `min × RPE² / 90` e o **realizado** `h × IF² × 100` — duas fórmulas para a mesma grandeza, divergindo 2,4×–6×. Unificadas num núcleo compartilhado (`calcularTssPorRpe`), não por cópia da fórmula corrigida: copiar deixaria os números iguais hoje e livres para divergir amanhã, que é como o bug nasceu. **Três lições.** **Primeira: a premissa que justificava a migração era falsa, e só executar revelou.** A Q1 foi decidida duas vezes (recalcular os `PENDENTE`; depois todos os 129) antes de rodar em dev mostrar que apenas **3** das 129 linhas vinham da fórmula antiga — as outras 126 vieram do gerador de plano, que já opera na escala certa. Recalcular teria destruído 126 valores melhores que o substituto. A migração saiu do escopo e o mecanismo construído foi revertido; a operação só foi segura porque o `spec-reviewer` exigira snapshot de rollback no DoR. **Segunda: teste com colaborador mockado não prova escala.** O recálculo por mudança de duração tinha três testes, todos com `TssCalculatorService` `@Mock` — e o valor esperado era o stub `49`, exatamente o que a fórmula **antiga** produzia. Cobriam wiring e nada mais. **Terceira: refatoração "neutra" precisa ser provada, não presumida.** Extrair o núcleo reordenou `h × IF × 100 × IF` para `h × IF² × 100`; multiplicação em ponto flutuante não é associativa, e a reordenação virava o arredondamento em 8 pares (duração, RPE) da faixa válida, alterando silenciosamente o caminho **realizado**. A suíte de 2307 testes não pegou — só varredura exaustiva pegou, e a segunda passagem do Codex chegou aos mesmos 8 casos de forma independente. Duas sentinelas travam a ordem. |
| ~~`fix-fc-alvo-base-inconsistente`~~ | 35/37 | ✅ **ENTREGUE 2026-08-22** — backend PR **#75** + front PR **#84** mergeados em `develop`; `./mvnw clean verify` verde depois do merge (**2660 unitários + 113 de integração, zero falha**). **Ainda NÃO arquivada:** restam 2 tasks, e as duas são a mesma validação — **5.3** (bpm no relógio == bpm da tela, com conta real) e **3c.5** (como o provedor converte `workout_doc` → FIT no download, sem fonte). A métrica de sucesso da change **é** esse bpm, então arquivar antes de olhar o relógio seria declarar entregue o que não foi verificado. Decisões de 2026-08-22: CA8 mantido como implementado (evolução no Radar) e os débitos 4b.4/4b.5 aceitos conscientemente (também no Radar). Era 🔴 **EM ANDAMENTO — bug ativo, atleta treina na intensidade errada.** **S · Full**, backend. A FC enviada ao relógio sai **+10% a +21% acima** da prescrita (medido: atletas 195/170 → +14,7%, 172/142 → +21,1%, 165/150 → +10,0%). Causa: o domínio raciocina em **%LTHR** (Friel, `ZonaTreinoService`) e o canal relativo do padrão Garmin é **%FCmax por definição** (FIT: 1–100 = %FCmax, >100 = bpm+100) — não existe percentual que se possa enviar por ali e chegue certo. Correção: resolver para **bpm absoluto** antes do push, que é a forma nativa do Garmin (`Frequência cardíaca personalizada`), validada na UI do Garmin Connect. **Raio de alcance medido:** das 629 etapas com alvo de FC, **183 (28,9%)** usam `NN-NN% FCmax` e são as afetadas; as outras 446 já trafegam em bpm e chegam certas. **A contradição do prompt está visível na base:** ele entrega as zonas em bpm e proíbe inventar valores, mas exemplifica `"90-95% FCmax"` — 71% seguiram a instrução, 29% seguiram o exemplo. Também medido: **165 etapas (26%) pertencem a atletas sem `fc_limiar`** (3 dos 6 atletas), o que torna "sem objetivo" um caminho frequente e obriga a avisar o treinador quando a prescrição dele for descartada. Os testes não pegaram porque afirmam a **string da unidade**, nunca um bpm — mesma classe do BUG-CONF-001. |
| ~~`enable-frontend-ci`~~ | 23/23 | ✅ **ENTREGUE e ARQUIVADA** (`changes/archive/2026-08/2026-08-04-enable-frontend-ci/`, arquivada 2026-08-10) — entregue 2026-08-04, PR front #53 mergeado (`65ad0a2`) e **branch protection ativa em `develop`**: exige PR e os dois checks (`Lint, build e testes` 2m27s + `E2E (Playwright)` 1m21s), branch atualizada, sem bypass de administrador, sem push direto nem force-push. **S · Full.** **Preparar o CI encontrou mais do que a spec previa.** A suíte E2E nunca era executada e estava podre: **6 de 15 specs passavam**. Faltava `webServer` no `playwright.config` (só rodava para quem tinha `npm run dev` aberto); **a porta do Vite não era fixa** — default 5173 com fallback silencioso, e a porta é parte da identidade da app para o Keycloak, porque o `redirect_uri` sai de `window.location.origin` e o client só registra `localhost:5174` (teria quebrado o walking skeleton do PKCE); e o fixture de auth emitia JWT sem claim de tenant. No fim, **um bug de produção que os specs detectavam sem plateia**: `DashboardSidebar` apontava o item "Dashboard" para `href="/"`, obsoleto desde que a landing assumiu a raiz — clicar nele levava o `ADMIN` para a landing pública. **E2E 15/15.** É a tese da change comprovada enquanto se construía a própria change. **Desvio deliberado do `CLAUDE.md`, corrigido lá:** zero approvals exigidos, porque o GitHub não permite aprovar o próprio PR — com 1 approval e um único dev, nenhum PR seria mergeável. **Dois limites registrados, não pendências:** o rollback do gate (documentado no `CLAUDE.md` da raiz) **não foi exercitado** — testá-lo derrubaria o gate recém-criado; e o GitHub **desativa workflows agendados após 60 dias sem atividade**, justo quando o agendamento seria mais útil. ~~⚠️ **Colisão viva:** `login.spec.ts` testa o ROPC; quando o PKCE mergear, precisa ser reescrito ou este CI fica vermelho.~~ **RESOLVIDA em 2026-08-05** (front PR #54): reescrito para o fluxo PKCE contra um IdP falso — o fluxo roda inteiro e de verdade, só o provedor é simulado. **E o CI provou o próprio ponto de novo:** a suíte reescrita passava 15/15 localmente e caiu para 5/15 no runner, porque o issuer do IdP falso estava escrito à mão e casava com o `.env` da máquina do autor — arquivo que não é versionado. Testes que passam por depender de uma máquina são a falha que este CI existe para expor, e ele a expôs na primeira execução real. |
| ~~`enable-backend-ci`~~ | 15/16 | ✅ **ENTREGUE 2026-08-09** — PR backend **#63** mergeado (`f0b9911`) e **branch protection ativa em `develop`**: exige PR e o check `Build e testes (verify)`, branch atualizada com a base, `enforce_admins` (sem bypass), sem push direto, force-push nem deleção. **S · Full.** O repositório não tinha `.github/`; o único status check era o deploy do Railway — o PR mergeava sem nada ser compilado. Agora `clean verify` roda em cada PR, em cada push para `develop` e nos dias úteis às 09:00 UTC: **2368 unitários + 62 de integração**, e é a primeira vez que os 11 `*IT` executam automaticamente. **Repetiu a lição do front, com defeitos diferentes:** o primeiro run derrubou a hipótese de hermeticidade e achou **três testes que só passavam na máquina do dev** — `OpenApiConfigTest` e `CoreSecurityConfigTest` exigiam Postgres em `localhost:5432` (o padrão H2 já existia no mesmo pacote, em `HealthConfigTest`, e não fora aplicado a eles), e `UsuarioLgpdConsentRepositoryTest` comparava precisão de timestamp que o `TIMESTAMPTZ` não guarda — **bug irreproduzível fora de Linux**, portanto invisível a qualquer suíte local. Escopo ampliado para `src/test` por decisão do founder, em vez de virar change própria; **zero diff em `src/main`**. **O gate foi provado, não presumido:** PR #64 quebrou um `*IT` de propósito e o merge ficou `BLOCKED` (conferido fora de draft, porque draft bloqueia sozinho); `git push origin HEAD:develop` foi recusado com `GH006`, feito pelo dono e com `enforce_admins` — CA3 e CA7 na mesma janela. **A CA4 é mais fraca do que a spec deixava supor, e os timestamps mostraram:** o deploy do Railway **concluiu 2min41s antes** de o CI em `develop` terminar — eles correm em paralelo e o Railway não espera verificação nenhuma. A ordem vem inteiramente do gate no PR, o que torna a proibição de push direto **estrutural**, não acessória. **Descoberta que vale além desta change:** no plano Free, repositório **privado não tem branch protection nem rulesets** (403 "Upgrade to GitHub Pro") — privatizar o backend desmontaria o gate inteiro; registrado antes de alguém virar a chave sem saber. Tempo: **4min18s** contra 1min55s local, **quatro medições todas com cache frio** (cache criado numa branch não é visível de outra), então o número com cache quente ainda não existe e a decisão de dividir em jobs fica esperando por ele. **Adiada 1 task (CA8 meio provada):** o agendamento existe e vai rodar, mas **ninguém verificou se um run vermelho chega a um humano** — mesmo ponto cego herdado do `enable-frontend-ci`. |
| `fix-tenant-validation-not-found` | 0/20 | 🔴 **ABERTA 2026-08-02** — branch `fix/tenant-validation-not-found` já criada no backend, ainda sem commit. O `TenantValidationAspect` trata "recurso não existe" e "recurso é de outro tenant" como o mesmo caso: devolve 403 "Acesso negado" e emite `SECURITY_VIOLATION` nos dois. Descoberta ao investigar um 403 que o coach recebeu aprovando um plano da **própria** assessoria — o id não existia em tabela nenhuma. **O dano real é no log:** um plano deletado gera alarme de invasão, o que treina quem lê os logs a ignorar a linha que existe para sinalizar ataque de verdade. Decisão do founder: **404 nos dois casos** (não vazar existência entre tenants), com a distinção só no log. **M · Full** apesar do diff pequeno: muda contrato de API em 29 controllers e mexe em guarda de multi-tenancy. Não conserta a origem do id fantasma no front — change própria. |

---

## Pós-MVP — backlog priorizado por ROI de continuidade

*(8 changes desta seção — `add-zone-confidence-management`, `refine-tss-tsb-precision`,
`add-continuous-daily-load-management`, `add-running-field-tests`, `add-race-evaluation-skill`,
`add-taper-guidance`, `add-macrociclo-structure`, `add-health-connect-ingestion` — foram confirmadas
como backlog frio no CPO weekly 2026-07-24, ver changelog acima. `validate-interval-workout-standards`
e `refactor-iaservice-decomposition` seguem aqui sem confirmação formal de poda.)*

**Aceleradores de qualidade (logo após o MVP):**
`add-zone-confidence-management` (12) — confiança nas zonas. (~~`progressao-treinos`~~ ✅ antecipada e entregue na Sprint 14.)

**Dívida técnica estrutural:**
`refactor-iaservice-decomposition` (~26) — decompõe `IaServiceImpl` (~1500 linhas: schema + geração + validação) em colaboradores testáveis, sem mudança de comportamento. **Sequenciar logo após `debito-tecnico-camada-ia`** (mesma classe) para não re-inflar; não é bloqueante de feature nem de beta, por isso fica no pós-MVP.

> `harden-plan-generation-resilience` ✅ foi **entregue como change independente** (não foi folded em `migrate-plan-prompt-to-skills`): a resiliência estrutural foi construída sobre o `IaServiceImpl` atual, extraindo `PlanoEstruturaReparador` + `PlanoResilienceService` e deduplicando os 4 validadores em `validarEstrutura3Etapas`. Esses colaboradores já adiantam parte da decomposição prevista em `refactor-iaservice-decomposition`.

**Refino do motor analítico:**
`refine-tss-tsb-precision` (45) · `add-continuous-daily-load-management` (21) · `validate-interval-workout-standards` (79) · `add-running-field-tests` (35).

**Eval ao vivo da geração de plano (Camada C, deferida):**
Eval que chama o LLM real e pontua a qualidade do plano gerado (aderência + heurísticas), reaberta quando houver **uso real** para baseline de comparação. Saiu de `add-plan-generation-eval-harness` no reescopo product-lens (golden-master + checker determinístico cobrem o de-risco da migração sem custo/flakiness de LLM).

**Capabilities de produto avançadas:**
`add-race-evaluation-skill` (77) · `add-taper-guidance` (29) · `add-macrociclo-structure` (36).

**Strava (deferido atrás de clareza legal):**
A família `strava-*` — `strava-oauth` (20) · `strava-activity-sync` (12 restantes) · `strava-async-import` (88, backfill 90d) · `strava-webhooks` (23) · `strava-conditional-insights` (48) · `strava-risk-semaphore` (59) — **sai do caminho do MVP**. Só entra com (a) clareza legal sobre uso inference-only e (b) caminho de exibição por atleta que satisfaça a restrição do Strava (nov/2024). **Nunca** alimentar o ML acceptance predictor com dado da API do Strava.

**Onda mobile (futura — depende da decisão de construir app mobile):**
`add-health-connect-ingestion` (~22) — read layer on-device (Health Connect/HealthKit) para sync first-party automático. O **importer backend já entra no `first-party-ingestion-architecture`** (testável via POST); só o read layer mobile fica gated num shell Android/iOS/RN, que ainda não existe.

**Lançamento:**
`marketing-landing-page` (17).

**Guarda-chuva (absorvido / não implementar como bloco único):**
`introduce-coach-assistant-core-features` — conteúdo distribuído entre as changes do Bloco 1/2.

---

## Changes concluídas (fora de sprint)

### `polish-workout-profile-legibilidade` ✅ **ARQUIVADA** — o perfil parou de afirmar o que não é verdade (2026-08-20)

**Entregue:** `menthoros-front` **#81** (merge `584397c`). **1215 testes unitários em 134 arquivos**
e **63 specs E2E**. Arquivada em
`changes/archive/2026-08/2026-08-20-polish-workout-profile-legibilidade/`.

**Nasceu de uma navegação de verificação em treinos reais, feita logo após o merge da
`refactor-workout-profile-chart`.** O encoding entregue lá funciona — num intervalado dá para ler
"seis tiros fortes com recuperação entre eles" sem ler texto. A navegação colheu seis observações:
quatro de acabamento e **duas do gráfico afirmando o que não é verdade**.

A **razão trabalho:recuperação** exibia `11:4`, `3:8` e `7:3` em treinos reais. Sem série, o cálculo
caía num fallback global que classificava por zona sobre o treino inteiro, e aquecimento e
desaquecimento entravam como "recuperação" — o `3:8` dizia que o atleta descansa quase três vezes
mais do que corre forte, com o gráfico ao lado mostrando o contrário. Virou `null` sem série: métrica
ausente é melhor que métrica que mente. E a **zona escrita na prosa** ("Corrida contínua Z2" em
`descricaoEtapa`) era descartada, então o bloco saía hachurado — "não sei a zona" — com o dado
escrito ali.

Acabamento: eixo em unidade única acima de uma hora (`0 10 … 50 1:00 1:10 1:15` misturava dois
sistemas na mesma régua, e `1:10` colidia com `1:15`); rótulo **medido** em vez de estimado, porque
`length * 6` escolhia "DESAQUECIMENTO" para um bloco de 100px onde ele mede 99; e série expandida
sem `blocoId` voltou a ser série, o que trouxe de volta o bracket `n×` e calou o `REC` repetido seis
vezes.

**O DoR reprovou a primeira versão, e o achado era real.** A task da série prometia "reusar o
critério do `itensFromEtapas`" — e não dava: os helpers são privados, tipados em `EtapaTreinoDto`, e
exigem a string literal `INTERVALADO`, enquanto o perfil reconhece também `tiro` e `esforço`.
Importar como estava faria a série **sumir em silêncio** nos treinos que o próprio módulo já trata
como trabalho. Virou extração parametrizada por assinatura e predicado — uma heurística, dois
vocabulários declarados —, com o critério de que o `TreinoEditDialog.test.tsx` continuasse verde
**sem alteração de asserção**, porque o editor é caminho de escrita. Cumprido.

**Três defeitos vieram de olhar a tela, e nenhum apareceu em teste.** A **descrição da etapa era
apagada ao salvar** (`serializarItens` não emitia `descricaoEtapa`) — perda de dado anterior a esta
change, sem relação com gráfico, corrigida aqui por decisão explícita. A **rampa era desenhada acima
do próprio nominal**, então o aquecimento saía mais alto que o bloco principal. E o **regenerativo
saía de cabeça para baixo**: a zona-alvo do treino era aplicada só ao miolo e o resto seguia em
altura de papel — duas escalas no mesmo eixo, com o corpo em 0.15 e o aquecimento em 0.46.

**A lição de processo é a mesma das duas changes, e agora com número.** Somando as duas, **oito dos
defeitos mais sérios não apareceram em teste** — vieram de renderizar e olhar. Três deles foram
causados por correções anteriores minhas, feitas sem ver o resultado na tela. O gate de qualidade
achou o que a análise no papel não achou (a rampa degenerando em patamar no piso da escala), o que
reforça a ordem: renderizar, olhar, revisar — nessa sequência, e nenhuma substitui a outra.

**Aberto:** `desenhar-bloco-sem-zona-conhecida` (a laje cinza que ocupa até 73% do gráfico em
treinos contínuos), não escalonada e aguardando decisão de design — com a pergunta que deve vir
antes: que fração dos treinos reais cai no modo degradado? E a conversão **bpm → zona**, que é
provavelmente o maior retorno disponível: as etapas reais trazem "165-175 bpm", e o dado de
intensidade existe, só não está na unidade que o front lê.

### `refactor-workout-profile-chart` ✅ **ARQUIVADA** — o perfil do treino passa a ser legível sem leitura (2026-08-19)

**Entregue:** `menthoros-front` **#80** (merge `123e59a`). **1168 testes unitários em 132 arquivos**
e **61 specs E2E**. Arquivada em `changes/archive/2026-08/2026-08-19-refactor-workout-profile-chart/`.

**O gráfico do treino não respondia à pergunta que o treinador faz.** O `WorkoutTimelineChart`
jogava toda a intensidade numa borda esquerda de 3px e usava o preenchimento — o maior canal visual
— para a etapa estrutural em vez da zona; as alturas iam de 65 a 128px num container de 70. E
**mentia**: badge, legenda e cor saíam de derivações independentes, então "Z1 100%" sobre blocos
laranja era consequência esperada do desenho, não azar. Reconstruído sobre o encoding canônico
(largura = tempo, altura = intensidade, cor = zona), com um seletor puro único do qual badge,
altura, cor e distribuição saem no mesmo retorno — não há como divergirem sem quebrar teste.

**A change nasceu de uma spec de componente pronta, e o gate de DoR a reprovou três vezes.** O Codex
achou cinco Critical na primeira passada, e quatro eram reais: a métrica de sucesso pressupunha um
canal de analytics que não existe no front; a assinatura do seletor servia só uma das **três** formas
de etapa do app; o chip IF era inimplementável na revisão (`TreinoPlanejadoDto` não tem
`intensidadePlanejada` — virou DEP-5); e **cinco critérios de aceite não eram verificáveis onde a
spec os colocava** — o Vitest roda jsdom com `css: false`, onde toda medida é zero e um teste de
altura passa nas duas versões. Migrados para Playwright, com a parte estrutural em Vitest.

**O padrão que mais custou: os três defeitos mais sérios não saíram de teste nenhum.** A geometria
era calculada contra uma largura fixa de 600px enquanto o plot media 532 — o último bloco vazava e
era cortado pelo `overflow: hidden`, então o desaquecimento sumia e o eixo de tempo mentia. O rótulo
das rampas era decepado pelo `clip-path` ("AQUECIMENTO" virava "TO"), e o AC-7 não pegava porque
procura `…` e `text-overflow: ellipsis`. E o modo degradado saía **chapado** no treino mais comum,
porque a spec dava o mesmo nível a aquecimento, principal e desaquecimento — o defeito D2 de volta
pelo caminho que hoje é o normal; esse foi o founder quem apontou, olhando a tela. Os 1168 testes
passavam nos três casos.

**Emendas aplicadas na spec canônica**, não como desvio silencioso: `ProfileEtapaInput` com três
adaptadores; DEP-5; `prescribed` passa a significar zona declarada (exigir "alvo numérico + limiar"
deixaria todo treino permanentemente degradado); e a §4.6, cuja prosa contradizia a própria tabela e
o AC-4.

**Métrica de sucesso: qualitativa por decisão.** Não há canal de analytics no front e construí-lo não
pertence a uma change de gráfico. Feedback do treinador após uma semana, com a taxa de edição após
aprovação (`editadoPeloCoach`) como contra-métrica — sai do banco, sem instrumentação nova.

**Aberto:** a avaliação qualitativa (task 5.2), que só roda depois do uso. E os follow-ups, com
**zona alvo em bpm** na frente: as etapas reais trazem `fcAlvoEtapa` como "165-175 bpm", não como
token `Z1`–`Z5`, então caem inteiras no degradado. É o maior retorno disponível — o dado de
intensidade **existe**, só não está na unidade que o front lê. Depende do `fcMax` do atleta (DEP-2).
Também: DEP-1, DEP-3, unificação das três formas de etapa e migração `zone` → `workoutZone` nos
demais gráficos.

### `preservar-serie-estruturada-na-edicao` ✅ **ARQUIVADA** — editar um treino na revisão achatava a série (2026-08-18)

**Entregue:** `menthoros-backend` **#72** e `menthoros-front` **#79**. Backend **2620 unitários + 103
de integração**; front **993 testes + 48 specs E2E**. Arquivada em
`changes/archive/2026-08/2026-08-18-preservar-serie-estruturada-na-edicao/`.

**O treinador mudava o TSS e a série virava esforço contínuo.** Sem aviso, com a duração total ainda
correta. Dois mecanismos independentes, e por isso corrigir um só não resolvia: o `TreinoEditDialog`
hidratava qualquer treino em quatro campos fixos e serializava de volta no máximo quatro etapas — uma
série de 4 pares heterogêneos entrava como o primeiro par e saía como 4 cópias dele; e o
`aplicarEtapasPatch` descartava o `blocoId`, que o caminho de adição já gravava certo.

O editor passou a usar o modelo de itens do `TreinoAddDialog` (extraído para um módulo comum), com
`itensFromEtapas` fazendo a hidratação inversa — agrupa por `blocoId` quando existe, infere a janela
repetida quando não (treinos da IA nascem sem). No backend, os dois caminhos de escrita viraram um:
o do patch expandia em DTO intermediário, e DTO não tem como carregar o agrupamento.

**O processo pagou por si.** O pre-mortem pegou, antes da implementação, que o design perdia a guarda
`blocosMudados` — sem ela toda edição administrativa viraria regravação estrutural, "o dano original
com aparência de feature correta". O DoR pegou que o código não compilaria (`TreinoPlanejadoPatch.etapas`
era mais estreito que o contrato real) e que a change dependia de um PR ainda aberto. O quality gate
pegou dois bugs introduzidos na reescrita: **etapa adicionada nunca era salva** (não havia seletor de
tipo, e `serializarItens` descartava item sem tipo em silêncio) e **a guarda valia pela metade**
(`distanciaKm`/`duracaoMin` são derivadas das etapas e escapavam dela). Os dois passaram pela mesma
brecha — testes que exercitavam o caminho sem verificar o resultado.

**Aberto:** validação em ambiente real (task 4.4) — o E2E mocka a API, então prova o payload que sai
do browser, não o backend gravando o `blocoId`. E o PR **#73**, que recupera três testes de contrato
escritos no gate e não commitados a tempo.

### `fix-fartlek-expansao-etapas` ✅ **ARQUIVADA** — o fartlek chegava ao relógio como bloco único (2026-08-17)

**Entregue:** `menthoros-backend` **#71** (`deb74b9`), quatro commits. Suíte **2614 unitários + 103
de integração**, verde no CI. Arquivada em `changes/archive/2026-08/2026-08-17-fix-fartlek-expansao-etapas/`.

**O `4x (1min forte + 2min leve)` ficava preso como texto na descrição.** Duas causas encadeadas. A
expansão em `IaServiceImpl` só era tentada para `tipoEtapa=INTERVALADO`; o LLM tipa a série ora
assim, ora como `PRINCIPAL` — ambos válidos no schema — e no segundo caso a etapa passava intacta,
sem log e sem validação reclamando. O detector de fartlek estava correto e nunca era chamado.

Corrigido o guard, restava a segunda: `IntervalsIcuWorkoutConverter` agrupa por `blocoId`, que só o
caminho do treinador atribui. A série chegaria ao relógio como 8 steps soltos. Atribuir `blocoId` na
expansão não era viável — `EtapaTreinoLlmDto` alimenta o JSON schema enviado ao modelo, e o campo
obrigaria o LLM a inventar um UUID por etapa. O converter passou a **inferir** o bloco pela repetição
observada, o que também corrige os treinos já persistidos, sem migration.

**Quality gate pegou um falso positivo real:** a inferência agrupava qualquer par repetido, então um
ondulado `10min Z2 / 5min Z3 / 10min Z2 / 5min Z3` virava `2x (...)` — uma série que o treinador não
prescreveu. Levantado de forma independente pelo `code-reviewer` e pelo Codex (convergência
cross-model). A janela passou a exigir uma etapa `INTERVALADO`.

**Escopo ampliado por decisão do usuário:** entrou junto o **500 em `GET /api/v1/planos/{atletaId}`**
depois de rejeitar um plano e gerar outro. `rejeitarPlano` mexe só em `reviewStatus` — o `status`
continua `PLANEJADO` —, e a query filtrava apenas `status != CONCLUIDO`, devolvendo `Optional` sem
ordenar: dois planos casavam e o Hibernate lançava `NonUniqueResultException`. O índice parcial
`uk_plano_semanal_atleta_semana_ativo` (V52) já definia a semântica certa (`WHERE review_status <>
'REJEITADO'`); a query passou a usar o mesmo predicado. Ordenar por semana não resolveria — os dois
planos são da mesma semana.

**Validado em ambiente real:** 12 blocos alternando Z4/Z1 no detalhe do plano, `5×` no editor.

### `fix-tipo-treino-progressao` ✅ **ENTREGUE** — o longão que o sync não reconhecia travava a progressão (2026-08-10)

**Sem change OpenSpec** — bug encontrado e corrigido direto na branch, sem passar por `changes/`.
Registrado aqui porque o efeito é de produto, não de refactor: o atleta cumpria a prescrição e mesmo
assim não recebia carga. Não há o que arquivar.

**Entregue:** `menthoros-backend` **#66** (`d8fe656`), dois commits — o fix (`e9392e8`) e a cobertura
dos consumidores (`f4ef83f`). Suíte **2390 unitários + 65 de integração**, verde no CI.

**O tipo do treino tinha duas verdades e a errada era a que decidia carga.** Um realizado vindo de
integração tem o `tipoTreino` inferido por heurística de duração e FC: um longão de menos de 90 min
com FC acima do limiar vira `TEMPO_RUN`. A reconciliação vincula o realizado ao planejado mas **não
corrige o tipo** — de propósito, porque a divergência é o insumo do `TipoTreinoConsistenciaValidator`.
O resultado é que `contarLongoes` não via esses longões, `longoes21d` nunca alcançava o mínimo de 2, e
`podeProgredir` ficava preso em `MANTER`/`PROGREDIR_LEVE`. O mesmo treino ainda entrava em
`TREINOS_DUROS` (que inclui `TEMPO_RUN`) e inflava o RPE médio, podendo empurrar para `REDUZIR` —
o atleta era penalizado pelo treino que deveria tê-lo promovido.

**A correção é um acessor, não uma escrita.** `TreinoRealizado.getTipoTreinoEfetivo()`: onde a
pergunta é "cumpriu a sessão prescrita?" (progressão, elegibilidade de intervalado, prompts do
planejador, projeção de prova), a prescrição do coach prevalece. Onde a pergunta é "o que de fato foi
feito" (TSS, decoupling, inferência de limiar, matching da reconciliação), continua valendo o tipo
executado. O campo persistido não é sobrescrito.

**O fix nasceu com metade da cobertura.** O primeiro commit aplicou o acessor em seis pontos e testou
dois. Os outros quatro ficavam livres para regredir em silêncio — e regressão em prompt é a pior de
achar, porque não quebra nada: só muda o texto que o modelo lê. O segundo commit fechou isso com um
teste por consumidor, sempre com o caso **sem vínculo** como controle, mais um `*IT` provando que as
duas queries trazem o planejado em fetch join (trocar o JPQL pelo método derivado não quebra nada
funcionalmente — vira um SELECT por treino numa janela de 42 dias).

**Cobertura verificada por contrafactual, não por contagem.** Neutralizando o acessor, **6 dos 7**
novos testes unitários falham; o sétimo é o controle sem vínculo, que corretamente segue verde.
Removendo o `LEFT JOIN FETCH`, **2 dos 3** ITs falham. O teste de progressão atravessa
`calcularHistorico` → `calcularDecisao` de propósito: a contagem isolada já era coberta, mas o que o
coach percebe é a decisão.

**Dois follow-ups abertos, nenhum agendado:**

1. **A heurística continua imprecisa para atleta sem plano.** O gate de `LONGO` é 90 min em
   `StravaActivityServiceImpl:489` e `IntervalsIcuActivityMapper:337`, duplicado. Quem não tem
   prescrição não tem vínculo, então o acessor não o socorre. Ajustar reclassifica histórico —
   merece change própria.
2. **Sem migration.** Treinos já vinculados passam a ser lidos corretamente; os órfãos permanecem com
   o tipo inferido.

### ~~`enable-backend-ci`~~ ✅ **ARQUIVADA** — CI que executa `verify` antes do merge (2026-08-09)

**Arquivo:** `changes/archive/2026-08/2026-08-09-enable-backend-ci/`. **Diferido com motivo:** a
task 1.4b (CA8 — confirmar que a falha do agendamento **notifica alguém**), que depende da primeira
execução agendada em 2026-08-10 09:00 UTC; e o tempo de CI com cache quente, que sai no próximo PR.

**S · Full · backend.** O repositório não tinha `.github/`. O único status check nos PRs era o deploy
do Railway — consequência do merge, não condição dele. Foi essa lacuna que deixou `Task5p1ControllerIT`
vermelho por **2,5 meses**, sem cobertura de contrato nos três endpoints de reconciliação manual do
coach, autorização e isolamento multi-tenant incluídos.

**Entregue:** `menthoros-backend` **#63** (`f0b9911`) e branch protection em `develop` exigindo
`Build e testes (verify)`, com `strict` e `enforce_admins`.

**O CI se pagou antes de virar gate.** Três testes passavam só por dependerem do ambiente do dev, e
o mais interessante é o padrão: em dois casos, **a solução correta já existia no mesmo pacote** e não
fora aplicada; no terceiro, o defeito era **irreproduzível fora de Linux**. Nenhum deles seria achado
rodando a suíte na máquina de quem os escreveu — que é a definição do problema que esta change
existe para resolver.

### ~~`fix-silent-token-renewal`~~ ✅ **ARQUIVADA** — renovação sem recarregar a página (2026-08-06)

**Arquivo:** `changes/archive/2026-08/2026-08-06-fix-silent-token-renewal/`. **Diferidos com motivo:** CA6 no Firefox (Chrome e Safari verificados; Safari é o restritivo) e a Q1 de persistir token entre abas (pergunta de conforto, decidir após conviver).

**S · Full · front + infra.** A tela recarregava inteira a cada ~4 minutos: `accessTokenLifespan` de
300s com notificação 60s antes, e o handler de `accessTokenExpiring` fazia `signinRedirect`. Não era
glitch de render — era navegação, com perda de estado e de scroll no meio do trabalho do treinador.

**Entregue:** `menthoros-infra` **#6** (rotação de refresh token no realm, aplicada nos dois
ambientes) e `menthoros-front` **#57** (`fd37fc1`).

**A decisão anterior estava meio certa.** O `oidcConfig.ts` desligava o `automaticSilentRenew`
alegando cookie third-party em iframe — verdade sobre o iframe. O erro foi generalizar para "nenhuma
renovação silenciosa serve": `signinSilent()` usa o refresh token quando existe, e um POST com token
no corpo não depende de cookie.

**Dois achados que só o navegador revelou:**

1. **Bug pré-existente do `StrictMode`.** O `signinCallback()` trocava o mesmo `code` duas vezes; o
   Keycloak trata como replay e **remove a client session**. O desenho antigo escondia isso — com
   redirect a cada 4 min, o app ganhava sessão nova antes de precisar da antiga. Ao depender do
   refresh token, ela vira a única que existe. Causalidade provada por experimento (`StrictMode` off:
   zero; on: uma por carregamento; com a correção: zero).
2. ~~**Multi-aba com rotação ligada derruba o coach para o login.**~~ **AFIRMAÇÃO FALSIFICADA por
   teste controlado — o problema não existe.** A/B com a rotação como única variável, duas abas em
   ambos: `false` → A 363s / B 360s ok; `true` → A 364s / B 370s ok; zero erros nos dois. A causa
   real das falhas observadas era o achado 1 (troca dupla do código), ainda não corrigido na época —
   com duas abas, o dobro de client sessions destruídas fazia o sintoma parecer de multi-aba.
   **Registrado porque o erro chegou a constar aqui, no `tasks.md` e no corpo do PR #57.**

**Também descoberto e corrigido no realm (`disable-ropc-direct-grant` e follow-ups):** rotação de
valor sem revogação era cosmética — o refresh antigo seguia aceito. Agora replay é recusado **e**
invalida a sessão, nos dois ambientes.

**CA1, CA2, CA3, CA4 e CA5 verificados.** CA6 parcial: **Safari OK** (o mais restritivo — o ITP é o
que quebraria o iframe), **Firefox pendente**.


> ✅ **Resolvido em 2026-08-10.** As duas changes anotadas aqui pela auditoria de 2026-08-03
> (`fix-volume-realizado-recalculo` e `fix-reconciliation-it-auth`) foram arquivadas, junto com uma
> terceira que a mesma varredura não tinha pego (`enable-frontend-ci`, 23/23, entregue em 2026-08-04).
> As três passaram a constar na tabela abaixo. Nenhuma change 100% completa segue em `changes/`.
>
> **O padrão que se repete vale mais que o mutirão:** a `fix-volume-realizado-recalculo` ficou **18
> dias** entregue e invisível ao planejamento, e não foi a primeira — é a mesma classe de órfã das
> auditorias de 2026-07-20 e 2026-08-03. Arquivar é operação de pasta, não de doc; enquanto não
> acontece, a change conta como ativa em qualquer varredura e volta como "zumbi" na auditoria
> seguinte. O gasto real não é mover a pasta: é a auditoria que a redescobre toda vez.

| Change | Tasks | Conclusão | Arquivo |
|---|:---:|:---:|---|
| `add-status-endpoint` | 13/13 | 2026-06-13 | `changes/archive/2026-06/2026-06-13-add-status-endpoint/` — cobaia do workflow `/implement → /qa → /ship`; endpoint público `GET /api/v1/status`. |
| `add-current-user-endpoint` | 13/13 | 2026-06-16 | `changes/archive/2026-06/2026-06-16-add-current-user-endpoint/` — `GET /api/v1/users/me`; DIP no service + `@WebMvcTest` (current-user-quality-debt foldada). |
| `reject-inactive-users` | ✓ | 2026-06-16 | `changes/archive/2026-06/2026-06-16-reject-inactive-users/` — `JwtTenantFilter` rejeita `ativo=false` com 423 (fail-safe via leitura direta / 503). |
| `harden-tenant-isolation` | ✓ | 2026-06-16 | `changes/archive/2026-06/2026-06-16-harden-tenant-isolation/` — `TenantContext` usa `ThreadLocal`; finders sem tenant removidos/documentados. |
| `harden-actuator-admin-exposure` | ✓ | 2026-06-16 | `changes/archive/2026-06/2026-06-16-harden-actuator-admin-exposure/` — health `show-details: when-authorized`; isenção `/api/admin` documentada. |
| `current-user-quality-debt` | ✓ | 2026-06-16 | `changes/archive/2026-06/2026-06-16-current-user-quality-debt/` — foldada em `add-current-user-endpoint` (DIP, `@WebMvcTest`, índice descartado). |
| `add-assessoria-onboarding` | ✓ | 2026-06-16 | `changes/archive/2026-06/2026-06-16-add-assessoria-onboarding/` — cadastro de assessoria (Keycloak Organizations), role `ATLETA`, vínculo `Usuario`↔`Atleta`, convite. Código em develop; **infra Keycloak de produção + migração Groups→Organizations pendentes** (runbook `docs/add-assessoria-onboarding-keycloak-runbook.md`). |
| `build-skills-core-foundation` | superada | 2026-06-16 | `changes/archive/2026-06/2026-06-16-build-skills-core-foundation/` — §1–4 (contratos/registry/orquestrador/persistência `V32`/skills) já entregues por `introduce-domain-skills-architecture` e follow-ups. §5 (integração na geração de plano) reavaliada: a thread de IA (`eval-harness → debito-tecnico → llm-tool-use → llm-code-switching`) cobre a modernização do prompt. |
| `add-plan-generation-eval-harness` | 12/12 | 2026-06-16 | `changes/archive/2026-06/add-plan-generation-eval-harness/` — Golden-master determinístico de `buildOptimizedPrompt` (5 arquétipos, 707 testes); rede de regressão antes da migração formatters→skills. `LocalDate.now()` congelado, `Locale` pt-BR, leitura classpath-safe. Zero mudança de produção (apenas `src/test/`). |
| `add-coach-attention-queue` | 13/13 | 2026-06-18 | `changes/archive/2026-06/2026-06-18-add-coach-attention-queue/` — Fila de atenção on-demand: `GET /api/v1/coach/attention-queue`, 6 sinais avaliados (fadiga/TSB, sobrecarga/plano, aderência 14d, inatividade, zonas vencidas, sem plano), dedup por atleta, corte severity ≥ ALTA, cap N=20. `hasAlert` do calendário semanal integrado. 38 testes novos, 825/825 verde. Follow-ups 7.1–7.5 registrados (não bloqueantes). |
| `add-recommendation-explainability` | 9/9 | 2026-06-19 | `changes/archive/2026-06/2026-06-19-add-recommendation-explainability/` — Campo aditivo `explanation: RecommendationExplanation` em `CoachAttentionItemOutputDto`: `rationale` PT-BR determinístico por sinal, `sourceRules` com constantes estáticas por método avaliador, `confidence=HIGH` (v1). `SinalAtencao` ganhou compact constructor com fail-fast. `@Schema` em `ExplanationConfidence`. 829/829 verde, QA gate (3 revisores) sem Critical. |
| `wire-coach-identity-and-attention-queue` | 14/14 | 2026-06-19 | `changes/archive/2026-06/2026-06-19-wire-coach-identity-and-attention-queue/` — Identidade real no `CoachLayout` (remove `mockCoach`/`mockTenant`); `useCurrentUser` + `useAttentionQueue`; `CoachAttentionQueuePage` com 3 estados (loading/erro/lista); SeverityChip cobrindo CRITICA/ALTA/MEDIA; contexto passado via `<Outlet context={...}>` (elimina dupla requisição). 44/44 testes, zero erros TS. `CoachInboxPage.tsx` deletado — será recriado em `add-coach-suggestion-inbox`. |
| `add-coach-suggestion-inbox` | 21/21 | 2026-06-19 | `changes/archive/2026-06/2026-06-19-add-coach-suggestion-inbox/` — Coach-in-the-loop v1: `tb_sugestao_coach` (V36), `SugestaoCoachGeneratorJob` (cron 06h, idempotente via UNIQUE partial index), service + controller (`/api/v1/coach/sugestoes`), 860 testes backend. Frontend: `CoachInboxPage` 3-colunas (filtros | lista com `CoachAthleteAvatar`/`SuggestionTypeBadge`/`ConfidenceBar` | painel revisão com tabs Detalhes/Raciocínio), busca os 3 status em paralelo, filtro local. Seed de dev `db/seed/seed_sugestoes_dev.sql`. Layout 3-colunas (mock original) validado pelo PO em detrimento do 2-painéis do design.md. |
| `manual-training-entry-lightweight` | 8/8 | 2026-06-19 | `changes/archive/2026-06/2026-06-19-manual-training-entry-lightweight/` — Desbloqueador de dado real: `POST /api/v1/atletas/me/treinos` + `GET /api/v1/atletas/me/treinos?dias=7`; `TreinoManualInputDto` (tipo/duração/distância/RPE/obs); best-effort match com `TreinoPlanejado`; `fonteDados=MANUAL`; formulário com chips de tipo, slider RPE, preview TSS, lista de recentes; ARIA acessível; 884 testes backend, 76 frontend. |
| `coach-plan-review-workflow` | 18/18 | 2026-06-20 | `changes/archive/2026-06/2026-06-20-coach-plan-review-workflow/` — Desbloqueador de confiança: `review_status` + `review_comment` em `tb_plano_semanal` (V37); `PlanoReviewService` com máquina de estados (AGUARDANDO→APROVADO/REJEITADO, 422 em transição ilegal); 4 endpoints (`GET /pendentes`, `GET /revisao?status=`, `POST /aprovar`, `POST /rejeitar`); atleta só recebe planos APROVADO; `CoachPlanReviewPage` 3-colunas (filtros+fila+painel minimalista); badge de pendentes no nav. 911 testes backend, 95 frontend. QA: nenhum achado Critical. |
| `debito-tecnico-camada-ia` | ✓ | 2026-06-17 | `changes/archive/2026-06/debito-tecnico-camada-ia/` — Confiabilidade da camada IA: `.entity()` (Spring AI structured output) em `WorkoutAnalysisListener` e `RaceProjectionNarrativeGenerator` no lugar de parsing manual; 3 user prompts externalizados; roteamento explícito `TaskComplexity.PLANO`/`gpt4oPlanoClient`; cache thread-safe; limpeza de templates órfãos/embedding morto. Validado em produção (análise pós-treino + log de roteamento). Suíte 701/701. Gap de resiliência estrutural (503 no REGENERATIVO) → folded em `migrate-plan-prompt-to-skills` (seção 9). |
| `add-athlete-progress-endpoints` | ✓ | 2026-06-17 | `changes/archive/2026-06/add-athlete-progress-endpoints/` — 5 endpoints read-only (PMC, zonas, recordes, readiness, home) sob `/api/v1/atletas`; readiness com heurística objetiva provisória; isolamento via service-layer (404 cross-tenant); Clock injetável. Suíte 733/733. **Follow-up:** extrair `CurrentAtletaResolver` quando `shell-dashboards` reusar a resolução `me`. |
| `add-coach-shell-dashboards` | 16 | 2026-06-17 | `changes/archive/2026-06/2026-06-17-add-coach-shell-dashboards/` — Primeira "casa" do treinador: roster de atletas + calendário semanal + KPIs por tenant. Flags `hasPendingSuggestion` iniciam `false` (preenchidas nas changes subsequentes). |
| `wire-coach-shell-to-dashboards` | M | 2026-06-18 | `changes/archive/2026-06/2026-06-18-wire-coach-shell-to-dashboards/` — Liga as 3 telas do coach aos endpoints reais (remove mocks): cliente curado + hooks + reconciliação DTO↔view-model. Frontend-only, zero backend. |
| `fix-openapi-client-generation` | L | 2026-06-18 | `changes/archive/2026-06/2026-06-18-fix-openapi-client-generation/` — `generate:api` determinístico restaurado: Fase A (`@Tag` ASCII kebab-case) + A2 (`@ArraySchema` em endpoints de lista). Cliente `src/api` mantido como fachada curada; geração integral adiada (Fase B). |
| `introduce-plan-constraints` | M | 2026-06-18 | `changes/archive/2026-06/2026-06-18-introduce-plan-constraints/` — Anti-alucinação (Sprint 7): seam `Constraint` (key+descrição+params); formatters emitem Constraints; bloco mandatório [1] no topo do prompt + `PlanQualityChecker` (4 keys) com contador Micrometer. Golden regenerado. |
| `harden-plan-generation-resilience` | M | 2026-06-18 | `changes/archive/2026-06/2026-06-18-harden-plan-generation-resilience/` — Resiliência estrutural (Sprint 8): `PlanoEstruturaReparador` + 1 retry com feedback → fim do 503 do REGENERATIVO 2 etapas (vira 422 com mensagem ao treinador). Dedup dos 4 validadores em `validarEstrutura3Etapas` + telemetria Micrometer. |
| `athlete-profile-drilldown` | 16/16 | 2026-06-20 | `changes/archive/2026-06/2026-06-20-athlete-profile-drilldown/` — Prontuário do atleta: endpoint agregador `GET /coach/atletas/{id}/perfil` (PMC 90d, aderência 8 semanas, plano vigente com 7 cards, top 3 sinais/sugestões, recordes). Tela `/coach/athletes/:id` com navegação roster → perfil. |
| `fix-intervalado-distance-calculation` | 10/10 | 2026-06-21 | `changes/archive/2026-06/2026-06-21-fix-intervalado-distance-calculation/` — Hotfix: distâncias erradas em treinos intervalados. `corrigirDistanciasEtapasTemporais` deriva `distanciaKm` via `duracaoMin ÷ paceZona` (Z2/Z1) para AQUECIMENTO/DESAQUECIMENTO/RECUPERACAO antes de `expandirEtapasAgregadas`. PR #8 mergeado. |
| `coach-edit-planned-workout` | ~22 | 2026-06-22 | `changes/archive/2026-06/2026-06-22-coach-edit-planned-workout/` — Fecha o ciclo de revisão: `PATCH /coach/planos/{id}/treinos/{id}` com `EtapaInputDto`, migration V39 (`editado_pelo_coach`), chip "Editado manualmente" na UI, TSS recalculado ou override manual. Validação concorrente via `@Version` + 409. PR #9 mergeado. |
| `infer-thresholds-from-recent-workouts` | ~18 | 2026-06-23 | `changes/archive/2026-06/2026-06-23-infer-thresholds-from-recent-workouts/` — Calibração automática de zonas: `ThresholdInferenceService` (quintil superior, MIN_AMOSTRAS=3, janela 30d, confiança ALTA/MEDIA/BAIXA); migration V40; Constraint no prompt; `LimiareisInferidosDto` no perfil coach; `LimiaresInferidosBanner` no frontend. Inclui bug fix de alucinação BPM para atletas sem limiar formal (fornece zonas calculadas em vez de "60-70 bpm"). PR #10 mergeado. |
| `introduce-coach-assistant-core-features` + `coach-assistant-triage-ui` | 8/8 (sprint 8) | 2026-06-26 | `changes/archive/2026-06/2026-06-26-introduce-coach-assistant-core-features/` — Guarda-chuva: endpoint agregado `GET /api/v1/coach/dashboard` (roster paginado + fila + calendário + insights); `aderenciaPercentual` (4 semanas) no `CoachAtletaResumoDto`; frontend inbox decomposto (3 hooks, 4 adapters, 5 painéis); `nivelExperiencia` e `aderenciaPercentual` exibidos no roster; `React.lazy` (−47 kB). PRs backend #12 + frontend #8 mergeados 2026-06-25. Follow-ups 7.1–7.2 documentados (mocks residuais não bloqueantes). |
| `fix-coach-inbox-metrics` | 8 seções | 2026-06-26 | `changes/archive/2026-06/2026-06-26-fix-coach-inbox-metrics/` — Frontend-only (Fast track): alinha threshold TSB do `formFromTSB` (−25→−20) com o `danger` do backend; corrige 4 bugs de métricas no adapter (`acuteLoad` ctl→atl, monotonia/loadDelta hardcoded, fadiga inline); adiciona ACWR (tile no inbox + coluna no roster com zonas de risco) e métrica "Forma" via `formFromTSB`; exibe limiares inferidos (FC/pace) no tab Status. Refactor pós-QA: zonas/tons centralizados em `getAcwrZone`/`getTsbFormaTone`. 205 testes (+32). PR #9 mergeado. Desbloqueia `coach-training-strain` e `coach-race-form-prediction`. |
| `consolidate-coach-inbox-tabs` | 8 seções | 2026-06-26 | `changes/archive/2026-06/2026-06-26-consolidate-coach-inbox-tabs/` — Frontend-only (Fast track): consolida o drill-down do atleta de 5 para 3 abas (Diagnóstico, Plano, Provas & sugestões), todas por-atleta. Corrige o bug global vs atleta (`if (dashboardInsights)`/`if (dashboardCalendar)` nunca null) que tornava as métricas PMC de `fix-coach-inbox-metrics` código morto; remove conteúdo mock (Ajuste rápido, Semana atual, Marcar como concluído, Últimos treinos vazio); deleta 7 arquivos órfãos + tipo `WorkoutItem`. Saldo −856/+353. 208 testes. PR #10 mergeado. Follow-ups F1–F4 (semântica de `recovery`, `planStatus` CONCLUIDO, fallback `acuteLoad`, `trainingType`) na spec arquivada. Desbloqueia o tile de Strain e o card de previsão de forma. |
| `coach-training-strain` | 6 seções | 2026-06-26 | `changes/archive/2026-06/2026-06-26-coach-training-strain/` — Frontend-only (Fast track): Training Strain (TSS_semanal × monotonia, Foster) como `DetailMetric` na aba Diagnóstico; `calcularStrain` + `getStrainZone` (padrão `getAcwrZone`); helper `ultimosTssValidos` compartilhado com `calcularMonotonia` (DRY do QA). 217 testes (+9). PR #11 mergeado. Resta `coach-race-form-prediction` na sequência do dashboard do coach. |
| `coach-race-form-prediction` | 6 seções | 2026-06-26 | `changes/archive/2026-06/2026-06-26-coach-race-form-prediction/` — Frontend-only (Fast track): previsão de forma (TSB) no dia da prova via decaimento exponencial do PMC (CTL τ=42, ATL τ=7, taper puro). `calcularPrevisaoForma` (reusa `formFromTSB`) + `calcularDiasAteProva` (`hoje` injetável); helper `provasOrdenadas` compartilhado com `buildRaceCalendarFromProfile` (DRY do QA); card condicional na aba Provas & sugestões. 225 testes (+8). PR #12 mergeado. **Fecha a tríade de melhorias do dashboard do coach** (métricas → consolidação de abas → strain → previsão de forma). |
| `coach-roster-operational-actions` | 7 seções | 2026-06-26 | `changes/archive/2026-06/2026-06-26-coach-roster-operational-actions/` — Frontend-only (Fast track): traz ações por-atleta do `/atletas` legado para o roster `coach/atletas` (coluna `type:'actions'`): Plano (`PlanosDialog`), Sincronizar Strava (`SyncStravaButton`), Projeção (`GerarProjecaoDialog`). Reuso dos dialogs legados as-is. 230 testes (+5). PR #13 mergeado. **Parte 1/2** da migração; resta `coach-roster-athlete-crud`. Primeiro `/qa` com camada cross-model Codex — pegou bug (`connected={false}` travava o sync) que os reviewers Claude não viram. |
| `align-coach-dialogs-projecao` | 5 seções | 2026-06-26 | `changes/archive/2026-06/2026-06-26-align-coach-dialogs-projecao/` — Frontend-only (Fast track): adequa o fluxo de projeção de prova ao dark-first (papel/gradientes → `elevation`/tokens; `GAP_*`/`CTL_*`/`ConfidenceBadge`/`MarcarOficialButton` → `semantic.*`; 78 hex → 0). Inclui fix de contraste (`surface[400]`, achado cross-model) e **fix de layout** (form quebrava o flex → botões cortados; `display:contents`). 238 testes. PR #16 mergeado. **3/3** da adequação dos dialogs. |
| `standardize-coach-dialogs` | sem spec | 2026-06-27 | Sem change OpenSpec (padronização iniciada via `/frontend-design`). **Fecha o arco** dos dialogs do coach: eleva `CoachDialog` a shell único e migra **todos** os dialogs (projeção, atleta, confirmações, Provas light-mode→dark, plano, edição/criação de treino, modais embutidos e os 2 inline de página). Cria helpers de botão (`PRIMARY/SUCCESS/DANGER/GHOST` em `actionButtonSx`) + palette semantic no tema; fonte única de cor de treino (`workoutColors`: tipo/status/etapa + escala de esforço). Resolve dep. circular movendo as 3 primitivas para `src/shared/`. `PROVA_STATUS_COLORS` tokenizado; 2 bugs de `borderTop` vazio corrigidos. Saldo −439 linhas. 241 testes. `/qa` sem Critical. PR #19 mergeado. |
| `align-coach-dialogs-base-plano` | 5 seções | 2026-06-26 | `changes/archive/2026-06/2026-06-26-align-coach-dialogs-base-plano/` — Frontend-only (Fast track): **fundação** da adequação dos dialogs ao shell coach. Cria `CoachDialog` base (dark-first, `elevation.highest`, header uppercase, `aria-labelledby`) + adequa `PlanosDialog`/`TreinoCard` (zero hex; RPE em zonas semânticas). 238 testes (+4). PR #15 mergeado. **1/3** da adequação; destrava `align-coach-dialogs-atleta` e `align-coach-dialogs-projecao`. Codex (QA) pegou a falta de nome acessível. |
| `coach-roster-athlete-crud` | 6 seções | 2026-06-26 | `changes/archive/2026-06/2026-06-26-coach-roster-athlete-crud/` — Frontend-only (Fast track): CRUD de atleta no roster `coach/atletas` (criar via botão Adicionar, editar com `buscarAtletaPorId`+merge, excluir com `ConfirmDialog`); error handling via Snackbar. Reuso de `AtletaDialog`/`ConfirmDialog`. Inclui fix `buscarAtletaPorId` `/v1`→`/api/v1` (404 ao editar, achado do Codex no QA). 234 testes (+4). PR #14 mergeado. **Parte 2/2 — fecha a migração coach/atletas** (paridade operacional com o `/atletas` legado; Provas e descomissionamento do legado ficam de follow-up). |
| `add-waitlist` | implementado (4.3 pré-go-live adiada) | 2026-06-29 | `changes/archive/2026-06/2026-06-29-add-waitlist/` — Waitlist pública pré-lançamento (M · Full, fora de sprint). **Backend:** `POST /api/v1/waitlist` (público, sem tenant, em `publicPaths` + `shouldNotFilter`), `tb_waitlist` (V43, constraint única `email_normalized`, `aceite_lgpd`), honeypot + rate-limit Caffeine 5/min/IP → 429, idempotência por e-mail resiliente a corrida (`DataIntegrityViolationException` estreitada ao índice). **Frontend:** rota `/waitlist` (e `/privacidade` placeholder) fora de `ProtectedRoute` — form no tema com perfil/faixa condicional, checkbox LGPD + link, honeypot, foco no sucesso; CTAs da landing repontados (`Começar` → `/waitlist`, `Entrar` → `/auth/login`). PRs backend #14 + front #21 mergeados 2026-06-30. QA 4 reviewers (multi-tenant aprovado), 10 fixes aplicados; A1 (XFF spoofing) e enumeração 200/201 aceitos como limitação MVP. 1050 testes backend + 259 front. **Pendências go-live:** política real em `/privacidade` (4.3), `CORS_ALLOWED_ORIGINS` prod, reforço de proxy confiável no XFF. |
| `redesign-landing-premium` | implementado (6.3/7.3 follow-up) | 2026-06-30 | `changes/archive/2026-06/2026-06-30-redesign-landing-premium/` — Redesign **premium** da landing (L · Full, frontend-only, fora de sprint). Porta a landing v2.0 token-driven (módulo `landing/`: content/primitives/ProductUI/sections/AccessForm + 11 seções), tema MUI premium escopado via `ThemeProvider` aninhado (`landingTheme` + augmentation `surfaceShift` em `mui.d.ts`), captura inline integrada ao `/api/v1/waitlist` (reusa `useWaitlist`; nº atletas → faixa, perfil=TREINADOR, LGPD+honeypot), **vídeo de showcase** no hero (loop mudo, fade na base, hero sobreposto) e link "Entrar". **Fix de rota `/` (AC1):** index do `ProtectedRoute` sombreava `/` → home legada movida para `/inicio`. QA frontend+clean-code (11 fixes; "FAQ teclado" falso positivo). 271 testes, audit de cor limpo, verificado no navegador. PR front #22 mergeado 2026-06-30. **Supera `marketing-landing-page`** (arquivada junto). Pendências: assets reais + bio do fundador (6.3), nav mobile (7.3). |
| ~~`marketing-landing-page`~~ ✅ superada | — | 2026-06-30 | `changes/archive/2026-06/2026-06-30-marketing-landing-page-superseded/` — Landing inicial (informacional, sem captura de lead). **Superada** por `redesign-landing-premium` (landing premium token-driven + captura inline). Arquivada como superada. |
| `infer-threshold-from-race-result` | ✓ | 2026-07-17 | `changes/archive/2026-07/2026-07-17-infer-threshold-from-race-result/` — 2ª fonte de `paceLimiarEstimado` (S · Full): quando o atleta tem uma prova real, válida (`foiRealizada`, `tempoRealizado`, distância resolvida 5000-21097m, não cancelada) e recente (90 dias), ela passa a ter precedência sobre a inferência passiva por quintil de `infer-thresholds-from-recent-workouts`. Fórmula de Riegel isolada + offset `+8s/km` (não reusa `RiegelCalculator`, acoplado ao pipeline de projeção — duplicação deliberada, D2/D2b do design.md). Migration V56 (`fonte_limiar_pace`, aditiva); fonte persistida no cálculo (não recomputada no read) e exposta ao coach via `AtletaPerfilCoachOutputDto.fonteLimiarEstimado`. `fcLimiarEstimado` inalterado (sempre quintil, `Prova` não tem FC própria). 4 rodadas de pre-mortem adversarial (Codex) no DoR corrigiram 5 achados reais antes da implementação. QA gate (3 revisores paralelos) sem Critical/Important; 1 achado Low (query sem filtro `CANCELADA`, corrigido) e boundary tests (BVA) adicionados. 1747 testes. PR backend #43 mergeado. |
| `add-athlete-billing-plan` | ✓ | 2026-07-17 | `changes/archive/2026-07/2026-07-17-add-athlete-billing-plan/` — Dados de cobrança do atleta (M · Full, backend+front): `tipoPlanoAtleta` (MENSAL/TRIMESTRAL/SEMESTRAL/ANUAL) + `dataVencimentoPlano`, migration V57 aditiva (nullable, sem backfill); `StatusVencimentoPlano.resolver` (função pura, janela de alerta 7 dias) exposto em `AtletaOutputDto`, perfil e roster do coach. Frontend: edição em `AtletaDialog`, badge no perfil (`CoachAthleteProfilePage`) e coluna "Vencimento" no roster (`CoachAthletesPage`). QA gate (code/security/clean-code + frontend-reviewer paralelos) corrigiu 2 Important: `GET /api/v1/atletas`/`{id}` sem `@PreAuthorize` (IDOR horizontal — qualquer `ATLETA` listava o roster inteiro do tenant, agora vazando também status de cobrança; restringido a `TECNICO`/`ADMIN`) e coluna de vencimento reusando formatador sem ano (`dd/MM` → `dd/MM/yyyy`, extraído para `billingPlanAdapters.ts`). Inconsistência de `Clock` (2 pontos com `LocalDate.now()` direto vs. 1 injetado) avaliada e mantida — trade-off já documentado no `design.md` D2/D3. PRs backend #44 + front #39 mergeados. Suíte back 1772 / front 570. |
| `refactor-color-system-premium-v2` | ✓ | 2026-07-18 | `changes/archive/2026-07/2026-07-18-refactor-color-system-premium-v2/` — Paleta "Premium v2.0" (L · Full, frontend-only): elimina colisão semântica entre categorias (`trainingType`/`trainingStage`) e tokens `semantic` (danger/warning/success/info); tira o lime de marca de contextos indevidos (Lime Discipline — allowlist `primary.*` + papéis de seleção/marca da sidebar); garante contraste WCAG AA nos 8 categóricos dedicados contra os 4 fundos de elevação (`categorical.violet`/`magenta` clareados de `#A855F7`/`#E0529C` para `#B670F8`/`#E364A6`, matiz preservado); consolida `theme.premium.ts` como single source of truth (`glass`, `sidebar`, já sem a feature-flag `premium-v2` — consolidada incondicional em 28/06). 3 testes automatizados novos (`theme.premium.test.ts` CA3, `limeDiscipline.test.ts` CA2, `contrastMatrix.test.ts` — 106 casos). Visual diff ao vivo (login real como coach) nas 3 telas-âncora (cockpit dashboard, athlete plan view, workout detail) achou e corrigiu 2 bugs reais de truncamento de texto sem indicação (`CurrentWeekPlan`: grid 7→4 colunas/linha; `MetricTile`: `noWrap` → `line-clamp:2`). QA (frontend-reviewer + clean-code-reviewer) corrigiu valor morto (`WORKOUT_STATUS_COLORS.PARCIAL` inalcançável em runtime) e mecanismo de enforcement morto/desatualizado (`forbidden-uses.ts` referenciava `categorical.cat4..cat8` já renomeados); duplicação `colors.ts`/`theme.premium.ts` (pré-existente) registrada como follow-up, não corrigida. PR front #40 mergeado. Suíte 676. |
| `refactor-threshold-orchestration` | ✓ | 2026-07-18 | `changes/archive/2026-07/2026-07-18-refactor-threshold-orchestration/` — Refactor interno sem mudança de comportamento observável (S · Fast, backend): extrai a orquestração de inferência de limiar (staleness → busca de treinos/provas → precedência prova-vs-quintil → mutação de `PlanoMetaDados` → log de outlier), antes em métodos privados de `TsbServiceImpl` só alcançáveis via `Method.setAccessible`, para um novo colaborador público testável `AthleteThresholdUpdater` (`services/helper/`). Unifica um predicado de staleness duplicado 3x (com divergência de semântica de `null` não intencional entre as cópias) em `ThresholdInferenceService.isFcLimiarDesatualizado`/`isPaceLimiarDesatualizado`; `CoachAthleteProfileServiceImpl` e `ThresholdConstraintFormatter` migrados para os métodos novos (ambos ganharam `ThresholdInferenceService` injetado). `TsbServiceImpl`: 748 → 643 linhas, volta a respeitar o teto informal do CLAUDE.md; perde `ProvaRepository` do construtor. Os 10 cenários de teste originais (staleness FC/pace, precedência prova-vs-quintil, CA5 da change `infer-threshold-from-race-result`, outlier WARN/INFO) portados para `AthleteThresholdUpdaterTest` chamando o método público direto, sem reflection; golden-master do prompt (`PlanoTreinoPromptBuilderGoldenTest`, `ProgressaoPromptIntegracaoTest`) confirma saída byte-idêntica. QA (code-reviewer + clean-code-reviewer) zero Critical nos dois; corrigiu null-check explícito em `atualizarLimiares` (método agora público, antes privado com atleta sempre garantido não-nulo), teste do caminho `assessoria == null` (dívida pré-existente, nem o teste original via reflection cobria) e 4 mensagens de log que ainda citavam o nome do método privado pré-extração. PR backend #45 mergeado. Suíte 1786. |
| `fix-volume-realizado-recalculo` | 11/11 | 2026-07-23 | `changes/archive/2026-07/2026-07-23-fix-volume-realizado-recalculo/` — S · Full, backend, PR **#48** (`d2558a2`). `volumeRealizadoKm` passou a ser calculado na leitura de `PlanoServiceImpl.buscarPlanoPorAtleta`, somando os `TreinoRealizado` da janela do plano em vez de depender do match com `TreinoPlanejado`: treino sem vínculo (`.fit`, Strava, log manual em dia sem treino previsto) **deixou de sumir do volume da semana** — o coach via menos volume do que o atleta correu. Smoke real confirmou `12.62 → 18.92 km` após registrar 6,3 km sem vínculo. Arquivada 2026-08-10, **18 dias após o merge**: entregue e invisível ao planejamento nesse intervalo. |
| `fix-reconciliation-it-auth` | 13/13 | 2026-08-02 | `changes/archive/2026-08/2026-08-02-fix-reconciliation-it-auth/` — S · Fast, backend, **zero diff em `src/main`**, PR **#59** (`50c4fb6`). `Task5p1ControllerIT` estava vermelho desde 2026-05-14 por dois defeitos empilhados no próprio teste: role `ROLE_USER`, que não existe no domínio, e ausência de JWT, sem a qual o `JwtTenantFilter` não popula o `TenantContext`. O custo não era o vermelho — eram os **3 endpoints de reconciliação manual sem cobertura de contrato por 2,5 meses**, autorização e isolamento multi-tenant incluídos. Sobreviveu tanto tempo porque o Surefire só roda `*Test` e não havia CI. Deixou o `verify` verde, que era a pré-condição de `enable-backend-ci`. Arquivada 2026-08-10. |
| `enable-frontend-ci` | 23/23 | 2026-08-04 | `changes/archive/2026-08/2026-08-04-enable-frontend-ci/` — PR front **#53** (`65ad0a2`) e branch protection em `develop` exigindo `Lint, build e testes` + `E2E (Playwright)`. Par da `enable-backend-ci`: fecham o mesmo gap estrutural, e foi a primeira das duas. Arquivada 2026-08-10. |

---

## Radar — specs no horizonte (não escalonadas ainda)

Items identificados como importantes para a jornada do coach, mas sem sprint definido. Revisitar antes de cada ciclo de planejamento.

**Limpeza de 2026-07-20 (auditoria CPO weekly):** removidas 10 linhas já concluídas/escalonadas
(`coach-plan-review-workflow`, `athlete-profile-drilldown`, `manual-training-entry-lightweight`,
`notify-athlete-week-closed`, `add-daily-readiness-checkin`, `complete-authorization-controllers`,
`intervals-icu-workout-push`, `fit-lap-metrics-parser`, `fit-lap-derived-metrics`,
`fit-running-dynamics-ingestion`) — todas já rastreadas na tabela de sprints principal acima, sem
precisar duplicar aqui. `complete-authorization-controllers` estava com status incoerente ("Escalonado
Sprint 14" sem tachado, apesar de concluída 2026-07-14) — corrigido removendo a duplicata.
`intervals-icu-workout-push` tinha uma linha de tabela markdown corrompida (células extras `| — | — |`)
— corrigido junto.

| Change | Status | Por que está no radar | Ação sugerida |
|---|:---:|---|---|
| ~~`customize-keycloak-login-theme`~~ | ✅ **ENTREGUE 2026-08-16** — PRs `menthoros-infra` **#11** e **#13** mergeados em `main`. S · Full, só `menthoros-infra`, **zero diff em `apps/`**. Arquivada em `archive/2026-08/2026-08-16-customize-keycloak-login-theme/` | Consequência direta da migração para PKCE: a senha saiu da aplicação, então o login acontece **na tela do Keycloak** — era o tema padrão, em inglês, sem marca. **O passe adversarial derrubou a premissa central:** o `Dockerfile.keycloak` era **órfão** e não existia caminho para o tema chegar ao container; criá-lo virou escopo. Segundo achado: o `sync-realm.sh` aplicava `loginTheme` cegamente e o `no-delete` **não** protege atributos de realm — sync contra alvo sem o tema derrubaria a tela de login. | **Entregue em local e HomeLab.** Tema com `parent=keycloak.v2` (não `keycloak`, que é o legado), sem `.ftl` próprio; PT-BR ligado por `internationalizationEnabled` — a tradução já vinha completa do tema base, o que faltava era a flag no realm; contraste medido e registrado no CSS; fontes **auto-hospedadas**, tirando o Google da tela pré-auth; preflight virou **guarda no `sync-realm.sh`** e o `keycloak-config-cli` saiu de `:latest`. **Rendeu três desdobramentos:** a seção 4 (Railway) virou `migrate-keycloak-dev-to-repo-build`; o `.env.sync` sobrescrevendo variáveis exportadas virou `menthoros-infra#12`; e a recuperação de senha, órfã entre changes, entrou no radar. O HomeLab entrou como **terceiro ambiente**, ausente do design original apesar de ser o alvo do `.env.sync` — e o ensaio nele pagou por si na primeira execução. |
| `coach-meta-intensidade-editor` | ✅ **DESBLOQUEADA em 2026-08-22** — `fix-fc-alvo-base-inconsistente` mergeada (backend PR #75, front PR #84). Era ⏸️ **Bloqueada** | **M · Full**, backend + front. Traz a **meta de intensidade do Garmin** para o editor do treinador: seletor de escolha única (`Sem objetivo` · `Ritmo` · `Zona de FC` · `FC personalizada`), validado na UI do Garmin Connect. Hoje `TreinoEditDialog.tsx:106` tem um campo rotulado **"Zona alvo"** ligado a `fcAlvoEtapa`, em **texto livre**: o rótulo não corresponde ao dado, e o parser aceita só 3 formatos não documentados — `"150 bpm"` (valor único), `"140 - 150 bpm"` (espaço no hífen) e `"Z2 (140-150 bpm)"` viram **nenhuma meta**, sem aviso. **Ressalva honesta:** a contagem no banco de dev **refuta a urgência** — zero etapas caem em "não reconhecido" e zero usam formato de zona, porque as etapas são geradas pelo LLM e saem consistentes. A change se justifica por **prevenção e clareza**, não por dano medido. | Escalonar **depois** da `fix-fc-alvo-base-inconsistente`, que estabelece a meta declarada no modelo canônico. Antes disso, o editor ofereceria uma escolha que o backend ainda resolve por precedência. |
| ~~`add-post-workout-debrief`~~ | 🔴 **Fechada — redundante** (CPO review 2026-07-24) | O `WorkoutAnalysisListener` (painel "Coach Insight") já entrega planejado-vs-realizado + score + recomendação via Sonnet em produção; o gap real (camada determinística + loop de aprendizado) é de `add-workout-metrics-analyzer`. | Nenhuma. Spec mantida em `changes/` como referência (não deletada), fora de sprint. Se ressurgir em auditoria, é superseded — não zumbi. |
| ~~`add-weekly-review-consolidation`~~ | ✅ **Entregue** — Fatia 1 de 3 (backend PR #50, 2026-07-24; arquivada em `archive/2026-07/`) | Núcleo determinístico: `tb_revisao_semanal` 1:1 com `PlanoSemanal`, geração no encerramento (listener `SemanaEncerradaEvent`), endpoint read-only coach-only, `weekOverWeekDelta`, congelamento (ADR-0006). Suíte 2134 verde, QA sem Critical. Destrava F2/F3. | — (concluída) |
| ~~`add-weekly-review-llm-focus`~~ | ✅ **Entregue** — Fatia 2 de 3 (front PR #46 2026-07-26 + backend PR #52 2026-07-27; arquivada em `archive/2026-07/`) | Narrativa `nextWeekFocus` (template determinístico + upgrade opcional por LLM em executor dedicado), `WeeklyFocusConsistencyChecker`, injeção no prompt com janela de 7d (D11) e loop de aprendizado via `consumedReviewOutcome` no `PlanoSemanal` (D9). Migration V72 + renomeação coordenada do contrato (D13/ADR-0007). Backend **2190** verde, front **748/748**. **Nasce desligada** (`menthoros.weekly-review.llm.enabled=false`) — em produção só roda o template, custo LLM zero. | **Gate A1 (custo LLM) segue ABERTO** — task 0.4 adiada no arquivamento: exige tráfego real com a flag ligada, que não existe pré-pilot. Instrumentação pronta (`focusSource` nos contadores `weekly_review.focus.rejected` e `weekly_review.outcome`), então medir é só rodar o canary via `CostTrackingAdvisor`. **Ligar a flag continua condicionado a esse gate.** |
| ~~`add-weekly-review-coach-card`~~ | ✅ **Entregue** — Fatia 3 de 3 (front PR #45, 2026-07-25; arquivada em `archive/2026-07/`) | Card read-only "Revisão semanal" no drilldown do atleta: hook `useWeeklyAthleteReview` (404 → empty, não erro), adapter + VM, estados loading/empty/error, read-only assegurado por teste. Renderiza `nextWeekFocus` quando presente — a narrativa da F2 aparece sem trabalho de front adicional. Lint + build + suíte **748/748** verdes. | — (concluída) |
| `add-retention-radar-lite` | **Iniciada — 1/28 tasks** (era "Pronta / Open Questions fechadas"; a primeira task foi marcada, então a change saiu do estado de espera) — fatia do bloco Retention Loop 90d (CPO weekly 2026-07-24, "Fora da caixa #1") | 7º motivo `RISCO_RETENCAO` na fila de atenção existente: fase D1-D120 + risco explicável (5 sub-sinais já disponíveis hoje) + Next Best Action estático (RF4). Traz o Retention Loop 90d (`prd/prd-retention-loop-90d.md`) ao radar canônico — antes maduro em `prd/` mas ausente daqui (gap apontado pelo CPO weekly). Reaproveita `SinalAtencao`/`CoachAttentionSignalEvaluator`; informa o coach na fila que ele já usa. | M · Full, backend + front mínimo. Trilha Full concluída: product-review (Go) + pre-mortem Codex (2 críticos corrigidos) + 6 Open Questions decididas no DoR. Rodar `/implement init add-retention-radar-lite` (ou o `spec-reviewer` formal antes). RF5 editável/jornada/coorte ficam para o bloco completo. |
| `add-external-call-resilience` | **Priorizada para a próxima sprint** (decisão do founder 2026-07-26) — antes "sem sprint atribuído / gap de visibilidade" desde o CPO weekly 2026-07-20. **Reescrita após grilling (2026-07-26): M · Full → S · Fast**, spec e tasks refeitas, branch `feature/add-external-call-resilience` criada | O gap deixou de ser teórico. A `add-weekly-review-llm-focus` adicionou um **terceiro** call site de LLM sem timeout (`WeeklyFocusModelClient`) e o `/codex:review` de 2026-07-26 mostrou o caminho de degradação. O grilling revelou que o risco era **subestimado**: a geração de plano é `@Transactional` e a chamada ao LLM roda **dentro** da transação, com lote de 4 em paralelo e pool Hikari default de 10 — cada chamada pendurada segura uma **conexão de banco**, não só uma thread. Um provider lento não derruba a IA: derruba login e telas do atleta. | **Escopo enxugado — circuit breaker saiu (ADR-0008).** Fica: timeout **por rota** de LLM (o `MultiModelConfig` já é por rota; o timeout é que vive no cliente HTTP do `ChatModel`, e `SIMPLE`/`PLANO` são o mesmo provider), timeout do Strava 5s/10s, timeout deixa de ser retentado, teto total na geração de plano, corte do lote por N falhas consecutivas e `Timer` por rota (hoje **não há** instrumentação de latência — só `Counter`). Resilience4j vira não-objetivo com gatilho de reavaliação registrado no ADR-0008. Duas derivadas a abrir: LLM dentro da transação, e reprocessamento de análises `FAILED` (hoje terminal). Já verificado: intervals.icu **já tem** timeout — só o Strava e o LLM faltam. |
| `refactor-llm-call-outside-transaction` | 🔴 **PRIORIDADE ALTA — promovida ao Bloco de Segurança** (decisão do founder 2026-07-26, no mesmo dia da criação). **Dívida com prazo, não com data**: não dói até a primeira assessoria grande, e aí dói de uma vez | A geração de plano é `@Transactional` e a chamada ao LLM roda **dentro** da transação: ~80s segurando uma conexão do pool (default 10) sem fazer nada. Com o lote em 4 paralelos, **4 das 10** ficam fixadas — sobra pouco para login, telas do atleta e dashboard. **Ponto de ruptura calculável:** lote leva `N × 20s`, então em **~90 atletas** ele passa dos 30 min do `recovery-limite-min`, cujo próprio comentário afirma que "nenhum lote real dura tanto" — uma assessoria de 100 alunos já invalida a premissa. Pior: a reação natural a lote lento é subir `BATCH_PLAN_LLM_CONCORRENCIA` (env var, sem deploy), e de 4 para 8 fixa 8 das 10 conexões. O timeout do ADR-0008 limita a posse, não desfaz o acoplamento. | M · **Full — `design.md` é bloqueante**: hoje o código faz `Hibernate.initialize` no grafo do atleta e usa as entidades depois da chamada; fora da transação elas ficam detached e `LazyInitializationException` é o risco real. Definir o que atravessa a fronteira define o tamanho do diff. Faz sentido **depois** de `add-external-call-resilience` (não mexer duas vezes no mesmo fluxo), mas não há dependência técnica. Precedente a seguir: `intervals-icu-activity-ingestion` já mantém a chamada HTTP fora da transação. |
| ~~`fix-reconciliation-it-auth`~~ | ✅ **ENTREGUE 2026-08-02** — PR backend #59 mergeado em `develop` (merge `50c4fb6`), **13/13 tasks**. ✅ **Arquivada em 2026-08-10** — `changes/archive/2026-08/2026-08-02-fix-reconciliation-it-auth/`. S · Fast, backend, **só `src/test`** | `Task5p1ControllerIT` está vermelho desde **2026-05-14**: 14 dos 20 testes retornam 403. Dois defeitos empilhados, os dois no teste — role `ROLE_USER` que não existe no domínio (as reais são `TECNICO`/`ATLETA`/`ADMIN`), e ausência de JWT, que impede o `JwtTenantFilter` de popular o `TenantContext`, fazendo `getRequiredTenantId()` estourar. O `X-Tenant-ID` que o teste envia é ignorado pela produção. **O custo real não é o vermelho:** os 3 endpoints de reconciliação manual estão **sem cobertura de contrato há 2,5 meses**, incluindo autorização e isolamento multi-tenant — justamente o fluxo em que o treinador vincula atividade externa ao plano do atleta. Sobreviveu tanto tempo porque o Surefire só roda `*Test` e não há CI no repo. | Baixo risco e baixo custo: **zero diff em `src/main`**, e o padrão de correção **já existe no repo** (`jwtDe` em `OnboardingSensitiveDataAccessIT:277-281`), então não há decisão de design. 11 tasks, 6 CAs. Um ponto genuinamente em aberto: quantos dos 20 testes falham por motivo legítimo depois da auth corrigida — as asserções são de 2026-05-03, e divergência real de contrato é **achado de produção**, não asserção para ajustar. **Desfecho:** a auth foi corrigida por JWT (`1d5e90e`) e o contrato dos 3 endpoints restaurado — o `verify` voltou a ficar verde, que é a pré-condição de `enable-backend-ci`. Resta **arquivar**. |
| ~~Habilitar CI no `menthoros-backend`~~ | ✅ **Virou change** — `enable-backend-ci` (S · Full, 14 tasks), aberta 2026-08-02 e escalada ao Bloco de Segurança. *(Linha mantida como rastro da origem do item; o acompanhamento é lá.)* | O repo **não tem `.github/workflows`**: o "CI verde + branch protection" descrito no `CLAUDE.md` da raiz não existe na prática, e as branch protections são de fachada sem status checks. Consequência já materializada: `Task5p1ControllerIT` ficou **2,5 meses** vermelho sem sinal, porque o Surefire só executa `*Test` e os `*IT` só rodam em `mvn verify`, que ninguém roda. É a causa raiz de `fix-reconciliation-it-auth`, não um vizinho dela. | **Pré-condição satisfeita em 2026-08-02:** `fix-reconciliation-it-auth` deixou o `verify` verde, então o CI não nasce mais vermelho — a janela para ligar é agora, antes que outro `*IT` apodreça. Sequência restante (dentro de `enable-backend-ci`): CI com `verify` (não só `test`, senão o gap continua) + execução agendada + status check exigido em `develop`. |
| `migrate-keycloak-dev-to-repo-build` | **Criada 2026-08-16** — S · Full, só `menthoros-infra` (provavelmente zero diff: a mudança é de configuração do serviço) | **Desmembrada da seção 4 de `customize-keycloak-login-theme`**, que entregou o tema em local e HomeLab. O serviço `menthoros-keycloak` no Railway roda a **imagem pública** `quay.io/keycloak/keycloak:26.6` com `source.repo: null` — **nada que a gente construa chega ao Keycloak de dev**, nem tema, nem provider, nem customização futura. O tema é só a primeira coisa a esbarrar nisso. Há também drift: dev está numa **tag móvel** enquanto local e HomeLab estão pinados em `26.7.0`. | Trocar `source.image` por `source.repo` construindo por `docker/Dockerfile.keycloak`, com **watch patterns** (não `rootDirectory`, que estreitaria o contexto e deixaria `keycloak/themes/` de fora). Risco concentrado: **este serviço autentica todo o ambiente de dev** e não há acesso ao host — o rollback é trocar a origem de volta pelo painel. Fazer em janela de baixo uso. Duas Open Questions antes de começar: se o GitHub App do Railway enxerga o repo privado, e se o build tem cache de camada. O ensaio de local + HomeLab cobre o Dockerfile, o tema e o preflight; **não** cobre a troca de mecanismo de origem, que é o miolo. |
| Ligar recuperação de senha no Keycloak (`resetPasswordAllowed`) | **Não é change ainda — item sem dono, identificado 2026-08-16** durante a validação de `customize-keycloak-login-theme` | O realm **não tem `resetPasswordAllowed`**, então o link "Esqueceu sua senha?" não aparece e a rota `/login-actions/reset-credentials` devolve `400`. O fluxo foi destravado pela migração PKCE (a senha saiu da aplicação), mas **ninguém o ligou**: `keycloak-user-onboarding-auth` listou "troca de senha, recuperação de conta" explicitamente **fora de escopo** (`proposal.md:21`) e foi arquivada em 2026-08-11 — o item ficou órfão entre as duas changes. Consequência hoje: um coach que esquece a senha **não tem caminho de recuperação**, só contato manual. Também é a razão de a task 3.5 de `customize-keycloak-login-theme` ficar com pendência: as duas telas do fluxo (pedido de e-mail e definição de senha) **não puderam ser validadas no tema novo**, porque o recurso está desligado. | XS · Fast, só `menthoros-infra` — um atributo no `menthoros-realm.json` aplicado pelo `sync-realm.sh`. As pré-condições já existem: SMTP configurado, política de senha (`length(12) and notUsername and notEmail`) vale na redefinição, e as telas herdam o tema `menthoros` sem trabalho adicional. **Dois pontos a verificar ao ligar:** (a) a entrega de e-mail em dev — o `sync-realm.sh` documenta que a GoDaddy dá *connect timeout* a partir do Railway, então o link pode não chegar mesmo com o fluxo correto; (b) a passada visual nas duas telas, fechando a 3.5. |
| Emitir FC **e** ritmo com o alvo executado declarado (intervals.icu) | **Não é change ainda — achado de fonte primária 2026-08-21, decisão do founder 2026-08-22** | A `fix-fc-alvo-base-inconsistente` implementou o CA8 como "FC vence, ritmo desce para o texto". A investigação contra fonte primária mostrou que **o provedor guarda os três alvos e escolhe na execução** — o evento tem `target: AUTO \| POWER \| HR \| PACE` (confirmado no OpenAPI), e o criador confirma no fórum. A regra atual **descarta informação que o canal aceitaria**: o ritmo prescrito pelo treinador vira texto quando poderia viajar como alvo secundário. O FIT também tem `secondary_target_*` (campos 19-22), então "os alvos não são acumuláveis" — afirmação do `design.md` da change — está desatualizado; o suporte é por dispositivo. | S · Full. **Bloqueante: reescrever o CA7** ("exatamente uma meta de intensidade"), que passa a ser falso por desenho. Só se verifica com **conta real** — mesma validação que a 5.3 de `fix-fc-alvo-base-inconsistente` ainda deve, então faz sentido agrupar as duas numa passada só. Não reabrir a change mergeada: custo de branch + PR + revalidação sem fechar a incerteza. |
| Versionar a reinterpretação de `fcAlvoEtapa` legado | **Não é change ainda — débito aceito em `fix-fc-alvo-base-inconsistente` (4b.4), registrado 2026-08-22** | Achado do Codex adversarial, classificado como ERRADO e **não corrigido por escopo**: o mesmo treino reenviado em datas diferentes pode gerar **bpm diferente por mudança de regra de interpretação**, não por mudança do atleta. O log registra o efeito, não a intenção — então, olhando o histórico, não há como distinguir "o atleta evoluiu" de "a gente mudou como lê o campo". | S/M · Full — **mexe em schema**: exige versionar a interpretação ou gravar um campo normalizado separado. Por isso ficou fora da change de correção. Companheiro natural: **4b.5** — o aviso de FC descartada é por **treino**, não por etapa, então 1 de 10 etapas perdidas produz o mesmo sinal que 10 de 10 (aceito na decisão 0.3, reforçado pelo adversarial). As duas tocam a mesma superfície de rastreabilidade e valem uma change só. |
| Semear o PMC com o CTL/ATL do intervals.icu na conexão | **Não é change ainda — identificado no `/implement init` de `intervals-icu-activity-sync-scheduler` (2026-08-22)** | A carga inicial importa 90 dias de histórico para o CTL (τ = 42) convergir a 88% do real; o intervals.icu **já calcula** CTL/ATL do atleta e expõe no wellness. Uma requisição semearia o PMC com o valor real no dia da conexão, e bastaria importar ~42 dias de detalhe por treino — menos cota e CTL certo desde o primeiro dia. | S · Full — mexe em `TsbServiceImpl` (hoje parte de zero) e tem decisão de produto: confiar no cálculo do provedor, que pode usar TSS diferente do nosso. Depois do scheduler validado em produção. |
| Expor métricas `.fit` já calculadas no drilldown | **Não é change ainda — quick-win identificado 2026-07-20** | A sequência `.fit` está completa (GCT, equilíbrio E/D, Pw:HR, EF por volta) mas 100% invisível no front — zero consumo desde `fit-running-dynamics-ingestion` (2026-07-13). Achado "fora da caixa" do CPO weekly: maior gap de exposição vs. esforço do backlog atual. | XS (3-5 tasks, frontend-only, zero endpoint novo — dados já nos DTOs de `GET /realizados/{id}`). Abrir com `/opsx:propose` quando priorizado. |

---

## Nota de capacidade

Com 1 dev, o caminho completo até a fronteira do MVP (Bloco 0 + 1 + 2) é da ordem de **~20 sprints / ~40 semanas**, sem contar o Bloco de Segurança. Trade-off explícito da escolha IA-first: a base fica robusta, mas o treinador só vê a jornada madura no fim.

**Fast-track sugerido** (se quiser antecipar o "momento de valor" sem abrir mão da base): Bloco 0 → `build-skills-core-foundation` → `debito-tecnico-camada-ia` → `add-coach-shell-dashboards` → `add-coach-attention-queue` + `add-recommendation-explainability`. Entrega um treinador operando sobre uma base de IA já sólida, e o RAG/personalização entram no "MVP+1".
